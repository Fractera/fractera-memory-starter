// ТИПЫ и определение языка для просмотрщика кода.
//
// Файл без зависимостей: его читают и клиент, и сервер, и он уезжает в чужие
// проекты вместе с инструментом.
//
// 🔒 ПЕРЕНЕСЁН ИЗ `fractera-next-starter/_tools/code-view/types/code-view.ts` (194-10). Адаптация одна:
// соответствие «расширение → язык» живёт в `code-langs.mjs` и переэкспортируется отсюда, потому что его
// без сборки читают дверь описания и приборы памяти.

export type CodeViewLabels = { loading: string };

export { BY_EXT, isCodeName, langOf } from "./code-langs.mjs";
