# Законы, по которым здесь строят

**Кому.** Тому, кто РАЗРАБАТЫВАЕТ память. Агенту памяти это читать не нужно — его инструкция
`MEMORY-AGENT.md`, и она короткая намеренно.

🔒 **С 2026-09-13 `CLAUDE.md` — ИНСТРУКЦИЯ СТРОИТЕЛЯ, А НЕ АГЕНТА.** Файл читает тот, кто открывает
`claude` в этой папке, то есть человек, пришедший менять продукт; работающий код его не читает
вовсе. Держать там описание агента памяти значило отдавать строителю чужую личность — и он честно
её принимал.

🪦 **ЭТОТ ТЕКСТ ЛЕЖАЛ В `CLAUDE.md` ДО 2026-09-09 И БЫЛ ОТТУДА УБРАН.** Claude Code читает
`CLAUDE.md` из рабочей папки автоматически — значит агент памяти загружал бы девяносто шесть строк
истории вместо логики работы. Инструкция даёт модели правила действия; история — учёту.

---

**Что это.** Отдельная служба на `:3700`. Снаружи о ней известно ровно одно: **договор**
(`contract.mjs`, отдаётся по `GET /v1/contract`). Всё остальное — её частное дело.

---

## 🔒 ЗАКОН ПЕРВЫЙ: ПАМЯТЬ — ЧЁРНЫЙ ЯЩИК, И РЕШЕНИЯ ПРИНИМАЮТСЯ ВНУТРИ

**Слова владельца 2026-09-09, дословно:**

> «мы делаем память как чёрный ящик, который должен принимать и возвращать данные, и те процессы,
> которые у них происходят внутри, она должна производить внутри самостоятельно. То есть это не
> агент какой-то снаружи должен уметь это делать, понимаешь, принимает решения добавлять строку,
> добавлять таблицу, или добавлять несколько таблиц, или программировать что-то. **Агент снаружи
> говорит: на тебе это и сделай то, что надо.**»

> «Я тебе сказал: это должно выглядеть, как будто мы обращаемся в облако Google — просто отправляем
> запрос, получаем результат, а что там происходит, не важно».

**Что из этого следует для каждой правки здесь:**

- 🛑 **Ни одно решение о хранении не выносится наружу.** Строка это, новая таблица, несколько
  таблиц или заявка в разработку — решает служба. Наружу уходит **результат**, а не развилка.
- 🛑 **Договор не объясняет устройство.** В нём нет и не будет слов «реестр», «признак»,
  «хранилище», «вектор», «граф». Потребитель, узнавший их, немедленно начнёт на них рассчитывать.
- 🔒 **Отказ несёт то, что потребителю НАДО СДЕЛАТЬ, а не то, что у нас случилось.** Готовый вопрос
  человеку — да; наши внутренние причины — нет.

✗ **ЧЕМ ЭТОТ ЗАКОН ОПЛАЧЕН.** Прежняя память жила внутри приложения бота (5 997 строк), и сервер
инструментов агента **импортировал её исходники по файловому пути**. Через этот шов прошёл дефект,
из-за которого запись в память была недостижима **сутки**. И через него же просьба «запиши мой день
рождения» стоила **278 секунд**: у памяти не было способа завести новый род значения, работа
вывалилась наружу — а снаружи она выразима только программированием.

---

## 🔒 ЗАКОН ВТОРОЙ: СТРОИМ ЗАНОВО, А НЕ ПЕРЕНОСИМ

**Решение владельца того же дня, дословно:** «наша архитектура, несмотря на то что работает…
создавалась без видимого плана, в большей степени хаотично… Я против переноса, я за то, чтобы с
нуля, с чистого листа, в новой памяти начать строить архитектуру таким образом, чтобы она сначала
умела выдавать простые результаты, затем всё более сложные, и в конечном итоге научилась сама себя
масштабировать. **Если мы просто её перенесём, то получим старый план на новом месте.**»

🔒 **Прежний код — источник ПРИНЦИПОВ, а не строк.** Заглядывать в него, чтобы понять, за что
заплачено, — правильно. Копировать оттуда — значит привезти сюда хаос, в котором он рос.

**Направление задано, порядок — нет:** сначала простые результаты → потом сложнее → в конце
способность масштабировать себя саму. Это цель, а не список шагов.

---

## 🔒 ЗАКОН ТРЕТИЙ: ДОГОВОР — ЕДИНСТВЕННЫЙ ИСТОЧНИК, И ИЗ НЕГО ПОРОЖДАЕТСЯ ВСЁ

`contract.mjs` объявляет методы. Из него порождаются: ответ `GET /v1/contract` · схемы инструментов
агента · проверка параметров · документация.

🛑 **Схемы инструментов агента приходят по HTTP, а не импортом файла.** Пока схема берётся импортом —
отдельность есть договорённость; когда по HTTP — факт.

🛑 **Рукописной копии договора не существует нигде.** Список, написанный руками рядом с кодом,
расходится с ним молча — в этом проекте измерено четырежды за три дня.

**Сторож:** `npm run check` — версия по semver · у метода сказано, когда звать, что вернёт и что при
промахе · **типы параметров только из JSON Schema**. Последнее оплачено сутками невидимого отказа.

---

## 🔒 ЗАКОН ЧЕТВЁРТЫЙ: ПАМЯТЬ — ИНСТРУМЕНТ ОДНОГО АРХИТЕКТОРА, А НЕ ПУБЛИЧНАЯ СЛУЖБА (2026-09-14)

**Слово владельца, дословно:** «мы помним о том что это инструмент архитектора а не публичный инструмент все что сюда загружается и
все что отсюда достается делается одним-единственным человеком. По этой причине критической проблемы нету. А для создания чата
пользователями и других заданий между различными людьми во избежание проблемы о которой ты говорил мы должны будем создавать работу
по другой архитектуре. Так и было задумано так и надо но об этом надо написать в документе чтобы ты не входил в заблуждение. То есть мы
просто усиливаем фильтрацию и делаем ответ более качественным но не ставим перед собой задачу защиты персональных данных».

🔒 **ОТСЮДА:** `who` в памяти — это «о ком речь», а не граница доступа. Фильтр по `who` делает ответ **точнее**, а не **безопаснее**:
чужой объект в ответе — дефект качества, а не утечка. Разграничения прав между людьми здесь нет и не строится.
🛑 **ЧАТ МЕЖДУ РАЗНЫМИ ЛЮДЬМИ И ЗАДАНИЯ МЕЖДУ НИМИ — ДРУГАЯ АРХИТЕКТУРА.** Не достраивать защиту персональных данных внутри этой
памяти и не предлагать её как решение: это подмена задачи. ✗ Оплачено в 200-6: агент поднял «чужой паспорт в ответе» как вопрос
защиты данных, хотя владелец держит память как свой инструмент.
🔒 **СВЯЗЫВАЮТСЯ ПАРАМЕТРЫ, КОТОРЫЕ ИМЕЕТ СМЫСЛ СВЯЗЫВАТЬ** (то же слово): спросили про ссылку — паспорт в ответ не идёт; род
найденного сверяется с тем, о чём спрашивают.

🪦 **ОБЕЩАНИЕ «ИСКАТЬ БЕЗ ИИ» АННУЛИРОВАНО ТЕМ ЖЕ ДНЁМ СЛОВОМ ВЛАДЕЛЬЦА, ДОСЛОВНО:** «Аннулируем обещание искать без искусственного
интеллекта вообще. Лишний костыль который тратит время и отвечает только на английском языке в ряде случаев мне не подходят. Мне нужна
стратегия которая ставит центр работу со схемой баз данных». Причина — матрица 200-7: механика слов не совпадает с русским вопросом никогда,
а на английском даёт уверенные ложные попадания по служебным словам. **Снят порядок «сначала механика без ИИ»; остаётся требование к отчёту**
(чем достали · три ступени оценки) — его владелец не отменял. Замена — стратегия «в центре схема базы данных» (планируется).

## 🔒 ЗАКОН ПЯТЫЙ: БОЛЬШАЯ ЧАСТЬ ДАННЫХ ДОСТАЁТСЯ БЕЗ ИИ, И ОТЧЁТ НАЗЫВАЕТ, ЧЕМ ДОСТАЛИ (2026-09-14) — 🪦 первая половина аннулирована, см. выше

**Слово владельца, дословно:** «Самое главное помни обещание о том что мы обещаем большую часть данная доставать без использования
искусственного интеллектом мгновенно через греп и другие твои технологии это должно быть в отчётах вот тестировании чем именно ты
доставал и давать этому оценку правильно неправильно абсолютно неправильно вот эти три шкалы».

🔒 **ПОРЯДОК ИЗВЛЕЧЕНИЯ — ОТ ДЕШЁВОГО К ДОРОГОМУ:** механически (SQL, совпадение слов, греп — мгновенно, без ИИ) → встраивание
(модель эмбеддингов, без хода рассуждения) → ход модели. Следующая ступень зовётся, только когда предыдущая не нашла.
🔒 **ОТВЕТ НАЗЫВАЕТ СПОСОБ У КАЖДОГО НАЙДЕННОГО** — поле `found_by`; иначе «достали без ИИ» — обещание, которое нечем проверить.
🔒 **ОТЧЁТ ТЕСТИРОВАНИЯ У КАЖДОГО ИЗВЛЕЧЕНИЯ ПИШЕТ:** чем достали · оценку по трём ступеням — **правильно / неправильно / абсолютно
неправильно**. Два деления («прошло / не прошло») скрывают разницу между «не то» и «вредное не то».

---

## Устройство

| Что | Где |
|---|---|
| договор — всё, что мир знает | `contract.mjs` |
| служба: три маршрута, ноль зависимостей | `server.js` |
| сторож договора | `scripts/check-contract.mjs` (`npm run check`) |
| прибор живости и отдельности | `scripts/probe/alive.mjs` (`npm run probe`) |

**Маршруты сегодня:** `GET /v1/health` (открыт) · `GET /v1/contract` (под замком) ·
любой другой `/v1/*` → `501 not-built` с объяснением.

🔒 **Замок — общий секрет машины `DATA_SECRET`**, а не новый ключ: ключ ради одной службы надо
кому-то выдавать и когда-то менять.
🔒 **Слушаем только петлю** (`127.0.0.1`) — решение владельца. **Петля не ослабляет границу:**
граница здесь — граница процесса, а не сети.
🪦 **«НОЛЬ ЗАВИСИМОСТЕЙ» ОТМЕНЕНО 2026-09-10 (шаг 178) РЕШЕНИЕМ ВЛАДЕЛЬЦА.** Здесь стояло:
🔒 «**Ноль зависимостей.** Пакет ради трёх маршрутов — ещё один способ не запуститься на чистой
машине». Закон был верен, пока у памяти не было лица: три маршрута и никакого экрана.

**Что его отменило.** Владелец потребовал испытывать память **из самой памяти**, а не из чата, и
задал способ дословно: «буквально скопируем всё, что есть, а потом уберём лишнее», «мы не
программируем всё заново». Страница приехала копией со службы `:3600` — с ней Next, React и сборка.

🔒 **ЧТО ИЗ ПРЕЖНЕГО ЗАКОНА ОСТАЛОСЬ В СИЛЕ, И ЭТО ГЛАВНОЕ:** пакет берётся, только если он **уже
стоит у соседа** и приехал вместе со скопированным файлом. Из 69 зависимостей чата взято 13 — те,
что реально импортирует перенесённый каркас. Мерилом служит сборка, а не глаз: убрал пакет —
`next build` обязан пройти.
🛑 **`/v1/*` ПРИ ЭТОМ НЕ ТРОНУТ.** Договор, глаголы и журнал остались тем же кодом на голом Node;
Next стоит рядом и получает только то, что не разобрал наш обработчик. Переписывание ядра под
фреймворк было бы ровно тем «программированием заново», против которого возражал владелец.

## Чего здесь нет и почему

**Методов.** Их ноль намеренно. Указание владельца: «сначала сделай под неё место, а потом подумаем,
что самое простое с одной стороны, фундаментальное с другой стороны, способное дать первый
результат». 🔒 **Пустой договор лучше договора, обещающего непостроенное:** метод, который виден и
не работает, тратит ход модели на вызов и ход на разбор отказа.

**Сборки.** Форма взята у `services/data` и `services/geo`: один файл, `node server.js`.
