# Отменённое — служба памяти

Здесь один раз перечисляется то, что снято, и что его заменило. В инструкции (`CLAUDE.md`) надгробий не бывает —
решение владельца 2026-09-12; история живёт здесь и в паспорте. Содержимое снятого восстанавливается из git:
`git log --diff-filter=D --format=%H -1 -- <путь>`, затем `git show <хэш>^:<путь>`.

## 2026-09-15 · шаг 203 — главная инструкция стала инструкцией управляющего памятью

- 🪦 **`MEMORY-AGENT.md`** — инструкция агента памяти. Ни одна строка кода её не читала; часть устарела («изображения, PDF — пока не
  разбираю»). Решение владельца: «Влить в CLAUDE.md, похоронить». Её правила, сверенные с кодом, — в `CLAUDE.md`.
- 🪦 **`CLAUDE.md` как инструкция строителя** — со слов владельца: «99% времени наша платформа предназначена для того чтобы управлять
  памятью – необходимость разработки памяти это один очень редкий случай». `CLAUDE.md` теперь — управляющий памятью; разработка —
  навык `memory-development`.
- 🪦 **Навыки `use-development-steps` и `use-testing` памяти** (русские копии федеральных) — влиты в `memory-development`
  (английский, решение владельца «both in english»; «Своя адаптированная копия»).

## 2026-09-15 · шаг 205-11 — паспорт переписан, история вынесена

- 🪦 **Прежняя редакция паспорта — 23 раздела, 1211 строк, 13 надгробий, отчёты шагов вперемешку с замыслом.** Слово владельца:
  «фундаментальный рефакторинг паспорта, который содержит бесконечное количество отродий, представляет из себя нечитаемые,
  непоследовательные шаги, которые больше путают, чем помогают агента». **Лежит целиком** в
  `development-docs/PASSPORT-HISTORY.md` — не удалена, а перестала быть описанием замысла и стала его историей.
- 🪦 **Прежняя нумерация разделов (§0…§23)** — заменена на §0…§14. Ссылки `§N` в коде, навыках и инструкциях перенумерованы;
  стережёт `scripts/probe/passport-refs-205-11.mjs` (три вердикта: законно · исключение на чужой документ · висящая ссылка).
  🛑 В `PASSPORT-HISTORY.md` нумерация осталась СВОЯ, и это сказано в его шапке.

## 2026-09-15 · РЕЕСТР ПРИЗНАКОВ ОТМЕНЁН ЦЕЛИКОМ — решение владельца

**Дословно:** «Всё, я понял, что нагородили мы полную ерунду. Убирай реестры признаков из
административной панели полностью, окончательно, безповоротно — делай откат или просто сотри. Мы не
будем использовать реестр признаков в проекте вообще. Очисти любое упоминание памяти о реестре
признаков — в документе и, если ещё есть, то в памяти; далее полностью упоминание о реестре
признаков, в том числе из пункта "как память обрабатывает запрос". Я дам тебе новую стратегию.»

- 🪦 **Реестр признаков `AGI-CONFIG`** — 24 записи, сторож `check-features`, каталог `GET /v1/features`
  и `GET /v1/features/{ключ}`, параметр `features` у `remember` и `recall`, подбор по кандидатам
  (`features-index`, `features-discover`), подключения элементов (`feature_consumers`, шаг 205-5) и
  страница панели «Реестр признаков» (шаг 205-6). Всё удалено, восстанавливается из git.
- 🪦 **Шаг 205 «Реестр признаков закрыт целиком»** — отменён этим же решением вместе с неисполненными
  подшагами 205-7…205-10. Итоги 205-1…205-6 и 205-11, 205-12 остаются историей работы.
- 🔒 **ЧТО СОЗНАТЕЛЬНО ОСТАВЛЕНО:** дверь `/service/memory/*` слоя данных (205-5) — она не про реестр,
  а про правило «одна дверь» и нужна любому потребителю памяти; и переписанный паспорт (205-11),
  из которого вычищен только реестр.
