# Отменённое — служба памяти

Здесь один раз перечисляется то, что снято, и что его заменило. В инструкции (`CLAUDE.md`) надгробий не бывает —
решение владельца 2026-09-12; история живёт здесь и в паспорте. Содержимое снятого восстанавливается из git:
`git log --diff-filter=D --format=%H -1 -- <путь>`, затем `git show <хэш>^:<путь>`.

## 2026-09-15 · шаг 203 — главная инструкция стала инструкцией управляющего памятью

- 🪦 **`MEMORY-AGENT.md`** — инструкция агента памяти. Ни одна строка кода её не читала; часть устарела («изображения, PDF — пока не
  разбираю»). Решение владельца: «Влить в CLAUDE.md, похоронить». Её правила, сверенные с кодом, — в `CLAUDE.md`.
- 🪦 **`CLAUDE.md` как инструкция строителя** — со слов владельца: «99% времени наша платформа предназначена для того чтобы управлять
  памятью – необходимость разработки памяти это один очень редкий случай». `CLAUDE.md` теперь — управляющий памятью; разработка —
  навык `memory-development`.
- 🪦 **Навыки `use-development-steps` и `use-testing` памяти** (русские копии федеральных) — влиты в `memory-development`
  (английский, решение владельца «both in english»; «Своя адаптированная копия»).
