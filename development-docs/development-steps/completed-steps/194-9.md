# 194-9 — вкладка «Поиск» показывает тот же блок, что загрузка

**Коммиты:** `b2a4392` (блок и `?object=`) · `d033713` (блок у каждого найденного сразу) · учёт `28db1bd` —
`fractera-memory-starter`, ветка `new-step-194` · **дата:** 2026-09-13.
🛑 **Глаз владельца на последнюю редакцию (`d033713`) нет** — названо в учёте и здесь.

## Откуда
Владелец: «на вкладке поиск нужно повторить тот же самый интерфейс который у нас находится в загрузке то есть я делаю
поиск и получаю в этом поиске и полное описание, и Самаре и запись в таблице и сам объект».

## Что делали и как
Общий `SavedView` на загрузку и поиск; `getMessageByObject` и `objectView`; `GET object-test?object=`; строки у объекта
может не быть (сохранён до таблицы) — это не отказ. Поправка владельца: блок «Что легло в память» стоит у каждого
найденного сразу, без кнопки «Открыть».

## Доказательства
- **A — прибор `object-search-view.mjs` через `https://memory.aifa.dev`, без модели:** сохранено messageId 21 → поиск нашёл
  его (0.690) → `?object=` отдал строку 21, поле в поле равную `?message=`.
- **B — негатив:** `agent-channel-routing.md` (демо-корпус 192) → `ok`, `row=null`, файл есть; чужой `tg-8-…jpg` → 404;
  уборка.

## Ошибки
Нет записанных.

## Эволюция навыков
Итог записан при закрытии шага 194 по учётной записи `28db1bd`.
