# 195-1 — раздел «Тест ссылок» в левом меню и вкладка «Загрузка»

**Коммиты (`fractera-memory-starter`, ветка `new-step-195`):** `d5e4c52` код · `74e002e` прибор; путь к браузеру заменён в 195-7
(`d67127b`, `ai-workspace` `ca085bc`). **Дата:** 2026-09-14. **Принят глазами владельца:** «greate go to next step».

## Что делали и как
- Раздел `link-test` сразу под «Тестом объектного хранилища», четыре стандартные вкладки (утверждённый паспорт §20, слово владельца
  «такая же стандартная вкладка как и для агент раг»); построена «Загрузка», остальные честно говорят «строится следующим подшагом».
- Экран и дверь **перенесены** со стенда «Тест чтения» ИИ-браузера (`read-test.client.tsx`, `api/fractera/read-test`) — закон владельца
  «скопировать, перенести и адаптировать». Адаптации: замок `benchGuard`, слова словаря памяти, строка `load_reached`.
- `lib/fractera/web.ts` — единственный путь памяти к браузеру; с 195-7 — через «одну дверь» слоя данных.
- В память ничего не пишется.

## Доказательства
**1. Дверь и прибор через публичный адрес.** `node scripts/probe/link-test.mjs` → `✓ OK`: MDN 200, «HTML: HyperText Markup Language |
MDN», текст 7872, load true · TodoMVC 200, «TodoMVC: React», полей 2 · две страницы за 23 с. Доказывает: архитектор получает
извлечённое браузером по каждой ссылке. Без правки: `404` двери / нет раздела.
**2. Негативы и собранное.** Без секрета `401 unauthorized` (JSON, не 307) · `127.0.0.1:3300` → `url-forbidden (forbidden-address)` ·
11 адресов → `400 too-many-urls` · строк 5 / векторов 38 до = после · в чанках `link-test` 1 файл, ложная строка 0 · порт 3700 держит pid
pm2. Повторён после 195-7 — тот же `✓ OK`.

## Ошибки
- Первая редакция звала порт браузера напрямую: договор тот же, но в обход «одной двери». Владелец поймал вопросом — исправлено 195-7.
- 🛑 Не проверено живьём: отказ `browser-unreachable` (нужна остановка браузера — запрещено).

## Эволюция навыков
Читались `use-development-steps` (маршрутизация, закрытие подшага). Перенос стенда соседней службы занял минуты вместо написания —
приём «сначала спроси, нет ли такого экрана у соседа» уже записан законами переноса; улучшать навык нечем.
