# 195-5 — навык `use-links`: чем агент располагает, когда речь о ссылке

**Коммит:** `5013f6a` (ветка `new-step-195`). **Дата:** 2026-09-14. **Решение владельца:** «195-13 и навык, потом закрыть» — из прежнего
состава 195-5 построен только навык; главная и страница API сделаны в 195-13.

## Что делали и как
- `.claude/skills/use-links/SKILL.md` — по-английски (машинный слой памяти одноязычен), у стенда ссылок вкладка «Навык» его показывает: имя в
  `stand-skills.ts`, файл читается с диска на каждый запрос, второй копии текста нет.
- Внутри: **сохранённая ссылка есть объект, у которого источник — адрес**; таблица «что в снимке есть и чего в нём нет»; главы как ответ на «на
  какой минуте»; **измеренная невозможность расшифровки** (`captions.download` → `401`); какие руки у агента есть и **что руки для ссылок у него
  нет вовсе** — ссылку кладёт человек на стенде, и договор 2.5.1 это говорит; как читать сохранённую ссылку (`find_objects` → `open_object` →
  главы внутри снимка, `ask_graph` по происхождению); **почему имя с ошибкой не находится** — таблица чисел из замера; восемь отказов с тем, что
  делать по каждому; канонический адрес ролика и сниппет как связанный объект.
- ✗ Найдено до прогона чтением `page.tsx`: общая ветка стенда ссылок перехватывала и вкладку «Навык» — навык не показался бы. Порядок ветвей
  исправлен, лишняя заглушка удалена.

## Доказательства
**1. Каждое утверждение навыка сверено со строкой кода (закон 194-14).** Десять имён рук — `what_i_already_know`, `write_value`,
`make_new_kind`, `promote_to_list`, `answer`, `ask_graph`, `search_vectors`, `find_objects`, `open_object`, `keep_object` — найдены в
`scripts/agent/memory-tools.mjs` по одному разу каждое; руки для ссылок в нём нет; в договоре `keep_links` нет (сторож: «договор годен: версия
2.5.1, методов 8»). Числа поиска и чисел глав — из приборов 195-8 и 195-4.
**2. Файл на сервере — тот же, что в git.** `sha256` начала: `3383e6c485040b3d` и локально, и на `/opt/fractera/memory` (7191 байт); сборка
знает `use-links` (3 файла); прежняя заглушка `data-link-soon` в сборке — **0** (была у вкладок ссылок).

## Ошибки
- Порядок ветвей экрана (выше) — поймано чтением, не прогоном.
- 🛑 Глаза владельца на саму вкладку ещё не получены на момент записи итога: вкладка за входом архитектора, прибору недостижима.

## Эволюция навыков
Написан новый навык. Урок для `use-testing`: **навык проверяется не чтением текста, а сверкой каждого его утверждения с кодом** — здесь это
поймало бы любое «у агента есть рука для ссылок», которого нет.

## Дополнение того же дня — навык переписан по правилам письма Skill Creator (`48b775e`)
Слово владельца, дословно: «Я напомню тебе что когда ты пишешь навык ты используешь ядро Skill Creator однако он предполагает сплит
тестирование различных навыков это тебе запрещено ты просто используешь его правила и принцип написания без тестирования».
Применены только принципы письма, **ни одного прогона, сплит-теста, `run_loop` или подагента**: описание стало напористым и несёт все «когда
звать» (URL в сообщении, YouTube, «на какой минуте», «тот сайт, что я сохранял» — даже без слова «ссылка»); «никогда/🛑» заменены причинами;
правило сформулировано общим, с одним измеренным примером. Факты сверены с кодом заново: десять имён рук по одному разу каждое, договор 2.5.1,
руки для ссылок 0, все восемь кодов отказов найдены, заголовок глав в снимке есть. Файл на сервере = git: `sha256` `77fd10d6d49a7714`, 95 строк
(прежний `3383e6c4…` — первая редакция).
