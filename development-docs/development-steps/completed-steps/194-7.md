# 194-7 — прибор на четырёх родах живых файлов, состав базы до = после, закрытие шага

**Коммит:** `f18063d` (прибор `scripts/probe/object-describe.mjs`; на сервер положен отдельно, `sha256` `4950fb34…` = git) —
`fractera-memory-starter`, ветка `new-step-194` · **дата:** 2026-09-13.

## Что делали и как
По ТЗ: картинка, PDF, аудио и короткое видео, метка `who = probe-194`, уборка со сверкой состава базы. Всё — через
публичный договор `https://memory.aifa.dev/v1/keep_object` с ключом памяти: картинка `404.jpg`, PDF
`docs/ai-company-brain-en.pdf` и видео `blog-media/boris-chernoy-post-1.mp4` названы адресом витрины
`www.fractera.ai` (память скачивает сама); звук вырезан `ffmpeg` из того же видео и ушёл формой. Видео живьём до этого
прибора не проверялось ни разу.

## Доказательства
**A — вывод прибора** (на сервере, `PROBE_RC=0`):
```
✓ image: 200 за 46960 мс, kind image, described true, messageId 34 · полное описание 3081 знаков
✓ pdf:   200 за 285789 мс, kind pdf, described true, messageId 35 · 2898 знаков
✓ video: 200 за 50150 мс, kind video, described true, messageId 36 · 3165 знаков · метки времени 17
✓ audio: 200 за 27736 мс, kind audio, described true, messageId 37 · 2182 знаков · метки времени 17
✓ у каждого: строка saved, три ссылки, source api, автор записан
✓ граф: документов прибора 4 из 4, все processed за 10 с
✓ OK
```
Доказывает: четыре рода, прочитанные разными путями (Claude, whisper-1, ffmpeg + кадры), проходят весь договор снаружи
до четырёх хранилищ с происхождением.

**B — негатив: состав базы**
```
состав до:    {"graph":5,"media":43,"rows":5,"vectors":38}
состав после: {"graph":5,"media":43,"rows":5,"vectors":38}
✓ состав базы после = до (строки, файлы, карточки, документы графа)
```
Доказывает: прибор не оставил в живой памяти ни одной строки, файла, карточки или документа графа. Без уборки,
дождавшейся разбора каждого документа, граф дал бы 9 вместо 5 (урок 194-16).

## Ошибки и находки
- 🛑 **PDF описывался 286 с при пределе хода Claude 300 с** (`lib/think.mjs`, `timeoutMs: 300000` в `describe.mjs`).
  В 194-2 тот же род занял 68 с. Запаса почти нет: PDF длиннее или медленный час подписки дадут отказ по времени. → долг.
- 🛑 **На сервере лежит забытая временная папка описания** `/tmp/memory-describe-GepWk7` от 18:02 — прибор 194-2 требует
  пусто (`leftovers()`); какой-то прошлый сбой её не убрал. Не удалена: причина не найдена. → долг.
- Замер «прибор завис» во время прогона оказался ложным: строка PDF появилась через 22 с после замера; разрешил журнал
  доступа nginx, а не счёт строк.

## Эволюция навыков
Читались `use-development-steps` (закрытие шага), `manage-git-branches-per-step` (вливание). Правок нет.
