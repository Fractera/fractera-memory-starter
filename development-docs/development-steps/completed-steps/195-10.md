# 195-10 — вкладка «Оценка» стенда ссылок: общий корпус случаев с подписью хранилища

**Коммит (`fractera-memory-starter`, ветка `new-step-195`):** `8b22494`. **Дата:** 2026-09-14. **Принят владельцем:** «принято».

## Что делали и как
- Решение владельца на развилку: «Общий + подпись (Recommended)» — один корпус и одна сводка (закон 189-6), у каждого случая подпись
  хранилища на всех стендах.
- `bench-cases.client.tsx` — подпись `words.stores[store]` первой в строке случая (незнакомый ключ — как есть); `benchCases.stores` en/ru;
  «Оценка» стенда ссылок рисует `BenchCases`.

## Доказательства
**1. Прибор `scripts/probe/link-verdict.mjs` через публичный адрес — `✓ OK`, без модели.** Поиск ссылки → случай 116 `store: link` без вердикта;
вердикт `good` с причиной записан и читается.
**2. Негатив и собранное.** `maybe` → `400 bad-verdict`, вердикта нет · без секрета `401` · уборка по метке, корпус 46 = 46 · в сборке подписи
хранилищ 2 файла, `data-case-store` 3.

## Ошибки
- До прогона чтением найдено: дверь `bench-cases` берёт метку уборки из адреса (`?mark=`), а не из тела — прибор исправлен до запуска; иначе
  проверочный случай остался бы в корпусе владельца.

## Эволюция навыков
`use-development-steps` читался. Нечего улучшать — строка написана.
