# Шаг 200 — стенд «Тест памяти» через публичный API: два глагола, вложения, типизированный ответ, матрица — ЗАКРЫТ 2026-09-14

**Репозиторий:** `fractera-memory-starter`, ветка `new-step-200` → влита в `main`, переименована в `old-step-200`. Отчёт фичи —
`development-docs/reports/feature-memory-bench-two-verbs-attachments-typed-output-over-public-api.md`.
**План удалён тем же коммитом, что этот итог** — восстановление: `git log --diff-filter=D --format=%H -1 -- development-docs/development-steps/new-steps/200/200-main.md`
и `git show <хэш>^:<путь>`. Первая (отменённая) редакция плана — `git show 9249485:development-docs/development-steps/new-steps/200/200-main.md`.

## Слова владельца, определившие шаг (дословно — в `current-steps.md` и итогах подшагов)
- «it most important page , which for test must have API request , but not direct call as another paged… /plan»
- «На вкладке тест памяти мы же не тестируем какие-либо другие методы кроме двух: сказать и спросить?… всё остальное это включение в эти два типа»
  → «Запускай процесс который удалит весь твой план и повтори весь план заново».
- «Сырой вызов» — «Убрать»; вложения — «кнопку загрузить аудио кнопку загрузить видео… обычная ссылка YouTube ссылка сделай валидацию»; ответ —
  «Только объект который включает в себя текст и идентификаторы объектов»; диалог — «добавь в 200-7 и go»; отчёт — «чем именно ты доставал… правильно
  неправильно абсолютно неправильно»; «go to 207 finish , create report»; английский прогон — «запусти эту часть Тестов на английском языке».
- Законы, рождённые шагом: четвёртый (инструмент одного архитектора), пятый (отчёт: чем достали, три ступени; обещание «без ИИ» 🪦 аннулировано),
  шестой (память полноценна для любого потребителя) — `LAWS.md`.

## Подшаги

| Подшаг | Что | Итог |
|---|---|---|
| 200-1 | дверь стенда — проводник в публичный `/v1/*`, ключ на сервере, только адресу этой машины; `GET` таблиц (было 14 × 405) | `200-1.md` |
| 200-2 | вход «Метод договора» по всем методам — **построен по ложной модели и откачен в 200-3**; таблицы по настоящей форме договора остались | `200-2.md` |
| 200-3 | стенд — два глагола; «Метод договора» и «Сырой вызов» убраны; список «от чьего имени» читается в тёмной теме | `200-3.md` |
| 200-4 | каждый расширенный параметр получает исход по договору (три значения исчезали молча); мёртвый ряд «Загрузка» снят | `200-4.md` |
| 200-5 | «Сказать» принимает вложения: файлы шести родов, ссылка, YouTube с проверкой рода; договор 2.6.0 | `200-5.md` |
| 200-6 | ответ двух глаголов типизирован (`text` + `objects`), договор 2.7.0; «Спросить» ищет объекты; сторож требует схему | `200-6.md` |
| 200-7 | матрица 28 строк + диалог + английский прогон; ручная оценка 19/9/0 и 6/3/1; оценка всей памяти; ответ о цикле навыков | `200-7.md` |
| 200-8 | закрытие: паспорт §13, описание API, обе инструкции, итоги | `200-8.md` |
| 200-9 | фильтр объектов (о ком, род, found_by) — **не строился, влит в 201-6** | — |

## Что умеет сегодня и чем доказано
Стенд проверяет «Сказать» и «Спросить» через публичный API (журнал nginx парами), каждый параметр доезжает или назван (прибор 54/54), вложения
всех родов ложатся (прибор `remember-attachments`, экран владельца), ответ по схеме (прибор `verbs-output` 16/16). Запись 14/14 в матрице.

## Чего не умеет — долги
Чтение фактов на русском (имена родов английские) · ложные попадания по служебным словам на английском · нет «не знаю» · объекты без фильтра о
ком · `depth` на фактах не действует, `BUILT_LEVELS = 2` лжёт · уборка оставляет строки сообщений и документы графа · род ролика youtube/web.
**Всё это — вход шага 201 «Схема в центре».**

## Ошибки шага
- ✗ 200-2 построен по ложной модели («все методы на стенде»), и вопрос владельцу был задан из ложной посылки — план удалён и составлен заново.
- ✗ В 200-6 поиск объектов встроен в «Спросить» сразу встраиванием и без фильтра о ком — найдено на экране владельца (чужой паспорт).
- ✗ Автоматическая оценка матрицы была мягкой — переоценено вручную.
- ✗ Ввод в поля React скриптом не доходит до состояния; инструмент `find` расширения работает от учётной записи расширения.
- ✗ Квота подписки сервера исчерпалась посреди 200-5.

## Эволюция навыков
Цикл правки навыков не нужен (отчёт §5): дефекты в коде ящика. Уроки — законами в `CLAUDE.md` памяти: судья мерит точность; вопрос из ложной
посылки даёт недействительный ответ.
