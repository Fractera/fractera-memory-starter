# 194-1 — таблица сообщений памяти и два указателя

**Репозиторий:** `fractera-memory-starter`, ветка `new-step-194`. **Коммит:** `4c7273e` (форма и прибор);
счёт строк в приборе — коммитом закрытия подшага. **Доставлено:** 2026-09-13, `BUILD_OK`.

## Что построено
`lib/messages.mjs`: `messages_that_came_into_memory` (32 колонки), указатели `…__links`
(`message_id · previous_message_id`) и `…__registry` (`message_id · fact_key · table_name · row_id` — форма
`automation_facts` службы 3600), семь индексов, лестница `LATE_COLUMNS` (пуста), `ensureMessages()`.
`CHECK` на `direction` (remember · recall · NULL), `kind` (text · audio · image · video · pdf · document),
`status` (described · saved · failed), `scope_source` (said · exif · device · guess).

🔒 **Поправка владельца посреди подшага, дословно:** «Похоже в таблице мы забыли добавить поля связанные
с календарем и гео метками». Добавлено до создания таблицы, без `ALTER`: `scope_radius_m`, `scope_place`,
`scope_source`, `event_starts_at`, `event_ends_at`, `event_time_zone`, `event_all_day`, `event_repeats`,
`remind_at` (к уже бывшим `scope_at`, `scope_lat`, `scope_lon`). Имена `scope_*` — из договора памяти.

## Доказательства
**A — прибор `scripts/probe/messages-table.mjs` на сервере, дословно:**
```
до: таблиц нет
✓ ensureMessages() первый вызов: {"ok":true}
после: messages_that_came_into_memory, …__links, …__registry
✓ повторный вызов ничего не меняет
✓ негатив: direction='delete' отвергнут
✓ негатив: kind='spreadsheet' отвергнут
✓ уборка по метке: строк probe-194 осталось 0
✓ OK
```
Таблицы создал код: до прогона их не было, прибор своего `CREATE` не содержит (закон 144).

**B — отдельная проверка счётом, мимо прибора:** до вставки 0 → законная вставка 1 → `direction=delete`
по-прежнему 1 → `kind=spreadsheet` по-прежнему 1 → после уборки 0.

## ✗ Что нашлось
**Негативный контроль прибора был слеп.** Законная вставка отвечает `{ok:true, rows:[]}`, оба отказа —
одинаковым `data-http-500`. По одним ответам сломанная запись неотличима от работающего `CHECK`. Проверка
счётом перенесена в сам прибор.

## Долг
Слой данных не отдаёт текст отказа SQL (`data-http-500` без причины) — «нарушен CHECK» и «таблицы нет»
снаружи выглядят одинаково. Чужая служба, в шаг не входит.
