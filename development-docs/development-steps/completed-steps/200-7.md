# 200-7 — проверка каждого органа у обоих глаголов через API, диалог «сказал → спросил → нашёл», отчёт — ЗАКРЫТ 2026-09-14

**Код прибора:** `fractera-memory-starter`, ветка `new-step-200`, коммит `220cb6d`, доставлен (`BUILD_RC=0`). Прогон на сервере 20:37–20:41 UTC.
Открыт словом владельца «go to 207 finish , create report , После того как создашь отчёт проанализируй и дай оценку всей работе памяти нужно ли
запускать новый цикл изменение навыков чтобы гарантировать более высокие показатели?»; §6 (диалог) добавлен словом «добавь в 200-7 и go»;
оценка по трём ступеням и «чем достали» — словом «…чем именно ты доставал и давать этому оценку правильно неправильно абсолютно неправильно».

## Что делали и как
- Прибор `scripts/probe/bench-matrix-200-7.mjs`: публичный адрес и ключ памяти, отдельный человек `probe-200-7`; 14 строк «Сказать», 8 строк
  «Спросить», пять диалогов (факт, опровержение, документ, ролик, «не знаю»), проверка чужого объекта; у каждой строки чем достали, оценка,
  проверка схемы `output`. Цена названа до прогона и подтвердилась: ~14 ходов модели, 1 единица YouTube, 1 страница браузером.
- Путь «экран → дверь стенда → `/v1`» не повторялся в матрице — доказан в 200-1…200-6; соответствие органа телу запроса — `bench-call` 54/54.
- **Оценка переделана вручную по ответам:** автоматическая дала 27/1/0 и засчитывала «факт есть среди всех отданных».
- Отчёт: `development-docs/reports/feature-memory-bench-two-verbs-attachments-typed-output-over-public-api.md` (матрица, находки, оценка памяти,
  ответ о цикле навыков). Журнал прибора — `development-docs/instruments/200-7-bench-matrix.log.txt`.

## Доказательства
**A (прибор, плоскость API).** 28 строк, все ответы по схеме договора; уборка 4 из 4. Ручная оценка: **правильно 19 · неправильно 9 ·
абсолютно неправильно 0**. Запись 14/14 правильно, включая опровержение в нити (Толедо → Севилья), три рода вложений и файл, три негатива формы
и рода. Диалог: документ (близость 0.520) и ролик (0.578) найдены первыми.
**B (негатив).** «Не знаю» (собака) — не выдумано ни одного факта; чужой объект — найден и оценён «неправильно» (паспорт `who: stand`); отказы
без `text` / без `who` — `400 missing-params`; `depth: ultra` — `bad_form`.

## 🛑 Главная находка
Механический поиск фактов сравнивает слова вопроса с английскими именами родов — **русский вопрос не совпадает никогда** и получает «вот всё,
что известно». 7 из 9 «неправильно» отсюда. Подробно — отчёт, раздел 3.

## Ошибки
- ✗ Автоматическая оценка прибора была слишком мягкой («факт есть среди всех») — показатель 27/1/0 не выдан за результат, переоценено по ответам.
- ✗ Род ролика в прибор записан как `youtube` при записи и пришёл `web` при чтении — прибор это не ловил; названо в отчёте.

## Эволюция навыков
Вывод отчёта: цикл правки навыков сейчас не нужен — дефекты в коде ящика, матрица шла мимо агента. `use-testing`: судья прибора обязан
проверять **точность** («в ответе ровно нужное»), а не **присутствие** («нужное где-то среди»); иначе прибор зелёный на ответе «вот всё».

## Дополнено тем же днём — поиск фактов по-английски (слово владельца)
Прибор `scripts/probe/recall-english-200-7.mjs` (коммит `f8324fb`): **правильно 6 · неправильно 3 · абсолютно неправильно 1**. Точны вопросы, где
слово совпало с именем рода (live/where/city/job/color/name). Дефекты устройства: служебное слово `with` дало уверенный чужой факт на вопрос о языке
(E8); `meet` ≠ `meetings` — нет словоформ (E7); «не знаю» по-прежнему нет (E9, E10). Подробно — отчёт, раздел 6.
