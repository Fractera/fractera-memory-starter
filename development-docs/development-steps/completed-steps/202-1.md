# 202-1 — раскладка мастерской и главное описание

**Коммиты:** `fractera-memory-starter`, ветка `new-step-202` — `2c63a51` (код), `824a9cd` (замок секрету машины), `8b5894f` (прибор страницы).
**Доставлено:** `DELIVERED_COMMIT=8b5894f`. **Дата:** 2026-09-15.

## Что делали и как
`app/[lang]/build/page.tsx` переписана на `WorkspaceShell` — ту же раскладку, что у `settings`. Порядок разделов — единственный источник
`_lib/build-sections.ts`. В шапке — цитата владельца о стартовом шаблоне и свёрнутая карточка «Как поделиться своим решением» (форк → ветка →
небольшие коммиты → доказательство → pull request → issue; лицензия MIT) со ссылками на репозиторий и новый `CONTRIBUTING.md`. Красная врезка о
риске переехала в раздел терминала. Страница под `<Suspense>`, замок — роль архитектора; с 202-7 и секрет машины (см. ниже).

## Доказательства
- **A** (`scripts/probe/build-page-202.mjs` на сервере): `/ru/build → 200, раскладка с левым меню: 1` · `пунктов верхнего уровня: 6 — Терминал
  Claude Code · Добавить задание · Текущие шаги · Шаги разработки · Навыки разработки · Главная инструкция` · `цитата в главном описании: 1` ·
  `карточка «как поделиться» со ссылкой на CONTRIBUTING: 1`; то же на `/en/build`. Без правки страница была одной колонкой без меню.
- **B**: `неизвестный раздел падает на терминал` · `без входа — 307 на …/login, мастерская не отдана` · `неверный секрет — 307`; живой браузер
  без входа тоже открыл страницу входа.

## Ошибки
- ✗ Прибор первого прогона считал отметки дважды (Next кладёт свойства и в данные гидратации) и резал меню до первого `</nav>`, находя 4 пункта
  из 6 — часть пунктов приходит в поток отдельным куском. Исправлено счётом атрибутов HTML по всему документу.
- Решение агента, названное вслух: страницу мастерской открыл **секрету машины** по двум точным адресам (`proxy.ts` + повтор проверки в
  странице). Иначе раздел нельзя было проверить ничем, кроме глаз владельца, а владелец велел не звать его. Прочитавший секрет и так владеет машиной.
- 🛑 **Глазами не проверено:** вид страницы в браузере — у браузера нет входа, пароль вводить запрещено.

## Эволюция навыков
`use-testing` сработал по назначению: оба слепых места прибора поймали его же негативы. Предложение: в `ANTI-PATTERNS.md` федерального слоя —
«счёт по HTML страницы Next с `cacheComponents`: свойства дублируются в данных гидратации, а часть разметки приходит отдельным куском потока».
