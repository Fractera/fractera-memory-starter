# 194-8 — инструмент просмотра объекта `_tools/object-view`

**Коммиты:** `2d20515` (перенос) · `79364bb` (починка сборки) · учёт `8696382` — `fractera-memory-starter`, ветка
`new-step-194` · **дата:** 2026-09-13 · принят глазами владельца: «vedeo , image , pdf, md was tested resoutl greate».

## Откуда
Владелец: «Инструменты тебе не нужно строить их обязательно нужно скопировать и вставить жёстко перенести и адаптировать».

## Что делали и как
Просмотр объекта перенесён из медиатеки панели (`ai-workspace/bridges/app/app/[lang]/media/_components/preview-popup.client.tsx`)
в `_tools/object-view` и адаптирован: фото, аудио, видео, PDF, Markdown, HTML в песочнице без `allow-same-origin`, текст.

## Доказательства
- **A — прибор `object-preview-kinds.mjs` `✓ OK`:** PDF `application/pdf` 96966 байт, Markdown `text/markdown` 13351,
  HTML `text/html` 202 — байт в байт, `inline`.
- **B — негатив:** клиентских файлов с `allow-scripts` — 1, с `allow-same-origin` — 0 (скрипты страницы не дотягиваются
  до памяти); локальный `tsc` 0 до доставки, `BUILD_RC=0`, `BUILD_ID` на месте.

## Ошибки
- Первая сборка упала: `the name previewOf is defined multiple times` (`objects.ts:165`). **Упавшая сборка стёрла
  `.next/BUILD_ID`** — служба жила прежней сборкой в памяти, перезапуск её уронил бы; прибор после провала мерил старую
  сборку, его 12 ✗ недействительны. Лечение: имя `previewItemOf`, `tsc` с кодом выхода до доставки.

## Эволюция навыков
Урок «перед доставкой `.ts` — `tsc`» записан в учёт как правило сессии. Итог записан при закрытии шага 194.
