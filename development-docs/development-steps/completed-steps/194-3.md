# 194-3 — экран описания

**Репозиторий:** `fractera-memory-starter`, ветка `new-step-194`. **Коммит:** `4a12591`. **Доставлено:** 2026-09-13.

## Что построено
В `object-bench.client.tsx`: кнопка «Получить описание» справа от «Выбрать файл», появляется только
при выбранном файле; счётчик секунд на время хода модели; поля «Полное описание» (новое) и «Саммари»
(прежнее `about`), оба правятся; смена файла сбрасывает прежнее описание; с сохранением едут
`full · title · tags · anchors · described_by · describe_ms · language` для 194-4; отказы словами на
каждый код (`think-quota-exhausted`, `model-key-missing`, `describe-kind-unsupported`…). Слова `en`/`ru`.
✗ Попутно исправлен `costNote`: прежний говорил «файл не читает ни одна модель» — с кнопкой описания
это стало неправдой.

## Доказательства
- **A — сборка на сервере:** слово «Получить описание» в 2 файлах `.next`, ложное «Получить описаниище» —
  в 0; поле `obj-full` в клиентском чанке — 1.
- **B — глаза владельца**, дословно: «looking greate , got to next step».
