# 203-2 — навык `memory-development` (EN): дисциплина разработки из FNS без сайтовой части

**Коммит:** `fractera-memory-starter`, ветка `new-step-203` — `c7fc4ac`. **Доставлено:** `dbdaba8`. **Дата:** 2026-09-15.

## Что делали и как

`.claude/skills/memory-development/SKILL.md`, 11 766 байт, английский (форма владельца: «both in english»; «Своя адаптированная копия
(Recommended)»). Образец письма — skill-creator: `name` и настойчивое `description` о том, что и когда; тело объясняет «почему»;
прогоны, бенчмарки и сплит-тесты не запускались (запрет владельца).

| Раздел FNS / навыка | Судьба |
|---|---|
| How you answer me — три движения | перенесён |
| Your memory — адреса учёта, приёмная, репорт фичи | перенесён под адреса памяти (без `GLOSSARY`, `ANTI-PATTERNS` — в дереве памяти их нет) |
| переживание обрыва, частота по событиям, без `/compact` | перенесён |
| планирование: 2–10 подшагов, пять разделов ТЗ, согласование формой | перенесён |
| маршрутизация задачи посреди шага A/B/C | перенесён; поиск — **самим агентом**, без подагентов |
| закрытие подшага и шага, судьба плана, git-цикл веток | перенесён |
| Testing · Answer format · When it does not work | перенесён, «лгавшие измерения» — из навыка тестирования памяти |
| You always evolve · skill-creator | перенесён с запретом прогонов |
| Development modes, migration, cases, product, architect layer, конфиги, виджеты, статика, motion, языки сайта, AI SDK | **отброшен** — это сайт порта 3000 |
| **своё памяти** | договор и semver · один вход `server.mjs` · два ключа · своё дерево · уборка приборов по метке · квота общая с ботом · правило в подсказке ломается · доставка гасит терминал |

Удалены русские `use-development-steps` и `use-testing` памяти (надгробие в `CANCELLED.md`). Законы — адресом `LAWS.md`, без копий.

## Доказательства

**A — текст навыка против диска, прибор `instruction-203-1.mjs` (дословно):**
```
OK   memory-development: сайтовых терминов 0
OK   адреса учёта в навыке: 6, отсутствуют 0
OK   memory-development 11766 байт (предел 12288)
OK   НЕГАТИВ B (CLAUDE.md стартера сайта): сайтовых терминов найдено 8 — [["APP-CONFIG",5],["DESIGN-CONFIG",7],["PLATFORM-CONFIG",7],["PRODUCTS-CONFIG",6],["widget",48],["generateStaticParams",1],["SEO",3],["motion",12]]
```
Доказывает: сайтовая часть не перенесена, а образец поиска её видит (на источнике — 8 терминов). Без правки навыка не было.

**B — папка навыков на сервере после доставки (`rcmd-203-clean.sh`, дословно):**
```
gone .claude/skills/use-development-steps
gone .claude/skills/use-testing
skills now: describe-incoming-object memory-development use-depth-ladder use-knowledge-graph use-links use-object-store use-tables use-vector-store
```
🛑 **Замена названного заранее доказательства.** План называл отданную страницу `/ru/build` (раздел навыков); снят список папки на
сервере. Страница строит раздел из этой папки с диска (`lib/build-docs.mjs:134`), поэтому замена близка, но **не доказывает отрисовку**.
Живое доказательство поведения — 203-3: строитель на сервере первым действием открыл этот навык.

## Ошибки

- Число «шести адресов» у FNS не переносится буквально: `GLOSSARY.md` и `ANTI-PATTERNS.md` в дереве памяти нет — навык называет только
  существующие адреса, прибор это проверяет.

## Эволюция навыков

- Отдельного навыка тестирования у памяти больше нет: доказательство — раздел `Proof` внутри `memory-development`. Если раздел начнёт
  расти, выносить в `references/` навыка, а не вторым навыком (две копии расходятся).
