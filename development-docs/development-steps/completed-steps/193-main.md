# Шаг 193 — реляционное хранилище: навык трёх методов записи (закрыт 2026-09-13, без прогонов)

**Слово владельца на закрытие:** «закрывай шаг 193 без прогонов».

## Что получилось
- **Ответ на вопрос владельца измерен прибором:** три руки — три разных SQL: `write_value` = `UPDATE` +
  история · `make_new_kind` = `ALTER TABLE ADD COLUMN` ×3 · `promote_to_list` = `CREATE TABLE` + перенос со
  своим временем. Четвёртый путь без имени — дописать в существующую таблицу (193-1).
- **Два дефекта рук найдены и починены:** имя чинилось вместо отказа (`8346c9c`) · незнакомый род
  утверждения молча пустел (`921b461`).
- **Навык `use-tables`** заменил `grow-the-shape` (193-2).

## Чего нет — названо
- Навык **не доказан различием поведения**: итерация 1 дала 100% в обеих конфигурациях; итерация 2 не
  состоялась. Единственное измеренное различие — род утверждения (`said` 7/7 против `fact` 7/7).
- Оптимизация описания не сделана (193-4).

## Ошибки
- ✗ Прогоны подагентами сожгли две сессии владельца. Итог — полный запрет подагентов.
- ✗ Прогоны писали в живую память без уборки. Убрано, сверено со снимком.

## Эволюция навыков
`use-development-steps`/`use-testing` не менялись. Урок в закон, а не в навык: навык проверяется
дёшево — прибором и чтением уже снятых журналов; эта находка шага и была сделана так.
