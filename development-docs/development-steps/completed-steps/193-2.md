# 193-2 — навык `use-tables`

**Коммит:** `82ac087`. Навык `.claude/skills/use-tables/SKILL.md` поглотил `grow-the-shape` целиком: у
графа, вектора и объектов по одному навыку на хранилище, второй навык о том же хранилище означал бы два
правила об одном предмете. Внутри: четыре руки и физический след каждой · необратимый выбор
«исправление против добавления» · «будет ли расти» вместо счёта · род утверждения `said`/`guess` ·
имя приводят к стандарту, но не чинят · имя-фраза и сверка с существующим.
Строка в `MEMORY-AGENT.md` заменена; паспорт §19 — надгробие `grow-the-shape`, раздел «Три метода записи».

**Доказательства:** A — `git rm` старого навыка, ссылок на `grow-the-shape` вне учёта ноль. B — прогон 193-3
(ниже): с навыком 7 из 7 записей пришли с `said`.
