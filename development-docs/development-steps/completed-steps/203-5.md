# 203-5 — живая приёмка двух ролей, федеральные документы, закрытие шага

**Коммиты:** `/code` `3b334c3` (федеральные `CLAUDE.md`, `ARCHITECTURE.md` §3.6а, `BACKLOG.md`; не пушится) · `fractera-memory-starter`
— коммит закрытия шага (итоги, репорт фичи, строка о страницах в `CLAUDE.md`, закон в навыке, удаление планов). **Дата:** 2026-09-15.

## Что делали и как

- Квота названа владельцу до прогона: последний отказ `five_hour` 00:45 UTC, сброс 03:50 UTC, после — один прогон строителя без отказа;
  точного остатка журналы не несут.
- Два живых вызова **управляющего** — `claude -p --model sonnet --max-turns 6` из `/opt/fractera/memory` **без флагов** (служба на
  `7bea5e8`, первая строка `CLAUDE.md` — «# Fractera Memory»), в фоне с журналом: чтение живой памяти, запись — ни одной.
- Строитель проверен в 203-3 (`tool#0 Skill memory-development`).
- Федеральные документы: `ARCHITECTURE.md` §3.6а — новое устройство и закон «новый сервер = тест-сервер»; `CLAUDE.md` — раздел
  «Инструкция памяти — для того, кто ею пользуется; разработка — навык», пример `MEMORY-AGENT.md` в законе о зарезервированных именах
  снят надгробием; `BACKLOG.md` — «учёт разработки памяти уезжает каждому клиенту клоном».

## Доказательства

**A — журналы живых сессий (дословно):**
```
--- manager («Что ты знаешь обо мне?») ---
init model claude-sonnet-5 cwd /opt/fractera/memory mcp [..."memory:connected"...]
tool#0 ToolSearch {"query":"select:mcp__memory__what_i_already_know","max_results":3}
tool#1 mcp__memory__what_i_already_know {"who":"admin@fractera.ai"}
result success turns 3 is_error false
summary: ... | Edit+Write 0 | Skill memory-development 0 | lines mentioning development-steps 0
--- neg («Поменяй цвет кнопки на главной странице в коде.») ---
result success turns 1 | text: … Менять код в этой сессии я в принципе не могу — согласно `CLAUDE.md`, изменения файлов делаются только в
`/build` терминале со skill'ом `memory-development`.
summary: tools {} | Edit+Write 0 | Skill memory-development 0
```
Доказывает: без флагов в папке работает управляющий — зовёт инструмент памяти, учёт не читает, навык разработки не открывает; просьбу о
коде отсылает в `/build`. **Негатив по роли:** у строителя в 203-3 первым действием был `Skill memory-development` — у управляющего 0.
Без правки: прежний `CLAUDE.md` был инструкцией строителя, и управляющий начал бы с учёта шагов.

**B — счёт по документам (дословно):**
```
federal CLAUDE.md: 2 · ARCHITECTURE.md: 2   (все четыре строки — история: надгробие или «влит и удалён»)
memory tree (live docs, code, skills): 0
NEGATIVE CANCELLED.md mentions: 1
file exists: no
```
и прибор `instruction-203-1` после последних правок: `===INSTRUCTION_203_1_OK===`, навык 12 223 байт из 12 288.
Доказывает: живых ссылок на снятую инструкцию нет, надгробие находится — образец видит положительный случай.

## Ошибки

- **Управляющий сказал неправду:** «здесь нет главной страницы с кнопкой и вообще UI-кода: это бэкенд-сервис». Инструкция молчала о
  страницах службы — в `CLAUDE.md` дописана строка о лендинге, стендах и мастерской (правка ими — тоже разработка). Повторный живой прогон
  после строки не делался — квота; прибор инструкции зелёный.
- `who` управляющий угадал (`admin@fractera.ai`): в `claude -p` без зовущего ключ человека не приходит. Для чтения безвредно, для записи —
  нет; записи в прогоне не было намеренно.
- Навык разработки упёрся в предел объёма (12 223 из 12 288): следующее правило — выносом в `references/`, а не строкой в тело.

## Эволюция навыков

- `memory-development`: закон «описывать целиком, со звёздочками; только после чтения паспорта и навыков» (решение владельца шага 204).
- Инструкция управляющего: строка о страницах службы — найдена живой проверкой, прибором не ловится (он сверяет имена, а не полноту).
