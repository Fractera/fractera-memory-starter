# 194-11 и 194-12 — главная memory.aifa.dev: новые роды и правда о том, кто их читает; оглавление

**Коммиты:** `db5fae0` (главная и оглавление) · `b5c6f45` (плавная прокрутка оглавления) · учёт `a2e94ed` —
`fractera-memory-starter`, ветка `new-step-194` · **дата:** 2026-09-13 · приняты глазами владельца. Один итог на два
подшага: они доставлены одним коммитом и измерены одним замером.

## Откуда
Владелец: «проверь что это присутствует в описании на главной странице memory.aifa.dev» (Markdown и HTML) и оглавление
`toc01` из каталога FNS.

## Что делали и как
Текст главной en/ru правлен в 7 местах: роды и правда — whisper-1 в OpenAI, Claude читает, OCR нет, код не запускается;
раздел форматов — 6 карточек в две колонки. Оглавление `landing-toc.tsx` (копия `toc`, `aria-label` из словаря), 18 пунктов.

## Доказательства
- **A — живая публичная страница:** `/ru` Markdown 9 · HTML 7 · «исходный код» 6; `/en` Markdown 9 · HTML 7 · «source code» 5;
  зеркало `/ru/index.md` обновилось само.
- **B — негатив:** «OCR» 0 и обещание локального Whisper 0 на обеих; оглавление — 18 из 18 пунктов и 18 из 18 разделов с
  теми же id, ложный якорь 0.

## Ошибки
Код начат раньше ТЗ — порядок нарушен; ТЗ `194-11.md`, `194-12.md` дописаны до доставки. Свойство страницы, не дефект:
главная отдаётся потоком, и половина пунктов оглавления приходит `<template>` и вставляется скриптом.

## Эволюция навыков
Итог записан при закрытии шага 194 по учётной записи `a2e94ed`.
