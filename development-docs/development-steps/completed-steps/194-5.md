# 194-5 — результат под формой

**Репозиторий:** `fractera-memory-starter`, ветка `new-step-194`. **Коммит:** `61b0969`. **Доставлено:** 2026-09-13, `BUILD_OK`.

## Что построено
- После «Сохранить объект» экран заново читает легшее дверью `GET /api/fractera/object-test?message=<n>`
  (`messageView()` в `objects.ts`) и показывает блок «Что легло в память»: сам файл (картинка · плеер
  аудио и видео · ссылка «Открыть файл»), полное описание из медиатеки, саммари и строку
  `messages_that_came_into_memory` — все колонки, горизонтальная прокрутка в своём контейнере.
- Дверь `GET /api/fractera/object-file?id=` (`fileOf()`): байты только объектов памяти, ключ склада
  остаётся на сервере; имя в `proxy.ts`.
- Слова `en`/`ru`.

## Доказательства
**A — прибор `scripts/probe/object-saved-view.mjs` на сервере, дословно:**
```
✓ сохранено: messageId 5
✓ чтение по номеру: 200
✓ колонки строки совпадают с формой таблицы (32 из 32)
✓ полное описание прочитано из медиатеки, а не из формы
✓ файл: 200 image/png
✓ байты совпадают с исходником (98757 = 98757)
✓ негатив, чужой файл tg-8-1788799325376.jpg: 404 {"error":"not-ours","ok":false}
✓ негатив, несуществующий номер: 404
✓ убрано своего: 1
✓ OK
```
**B — глаза владельца:** экран предъявлен, ответ дословно — «go to 194-6».
