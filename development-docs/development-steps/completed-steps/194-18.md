# 194-18 — документы говорят правду об объектах: договор, страница API, README, паспорт ящика

**Коммит:** `b191a8e` (`fractera-memory-starter`, ветка `new-step-194`) · **дата:** 2026-09-13 · доставлено на
`213.199.61.7`, `DELIVERED_COMMIT=b191a8e`. **Глаза владельца на страницу API — ждут.**

## Откуда
Сверка API v1: README §1 и `capabilities.json` обещали OCR и «media handles the pipeline itself»; `api.i18n.ts`
en/ru — «land in object storage»; `BLACKBOX-API.md` об объектах молчал и застрял на 175-6. Владелец: «Делай полный
вариант: методы объектов в API», затем «исправить документацию, которая сейчас обещает лишнее».

## Что делали и как
- `app/[lang]/settings/_i18n/api.i18n.ts` en/ru: методы `keep_object`, `find_objects`, `open_object`; параметры
  `file`, `url`, `name`, `source`, `author`, `title`, `summary`, `full`, `tags`, `anchors`, `question`, `id`, `from`;
  адрес `GET /v1/objects/{id}/file`; `media` и `remember.returns` — по коду; четыре новых примера.
- `api-doc.tsx`: у метода виден род тела (`multipart | json`); примеры `curl -F`, по адресу, поиск, скачивание файла.
- `README.md`: §1 — роды как в коде (whisper-1, Claude, шесть кадров, код не исполняется), четыре хранилища,
  страница не файл; разделы `keep_object`, `find_objects`/`open_object`; `media` `{url}`; сравнение и FAQ без OCR.
- `capabilities.json`: `multimodal_input` по коду, `object_api`, `attachments`, `four_stores_per_object`.
- `development-docs/BLACKBOX-API.md`: раздел «Объекты»; надгробия «только петля» и «двух глаголов хватает»;
  «чего не умеет» — страницы по ссылке и удаление через договор; приборы.
- Главная: примеры `media` без `kind`.

## Доказательства
**A — поверхности на сервере** (сборка страницы API, живой HTML главной):
```
build[find_objects]=13 · build[open_object]=3 · build[keep_object]=17 · build[GET /v1/objects/{id}/file]=3
build[Keep a whole thing]=2 · build[Положить вещь целиком]=1 · build[Скачать файл объекта]=1 · build[multipart]=7
build_negative[ЗАВЕДОМО_ЛОЖНАЯ_194_18]=0
landing_ru http=200 · kind_audio=0 · media_url_sample=2 · «Исходный код»=5 · negative=0
landing_en http=200 · kind_audio=0 · media_url_sample=2 · «Source code»=5 · negative=0
```
Доказывает: страница API собрана с методами объектов на обоих языках, главная отдаёт примеры без `kind`.
Страница API за входом — глазами не проверена, предъявляется владельцу.

**B — прибор переводов и негатив по репозиторию:**
```
node scripts/probe/api-i18n.mjs → I18N_RC=0, 40 ✓ (в их числе «НЕГАТИВНЫЙ: выдуманного ключа в словаре нет»)
OCR = 0 · parsed directly = 0 · handles the pipeline itself = 0 · land in object storage = 0
ложатся в объектное хранилище = 0 · url|id = 0 · "kind": "audio" = 0 · "kind": "pdf" = 0
приняты и записаны в объектное хранилище = 0 · tsc 0 · договор 2.5.0 годен · capabilities.json — JSON
```
Доказывает: у каждого метода и параметра договора есть слова в каждом языке, и ни одного прежнего ложного обещания
в пяти документах и коде не осталось. Без правки: прибор переводов дал бы `✗` у трёх методов и тринадцати параметров.

## Ошибки
- ТЗ называло негатив «прибор падает, если убрать перевод параметра»; отдельно это не прогонялось — прибор читает
  словарь по фиксированному пути, а его встроенный негативный контроль (выдуманный ключ = 0) прошёл.

## Эволюция навыков
Читался `use-development-steps`. Правок навыков нет; навык агента о методах объектов — следующий подшаг 194-14.
