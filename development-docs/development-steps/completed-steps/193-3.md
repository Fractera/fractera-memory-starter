# 193-3 — проверка навыка на живых руках

**Коммиты:** `3781e14` · `b507284` · `921b461`.

**Итерация 1** — 4 случая × (с навыком / без), 8 прогонов подагентами, ~1,1 млн токенов. По признакам,
названным до прогона: **100% и с навыком, и без** — случаи проверяли то, что уже сказано в описаниях рук.

**Находка, сделанная чтением журналов БЕЗ модели:** без навыка все 7 записей пришли с `claim=fact`, с
навыком — 7 из 7 `said`. Хранилище молча превращало `fact` в «род не назван»: сказанное человеком теряло
свой род без единой ошибки. **Починено в руках** (`921b461`): незнакомое слово отвергается с объяснением.
Проба на сервере: `fact` → «Род утверждения «fact» не понят…», `said` → записано с `said`.

✗ **Прогоны не убрали за собой** в живой памяти: колонки, таблицы, строки `eval-193-*`. Убрано, сверка со
снимком «до»: колонок 42 = 42, таблиц 4 = 4. Уборка по метке — `server-clean-after-runs.sh`.

🛑 **Итерация 2 остановлена владельцем и не состоялась.** Её запуск сжёг вторую сессию подряд. Решение
владельца: подагенты запрещены полностью, skill-creator — только образец для написания навыка
(закон в `CLAUDE.md` `477d8b5`, deny `Agent`/`Task`). Случаи 5–8 остались в `evals.json` как описание, не прогон.
