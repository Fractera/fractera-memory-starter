# Состояние работы — служба памяти (порт 3700)

> **Читается ПЕРВЫМ в каждой сессии, раньше кода и раньше паспорта.**

## ▶ СЕЙЧАС — 2026-09-14 (запись федерального агента, `/code`)

**▶ ШАГ 195 В РАБОТЕ (2026-09-14, после обрыва сессии на пределе окна):** федеральный 196 закрыт (`/code` `1edfd65`).
Паспорт §20 «Ссылки — слой работы с данными» **утверждён владельцем** («Утверждаю (Recommended)»), решения по трём развилкам
дословно в `PASSPORT.md` §20.6 и `new-steps/195/195-main.md`; заведён `195-6.md` («сравнить»). Коммит `fd398f9` в `main`.
Ветка **`new-step-195`** от `fd398f9`. ▶ **Подшаг 195-1:** раздел «Тест ссылок» в левом меню — четыре стандартные вкладки
(построена «Загрузка», остальные честно «строится следующим подшагом»); экран и дверь **переносятся** со стенда «Тест чтения»
ИИ-браузера (`fractera-ai-browser-starter/app/[lang]/settings/_components/read-test.client.tsx`, `app/api/fractera/read-test/route.ts`);
в память ничего не пишется. План сессии — `~/.claude/plans/last-message-pure-wreath.md` (утверждён). 🛑 Уточнить до 195-6:
сохраняется ли новая версия при «сравни». Федеральная приёмная: давняя заявка `27-08-2026_19-42-05.md` не разобрана — названа
владельцу, к 195 не относится.
⏳ **195-1 доставлен и доказан, ЖДЁТ ГЛАЗ ВЛАДЕЛЬЦА** (`memory.aifa.dev/ru/settings?section=link-test`). Коммиты ветки: `d5e4c52` код ·
`74e002e` прибор; ветка в origin = `74e002e`. Доставка `deliver-memory.sh 3bb7b1d`: `BUILD_RC=0`, порт за 4 с, слушатель `1288224` =
pid pm2, `DELIVERED_COMMIT=74e002e`. `tsc` 0 до доставки. **Прибор `scripts/probe/link-test.mjs` через `https://memory.aifa.dev` `✓ OK`:**
MDN — 200, «HTML: HyperText Markup Language | MDN», текст 7872, HTML 103822, load true · TodoMVC — 200, «TodoMVC: React», текст 80,
полей 2 · дверь за 23 с (две страницы по очереди) · без секрета `401 unauthorized` · `127.0.0.1:3300` → `url-forbidden
(forbidden-address)` · 11 адресов → `400 too-many-urls` · строк 5 / векторов 38 до = после. Чанки: `link-test` 1 файл, ложная строка
0. 🛑 **Не проверено:** отказ `browser-unreachable` — нужна остановка ИИ-браузера или перезапуск памяти с адресом-приманкой (запрещено);
`tsx` в дереве нет. Итог `completed-steps/195-1.md` пишется после слова владельца; 195-2 не начинать до него.

**Истинное состояние, проверено 2026-09-14 чтением репозитория:**
- ✅ **Шаг 194 ЗАКРЫТ** — `51ae673` «Шаг 194 закрыт: итоги 194-7 и 194-main, репорт фичи…», влит в `main`, ветка `old-step-194`,
  итоги `completed-steps/194-1 … 194-19`, `194-main`. 🪦 Раздел «КОНЕЦ СЕССИИ 2026-09-13» ниже писался ДО закрытия и говорит
  «194-7 не сделан» — это история, а не состояние. ✗ Шапка не была обновлена в коммите закрытия.
- 🛑 **Шаг 189 открыт** — план в `new-steps/189/`, ждёт 189-7 (прогон владельцем на его данных).
- ⬜ **Шаг 195 «ссылки в памяти» — следующий, не начат, папки плана нет.** Утверждённый план — `~/.claude/plans/eager-knitting-panda.md`,
  раздел C: 195-1 род `web` и колонка `url` · 195-2 `keep_links` в договоре · 195-3 раздел «Тест ссылок» в левом меню и стенд ·
  195-4 YouTube до четырёх хранилищ · 195-5 главная, страница API, навык.
- 🔒 **Слово владельца 2026-09-14 (интерфейс выбора): «195, меню ссылок первым (Recommended)»** — раздел «Тест ссылок» в левом
  меню идёт первым. И тем же выбором: **«Чинить до 195 (Recommended)»** — сначала надёжность ИИ-браузера (федеральный подшаг
  196-8), на котором 195 стоит целиком. Слова владельца того дня: «Страницы и так на шаги памяти мы все ещё не создали страницу в
  левом меню которое созданы для работы над нашей задачей а именно работа с ссылками. Возможно мы потеряли фокус и решали задачу
  связанные только с браузером это хорошо ну давай вернёмся к памяти что у нас в плане».
- **Шаг 197 (федеральный):** общий подвал служб — `components/shell/services.ts` + `site-footer.tsx` байт в байт в памяти, чате и
  ИИ-браузере; в `main` памяти `3bb7b1d`. Правка этих двух файлов — одним и тем же файлом во все три службы.
- Состояние ИИ-браузера и шагов 196–198 — в федеральном `/code/development-docs/development-steps/current-steps.md`.
- 🔒 **Этап 9 по 195 пройден: «Подтверждаю (Recommended)»** (2026-09-14). Затем владелец, дословно: «На вкладке памяти в левом меню
  мы так и не сделали ничего связанное с тестом ссылок. Там должно быть такая же стандартная вкладка как и для агент раг связанные с
  ссылками. То есть ссылки это такой же равнозначной самостоятельно способ работы с памятью как и любой другой который мы описали то
  есть он должен внутри себя иметь способ найти и посмотреть и протестировать. То есть прямо из памяти положил вызываться API Аи
  браузера. Понимаешь о чем я говорю? Затем я должна это внести в описании проекта как ещё один слой работы с данными соответственно
  здесь появится навык который будет обращаться к API Аи браузера? Также это лежит в паспорт». ▶ **Порядок:** раздел паспорта
  «Ссылки — слой работы с данными» → утверждение владельцем → код раздела «Тест ссылок» сразу со всеми вкладками стенда → навык.

## ⏸ (ИСТОРИЯ) КОНЕЦ СЕССИИ 2026-09-13 ~18:55 CEST

**Последнее слово владельца, дословно:** «Зафиксируй итоговый результат все сохрани и проверь я заканчиваю эту
сессию сразу как только ты полностью закончишь работаю над этим заданием».

**Где остановились — шаг 194, ветка `new-step-194` (в `main` НЕ влита):**
- ✅ приняты владельцем: 194-1 … 194-6, 194-8, 194-10 (код: анализ, не исходник), 194-11 (текст главной), 194-12 (оглавление + плавная прокрутка), вводный текст стенда объектов; 194-9 (поиск: блок у каждого найденного) — доставлен `d033713`, глаз владельца на последнюю редакцию нет.
- ✅ **194-13 (КРИТИЧЕСКОЕ) доставлен и доказан:** `152b3d9` на сервере (`BUILD_ID` 18:53:34, health 200). Граф ВСЕГДА получает полное описание + происхождение (источник · автор · дата UTC), колонка `author`, автор из замка. Прибор `object-four-stores.mjs` `✓ OK`: из строки таблицы — файл с полным описанием · карточка вектора · документ графа `processed` за 16 с · граф находит объект, в контексте полное описание, «Откуда это известно: тестовый стенд памяти, прислал …» и дата `2026-09-13 17:03:29 UTC`; негативы (несуществующее слово, пустые якоря → откат) и уборка. Итог — `completed-steps/194-13.md`. Глаз владельца на это ещё нет.
- ✅ **Вводный текст стенда объектов свёрнут в аккордеон (2026-09-13, вечер):** слово владельца — «занимает слишком много места… разместить вверху сразу под описанием… скрыт как вкладка аккордеона которая закрыта». Блок перенесён из-над секции внутрь неё, сразу после строки описания вкладки; обёртка — `SettingsCard` (`<details>`, закрыт по умолчанию), заголовок `intro.cardTitle` en/ru, признак `data-object-intro`. `tsc` 0. Глаз владельца нет.
- 🔒 **Сверка API v1 с объектным хранилищем (2026-09-13, вечер, слово владельца: «детально проверил насколько первая версия API все ещё остается актуальна… фокус на… объектного хранилища и его расширенных типов»). Исход A — та же способность, до кода, решение за владельцем. Измерено чтением кода:**
  1. 🛑 `/v1/remember` поле `media`: `lib/params.mjs` — `works: true`, ответ `accepted` «вложения приняты и записаны в объектное хранилище»; `values.media` НЕ ЧИТАЕТ НИКТО (грep по lib, server.mjs, scripts/agent — только params.mjs). Вложение молча выбрасывается с отчётом об успехе. Живым вызовом не проверено: запись в живую память и квота.
  2. Роды договора `MEDIA_KINDS` = image·video·audio·pdf·html·text; у хранилища с 194-10 ещё markdown и code (JSON/YAML/XML — код). И противоположные правила: договор «род называет зовущий», `describe.mjs kindOf` угадывает по расширению.
  3. Форма `{kind, url|id}` не имеет исполнителя: адрес никто не скачивает, чей `id` — не определено; POST `/v1/*` принимает только JSON — файл через договор не прислать.
  4. В договоре нет методов объектов (положить · найти · открыть): они только двери стенда `/api/fractera/object-*` и руки агента `find_objects/open_object/keep_object` на 127.0.0.1. Внешний зовущий (API, Telegram) объект не положит и не найдёт. Нет полей происхождения `source`/`author` (закон 194-13). Прямо задевает 194-14.
  5. Документы врут: README §1 и `capabilities.json` обещают OCR и «media handles the pipeline itself»; `api.i18n.ts` en/ru повторяет «land in object storage»; `BLACKBOX-API.md` об объектах молчит, §3 застрял на 175-6.
- 🔒 **Слово владельца 2026-09-13, вечер, дословно: «Делай полный вариант: методы объектов в API».** Исход A. Заведены ТЗ `194-15` (keep_object файлом), `194-16` (по адресу + честное media), `194-17` (find/open/файл), `194-18` (документы). Порядок: 15 → 16 → 17 → 18 → 14 → 7. **Этап 9 пройден: «Подтверждаю»; media — «Через ingest (Recommended)».** ✅ **194-15 закрыт** `3df24ad`, доставлен: `POST /v1/keep_object` (multipart потоком → `object-ingest` → `ingest()` = describe + keep), договор 2.3.0; прибор `v1-objects.mjs` `✓ OK` — картинка через публичный адрес с ключом памяти за 53 с в четырёх хранилищах, source=api; негативы 401 / not-multipart / describe-kind-unsupported / ключ памяти во внутреннюю дверь 401; уборка 0. Итог `completed-steps/194-15.md`. ▶ **Следующий — 194-16** (по адресу + `media` через ingest).
- 🔒 **Слово владельца 2026-09-13, поздний вечер, дословно: «Да, делай вкладки навыков, потом продолжай API , исправить документацию, которая сейчас обещает лишнее. (свёрнутый текст: memory.aifa.dev/ru/settings?section=object-test. - success)».** Аккордеон вводного текста принят глазами. Вкладки «Навык» — исход A, подшаг **194-19**: граф → `use-knowledge-graph`, вектор → `use-vector-store`, тест памяти → `use-tables` + `use-depth-ladder`, объекты → + `use-object-store`. **Новый порядок хвоста: 194-19 → 194-16 → 194-17 → 194-18 → 194-14 → 194-7.** ✅ **194-19 построен и доставлен** `2914239`: вкладка «Навык» у графа, вектора, теста памяти («Тест» · «Навык») и объектов (два навыка), карта `_lib/stand-skills.ts`; сборка на сервере несёт все шесть имён, логика вкладок проверена `tsx`. Итог `completed-steps/194-19.md`. ✅ **Принят глазами:** «Да, вкладки работают, продолжай API». ▶ **194-16**.
- 🔒 **Слово владельца 2026-09-13, ночь, дословно — новая сущность «ссылки»:** «У нас есть ещё одна сущность которую мы ещё не касались и которые мы вообще забыли. Это ссылки. Рассказываю как они работают: – если приходит ссылка то мы поступаем с ней как с объектом если KAM из неё содержимое страницы или веб приложения. Используем для этого Cloud Brother расширение. И одновременно с этим есть вторую основное решение это парсер ссылок из YouTube. Насколько я понимаю для этого нужен ключ? Значит этот ключ должны добавлять в настройках. Я вижу это как элемент который требует: – API – описание в документы на главной странице memory.aifa.dev – на вкладке тест памяти новый раздел для добавления тестирования с ссылкой – в левой вкладке ниже чем тест объектного хранилища тест ссылки Хотя обрабатывать нужно уметь ни одну ссылку а несколько ссылок. То что касается извлечение содержимого анализа здесь принципиально важно извлечь именно весь доступный исходный код (текстовую составляющую, все что касается интерактивных элементов или медиафайлов проводится поверхностный анализ через атрибуты или заголовки) а не только Мета записи. – – – главный стенд теста памяти пока не трогаем, там много работы многие сделанные вещи до сих пор отмечены как сырые сейчас пока этого не касаемся». → исход C: **шаг 195 «ссылки»**, план согласуется до кода. Слова, искажённые распознаванием («KAM», «Cloud Brother»), — уточняются у владельца, не угадываются. Главный стенд теста памяти не трогать.
- 🔒 **Ответы владельца 2026-09-13, ночь, дословно.** На вопрос «Чем память будет открывать страницу по ссылке…»: «https://github.com/daijro/camoufox will use - Это большое полномасштабное решение которое получит под себя собственность субдомен: ai-browser.aifa.dev Мы будем его использовать разными сервисами и этот сервис будет вызывать его первым.. Также с ним будут работать агенты и проще механизм имеет в виду. Это затронет bootstrap а также настройку Domino в3002 административные панели вкладка личный домен.» На вопрос о YouTube: «also with ai browser https://github.com/daijro/camoufox». → **шаг 196 — служба ИИ-браузера (федеральная, свой субдомен, bootstrap, `SUBDOMAINS` панели)**; шаг 195 (ссылки в памяти) опирается на 196. Затем владелец: «go to /plan» — планирование в режиме плана. 🔒 **План утверждён владельцем 2026-09-13, ночь (режим плана, «approved»): `C:/Users/Usuario/.claude/plans/eager-knitting-panda.md`.** Порядок **A → B → C**: A — хвост 194 (194-16 · 17 · 18 · 14 · 7); B — **шаг 196** служба ИИ-браузера: репозиторий `Fractera/fractera-ai-browser-starter` ↔ `/opt/fractera/ai-browser`, порт 3800, движок `python -m camoufox server` + Node `/v1` (`read {urls[]}`, `youtube`), `SUBDOMAINS`/`PROXY_PORTS` панели, аддитивный блок `bootstrap.sh`; C — **шаг 195** страницы по ссылке: род `web` (не `link` — имя `__links` занято), колонка `url`, `keep_links`, раздел «Тест ссылок» после объектов, главная. Главный стенд теста памяти не трогать. ТЗ 196 и 195 кладутся в учёт в начале каждого шага. ✅ **194-16 закрыт** `aba9e75` (+ `e19cb34` уборка прибора): `keep_object` по `url` (`lib/fractera/fetch-url.ts`, защита в `lookup` соединения), `remember.media` через ingest с `objects`, договор 2.4.0; прибор `✓ OK` — по адресу байт в байт, пять запрещённых адресов 400, remember вернул судьбу обоих вложений, уборка 0. Первый прогон оставил 3 документа графа — опознаны и удалены, проверено 0. В журнале памяти остались 2 записи прибора (`forget_journal` стирает весь журнал — не зовётся). Итог `completed-steps/194-16.md`. ✅ **194-17 закрыт** `fbaa9b3`: `find_objects` (с `messageId`/`kind`/`summary` из таблицы, без записи случаев стенда), `open_object` (+ адрес файла), `GET /v1/objects/{id}/file` потоком; мост `lib/loopback.mjs`; договор 2.5.0, методов 8. Прибор `✓ OK`: README найден по смыслу, текст 12000 из 26931, файл sha256 совпал; без ключа 401, чужой id not-found/404, бессмыслица found:false. Итог `completed-steps/194-17.md`. ✅ **194-18 закрыт** `b191a8e`: страница API en/ru (три метода объектов, 13 параметров, род тела, 4 примера), README (§1 по коду, разделы объектов, FAQ и сравнение без OCR), `capabilities.json`, `BLACKBOX-API.md` (раздел «Объекты», надгробия), главная без `kind`. Прибор переводов 40 ✓, старых обещаний 0 по десяти образцам; сборка несёт методы на обоих языках, главная ru/en 200 без `kind`. Итог `completed-steps/194-18.md`. 🔒 **Страница API ждёт глаз владельца** (`memory.aifa.dev/ru/settings?section=api`). ✅ **194-14 закрыт** `96c752e`: навык `describe-incoming-object` переписан под настоящий API (двери, происхождение, граф всегда, отказы адресов, пределы), каждое утверждение сверено со строкой кода; урок уборки графа в `use-testing` («Да, дописать»); оба файла на сервере, sha256 = git. Итог `completed-steps/194-14.md`. ✅ **194-7 закрыт** `f18063d`: прибор `object-describe.mjs` `✓ OK` — картинка 47 с, PDF 286 с, видео 50 с (17 меток), звук 28 с (17 меток), граф 4/4, **состав базы до = после** (5·43·38·5). Долги: PDF 286 с при пределе 300 с; забытая `/tmp/memory-describe-GepWk7`. ✅ **ШАГ 194 ЗАКРЫТ 2026-09-13** — итог `completed-steps/194-main.md`, репорт `reports/feature-objects-enter-memory-four-stores-api-probes-and-skill.md`, план удалён тем же коммитом, ветка влита в `main` и названа `old-step-194`. ▶ **Следующий — шаг 196** (служба ИИ-браузера, федеральный слой): начать с ТЗ в `/code/development-docs/development-steps/new-steps/196/` и подшага 196-1 (замер до стройки). 🔒 **DNS для 196 готов (2026-09-13, ночь):** у `aifa.dev` нет общей записи `*` (случайное имя не разрешается), поэтому владелец добавил A `ai-browser` → `213.199.61.7`; измерено: через 1.1.1.1 и 8.8.8.8 отвечает `213.199.61.7`, негатив (случайное имя) — нет ответа. Нужна для сертификата в 196-6.
- 🔒 **Ответы владельца 2026-09-13, ночь, дословно.** Установка на новых серверах: «both always memory and ai-browser» — **память и ИИ-браузер ставятся всегда**. 🪦 **ЛОЖНОЕ УТВЕРЖДЕНИЕ АГЕНТА СНЯТО ТОЙ ЖЕ НОЧЬЮ:** здесь стояло «сегодня память в `bootstrap.sh` — компонент по выбору (`maybe_step`, csv «memory»)». **Измерено чтением установщика:** служба памяти ставится ВСЕГДА — `soft_step "memory_clone"` и `soft_step "memory_start"`; `maybe_step` объявлен и не вызывается нигде; `memory` в `COMPONENTS` — хвост прежнего поколения, пишется только в `installed-components.json` и ничего не включает. ✗ Вывод был сделан по двум строкам поиска без чтения блока и ввёл владельца в заблуждение. 🔒 **Закон владельца, дословно:** «Требования устанавливать везде все связи делаем окончательные бесшовные, мы будем добавлять множество дополнительных микро service таких как память чат браузер и так далее и все должны присутствовать всегда». В 196-7 ИИ-браузер встаёт тем же `soft_step`, без выбора. YouTube без субтитров: «Что досталось + причина». Три записи прибора в журнале памяти: «Оставить». Урок уборки в навык `use-testing` памяти: «Да, дописать» — внесено сразу. Затем: «or go to next ?» и о странице API: «i see but which q?» — предъявлено, что смотреть; ▶ **194-14**. Факты первоисточников: Camoufox — форк Firefox, Python-пакет `camoufox`, MPL-2.0, виртуальный дисплей для сервера, в README год перерыва в сопровождении; YouTube Data API ключом даёт только открытые данные, `captions.download` требует OAuth и права редактировать видео; Claude in Chrome требует видимое окно Chrome и `/login`. Вопрос о вкладках «Навык» у графа, вектора и теста памяти владелец не ответил — открыт.
- ⬜ **194-14:** переписать навык `describe-incoming-object` под API и Telegram после доказательства 194-13 (ТЗ `new-steps/194/194-14.md`).
- ⬜ **194-7:** закрытие шага — итоги 194-7…194-14, `194-main`, удалить план, влить в `main`, отправить.
- 🛑 **Урок сессии:** доставка перезапускает службу и рвёт запрос владельца посреди хода модели — предупреждать до доставки. Подагенты запрещены. Перед доставкой `.ts` — `tsc` (сборка на сервере стирает `BUILD_ID` при провале).

## ▶ СЕЙЧАС — шаг 194: объект входит в память (заведён 2026-09-13, план подтверждён)

План: `new-steps/194/194-main.md` + `194-1 … 194-7`, ветка `new-step-194` от `main` (`9383680`).
Описание файла моделью (аудио и видео — OpenAI + ffmpeg, остальное — Claude по подписке), таблица
**`messages_that_came_into_memory`** (имя выбрано владельцем) и два указателя, результат одной строкой
под формой, навык `describe-incoming-object` на английском во вкладке «Навык».
🔒 **Слово владельца на план — «Подтверждаю»; видео — «Дорожка + кадры сейчас».** Подагенты запрещены.

| Подшаг | Состояние |
|---|---|
| 194-1 таблица и указатели | ✅ `4c7273e`, таблицы созданы кодом на сервере, `CHECK` доказан счётом |
| 194-2 ✅ ЗАКРЫТ | итог `completed-steps/194-2.md`; прибор 16:11 Berlin `✓ OK`: картинка 37 с, PDF 68 с, аудио 24 с с метками `[00:00–00:08]`; видео живьём — в 194-7 |
| 194-2 (история) | построено: `lib/describe.mjs`, дверь `object-test/describe` (имя в `proxy.ts`), `think()` +`dirs`/`timeoutMs`, `model.mjs` +`transcribe()`; прибор `scripts/probe/object-describe-door.mjs` (3 хода Claude + 1 whisper). Коммит `46122db`, доставлено. **Прогон ~15:40 CEST:** дверь, привратник и замок отвечают JSON ✓ · негатив `.exe` 400 за 157 мс ✓ · временные папки стёрты ✓ · **картинка, PDF, аудио — `think-quota-exhausted`, лимит подписки на сервере, сброс 16:10 Europe/Berlin.** Положительный случай НЕ доказан; повторить прибор после сброса. **Половина OpenAI доказана отдельно:** голосовое `whisper-1` 29 слов за 2088 мс; негатив 4 байта → `model-unreachable` «Invalid file format» |
| 194-2+ метки времени | 🔒 решение владельца «да, добавь метки времени в расшифровку»: `transcribe({timestamps})` → `verbose_json` сегменты; аудио и видео идут Claude строками `[мм:сс–мм:сс]`, кадры видео встают между репликами; прибор проверяет метки в полном описании аудио |
| 194-3 ✅ ЗАКРЫТ | принят глазами владельца: «looking greate , got to next step»; итог `completed-steps/194-3.md` |
| 194-4 ✅ ЗАКРЫТ | `3a06e0a`, прибор `✓ OK`, сирот-карточек 0; итог `completed-steps/194-4.md`. 🛑 Долг: удаление документа графа через секунды после записи не выполняется (движок ещё разбирает) — откат `keep()` на ступени строки может оставить документ |
| 194-5 ✅ ЗАКРЫТ | слово владельца «go to 194-6»; итог `completed-steps/194-5.md` |
| 🔒 задача посреди шага 2026-09-13 | дословно: «go to 194-7 , and Нам на вкладке поиск нужно повторить тот же самый интерфейс который у нас находится в загрузке то есть я делаю поиск и получаю в этом поиске и полное описание, и Самаре и запись в таблице и сам объект. Посмотри у нас есть вкладка инструменты и там должны существовать инструменты для просмотра наших объектах аудио видео фото и PDF и HTML. Данные инструменты скорее всего находятся по 3000 имеет смысл их разместить в этом же репозитории.» → исход A (та же способность): новые подшаги 194, закрытие шага — последним. **Измерено:** в FNS `_tools` 8 инструментов, просмотрщиков нет. **Выбор владельца: «Строить свои просмотрщики».** Подшаги 194-8 (`_tools/object-view`) и 194-9 (поиск = блок загрузки); 194-7 — последним |
| 🔒 ещё два слова владельца | 1) «кроме PDF мы также можем работать с Markdown и HTML форматом проверь что наш навык и наш архитектура позволяет это делать в равной степени. Также проверь что это присутствует в описании на главной странице memory.aifa.dev» · 2) **«Инструменты тебе не нужно строить их обязательно нужно скопировать и вставить жёстко перенести и адаптировать»** — отменяет выбор «строить свои»; ищу источник на живом слоте и в панели |
| проверка Markdown/HTML (измерено) | описание и карточка читают `.md`/`.html` как текст ✓ · ✗ строка таблицы: PDF → `pdf`, а Markdown и HTML → безликий `document` · ✗ навык не называет Markdown и HTML · ✗ главная memory.aifa.dev: Markdown как вход не упомянут нигде, HTML только в одном ответе FAQ, в разделе форматов «PDF и документы» |
| источник просмотрщиков (измерено) | живой слот: те же 8 `_tools`, просмотрщиков нет · **панель `ai-workspace/bridges/app/app/[lang]/media/_components/preview-popup.client.tsx`** — просмотр медиатеки, кандидат на перенос · FNS `components/ai-elements/attachments.tsx`, `components/media/video-*` |
| 🔒 слово владельца после 194-8 | дословно: «vedeo , image , pdf, md was tested resoutl greate , but У меня не получилось загрузить файлы ts,tsx подозреваю что и другие файлы такого типа не поддерживаются? Разработать процедуру которая позволит в том числе загружать такие файлы генерировать описание генерировать сама ну в общем все то же самое и просмотрщик кода у нас тоже есть который позволяет просматривать элементы без необходимости конечно их запускать» → 194-8 принят глазами; код — исход A, новый подшаг 194 (просмотрщик — перенос `code-view` из FNS `_tools`). Вопросы о главной странице и родах markdown/html владелец не ответил — остаются открытыми |
| ▶ 194-13 построено | ТЗ `194-13.md`, `194-14.md`. `keep()`: граф ВСЕГДА — текст = название + полное описание + теги + id объекта + саммари; шапка «Откуда это известно: <источник>, прислал <автор>, <дата UTC>, файл «…»»; якоря от модели или название; пустые якоря — отказ и откат. Поздняя колонка `author`. Дверь: автор из замка (email сессии / «прибор»), `source` по умолчанию `stand`. Прибор `object-four-stores.mjs` (публичный адрес, ожидание `processed`, вопрос графу по уникальному слову, негативы, уборка после разбора) — запускается |
| 🔒 КРИТИЧЕСКОЕ СЛОВО ВЛАДЕЛЬЦА: граф и связи | дословно: «Ты меня немного смутил но я уже подозревал что в рамках работы фактуры ты забыл, что нужно полное описание отправлять в граф одновременно при этом усилия его дополнительной информации о том откуда поступило это объект в данном случае из тестового стенда в реальном же случае там должен быть написан реальный источник например Telegram от Рома Амстер, должна быть написано дата. Логичным образом должна быть создана запись в векторном хранилище, и разумеется созданной связи между всеми четырьмя базами что должна отображаться в таблице. Замечательным образом проверь эту часть работы архитектуры именно для этого мы создавали. И сразу после этого замечательным образом исследуем навык. Потому что именно этот навык будут делать эту работу. Когда она будет приходить не из стенда а из-за реального пространства это будет либо API либо Telegram. Это критически важно обеспечить пожалуй самый сложную часть работы архитектуры с объектами.» → **194-13**: граф ВСЕГДА, полное описание + происхождение (источник · кто · дата), сквозная проверка четырёх связей из строки таблицы; **194-14**: навык под API и Telegram |
| 🔒 слово владельца о вводном тексте стенда | дословно: «Тест объектного хранилища / Те же три шага для объектного хранилища… // need update : Совершенно не отражает смысл согласись? … данная вкладка позволяет оценить работу связки объект на хранилище а также всех других служб которые участвуют с хранении данных… каждый объект … прописывается в базе данных, векторном хранилище, и в граф rag … сохраняется в исходном виде в объект хранилище … находить объекты … без использования искусственного интеллекта по прямому совпадению так и с помощью глубокого поиска. Данная служба поддерживает следующий типы объектов: – изображение: извлекается… попадает в… хранится… – видео… итак по всем.» → `hint` переписан; новый блок `objectBench.intro` (3 абзаца + 9 родов) над вкладками. Уточнено против наброска: граф — только при описании моделью (нужны якоря); прямое совпадение по названию и тегам стенд пока не использует — сказано словами |
| 🔎 194-9 «can't see new updates» (измерено) | слово владельца: «can't see new updates» + вставка экрана поиска, заканчивающаяся кнопкой «Открыть». nginx: 18:27:48 свежие чанки (страница перезагружена, код новый), 18:27:52 `object-search` 200, **ни одного запроса на открытие** — кнопку не нажимали. В сборке `object-test?object=` 1 файл, `object-open` 0; сервис-воркера нет. **Причина — прочтение, а не доставка:** блок стоял за «Открыть», а владелец просил видеть всё «в этом поиске». Решение агента: `SavedView` у каждого найденного объекта сразу, кнопки «Открыть» нет |
| ⏳ 194-9 доставлен (первая редакция) | `b2a4392`, `tsc` 0, `BUILD_OK`. Общий `SavedView` на загрузку и поиск; «Открыть» ходит в `?object=`; дверь `object-open` оставлена агенту. **Прибор `object-search-view.mjs` через `https://memory.aifa.dev`, без модели:** сохранено messageId 21 → поиск нашёл его (0.690) → `?object=` отдал строку 21, поле в поле равную `?message=` · негатив: `agent-channel-routing.md` (демо-корпус 192) → `ok`, `row=null`, файл есть · чужой `tg-8-…jpg` → 404 · уборка. **Ждёт глаз владельца**; затем 194-7 — закрытие шага |
| ▶ 194-9 начат (история) | слово владельца «go to 194-9» — принято как согласие по 194-10, 194-11, 194-12. Строится: `getMessageByObject` · `objectView` · `GET object-test?object=` · общий блок «Что легло в память» для загрузки и поиска · прибор `object-search-view.mjs` без модели |
| ⏳ 194-10 и 194-12 передоставлены | `b5c6f45`, `tsc` 0, `BUILD_OK`. **Прибор через публичный `https://memory.aifa.dev` на файлах владельца:** `landing-toc.tsx` (4318 байт) описание 200 за 33 с, полное 2644 знака, блоков кода 0 — анализ «рисует карточку-оглавление»; `landing.tsx` (22955 байт) 200 за 37 с, полное 3011 знаков, блоков кода 0; оба сохранены (`kind=code`, `saved`), дверь файла `text/plain`, байты совпали, уборка. Предел 20 МБ снят. **Прокрутка:** на публичной `/ru` `<html>` со `scroll-smooth` 1, `motion-reduce:scroll-auto` есть, разделов со `scroll-mt-16` 18, негатив 0. **Ждёт глаз владельца** |
| 🔒 слово владельца об оглавлении | дословно: «Во время изменения дизайна ты был большой молодец и сделал блок навигации. Но ты забыл добавить плавное прокрутку.» → в 194-12 добавить плавную прокрутку тем же приёмом, что на 3000; едет одной доставкой с правкой описания кода |
| 🔎 диагноз «код не работает» (измерено) | nginx `POST object-test*` с 17:30: 17:39:21 describe `400` (до доставки 194-10, код ещё не род) · 17:59:25 describe `landing.tsx` `200` за 80 с — модель описала устройство файла, **сохранения после не было** · 18:02:37 describe `502` «upstream prematurely closed» — **мой `pm2 reload` доставки 194-11/12** (`DELIVERED_COMMIT` 18:00:26, лог сборки 18:02:33, старт процесса 18:02:39). OOM нет, `max_memory_restart` нет. ✗ **Урок: доставка перезапускает службу и рвёт запрос владельца посреди хода модели — предупреждать до доставки.** Дефекта самой загрузки кода не найдено, но и путь `landing-toc.tsx`/`landing.tsx` через публичный адрес не пройден — прибор переписывается на них |
| 🔒 слово владельца: код не работает | дословно: «робую делать загрузку файлов fractera-memory-starter\app\[lang]\_components\landing-toc.tsx, fractera-memory-starter\app\[lang]\_components\landing.tsx провал и везде неудачи//Во-первых для полного описания нужно пытаться ставить предел верхний потому что не нужно чтобы он с точки зрения кода переписывал все его содержимое его задача понять и описать что этот элемент кода выполняет, не стоит задачи для кода переписать всю его содержимое. А во-вторых тут не должно быть никакого лимита по килобайтам и так как мы сохраняем обычный файл который потом будем просматривать просмотрщики то не должно быть никаких ограничений. Но не маленькие небольшой файл мне не удалось загрузить. И однозначно я увидел что описание работает не так как он нужно. Сам подумай предположим я нашел какую-то интересную функцию она может быть делает кнопку пульсирующей, мне не надо чтобы в описании было написано информация о том какие строчки кода один к одному мне есть нужно проанализировать и сказать что эта функция для Моргане кнопки и дать дополнительное описание по импортом и экспортом используемые технологии если это возможно. Ну в общем с файлами кода  пока у нас не получилось тестирую исследуй» → **194-10 не принят**: (1) найти причину отказа на `landing-toc.tsx`/`landing.tsx`; (2) описание кода — анализ назначения, импорты/экспорты, технологии, с верхним пределом длины, БЕЗ дословного исходника (отменяет решение агента «исходник дословно до 12 000 знаков»); (3) снять предел размера файла |
| ⏳ 194-11 и 194-12 доставлены | `db5fae0`, `tsc` 0, `BUILD_OK`. **Замер публичной memory.aifa.dev:** `/ru` Markdown 9 · HTML 7 · «исходный код» 6; `/en` Markdown 9 · HTML 7 · «source code» 5; «OCR» 0 и обещание локального Whisper 0 на обеих; зеркало `/ru/index.md` обновилось само (строится из словаря). Оглавление: `aria-label="Оглавление"`/`"Contents"`, **18 из 18 пунктов и 18 из 18 разделов с теми же id**, ложный якорь 0. 🔒 **Свойство страницы, не дефект правки:** главная отдаётся потоком (`cacheComponents`) — в `<nav>` первыми приходят 9 пунктов, остальные 9 ложатся `<template id="P:…">` и вставляются `$RS` при загрузке; разделы главной приходят так же. ✗ Прибор ошибся дважды: образец `new RegExp` в шаблонной строке не совпал с разметкой, затем счёт внутри `<nav>` не видел потоковых пунктов — третий замер по всему документу. **Ждёт глаз владельца** |
| 194-11 и 194-12 построены (история) | ТЗ `194-11.md`, `194-12.md`. Текст главной `en`/`ru` правлен в 7 местах (роды + правда: whisper-1 в OpenAI, Claude читает, OCR нет, код не запускается; раздел форматов — 6 карточек, сетка 2 колонки). Оглавление: `landing-toc.tsx` (копия `toc`, `aria-label` из словаря), 18 пунктов над телом. ✗ код начат раньше ТЗ — порядок нарушен, ТЗ дописаны до доставки. Дальше: зеркала `index.md`/`llms.txt`, `tsc`, доставка, замер отданной страницы |
| 🔒 слово владельца после 194-10 | дословно: «and for update content about new object types and after  design memory.aifa.dev add to page block toc1 from https://aifa.dev/ru/architect/design?section=blocks&kind=page-material» → **«да» на правку публичного текста главной** о новых родах (Markdown, HTML, код) = 194-11; блок оглавления `toc1` из каталога FNS на главную памяти = 194-12. Глаза на экран кода (194-10) словом не подтверждены |
| ⏳ 194-10 доставлен | `fa3ca9e`: локально роды `.ts`/`.tsx` → code, `.mp4` → video, `.pdf` → pdf, `.md` → markdown, `.html` → html, `.txt` → text; `tsc` 0 ошибок · `BUILD_OK`, `BUILD_ID` есть · прибор `object-code.mjs` `✓ OK`: `data-service.ts` (mime `video/mp2t`) описание 200 за 51 с, `code-view.client.tsx` (без mime) за 39 с, исходник дословно, строка `kind=code`, дверь `text/plain`, байты совпали · **пересборка таблицы без потерь: 4 строки владельца до (document·image·pdf·video) = 4 после, временной таблицы нет, индексы на месте.** Прежняя md-строка владельца осталась `document` — записана до родов. **Ждёт глаз владельца**; текст главной — ждёт его «да»; затем 194-9 и 194-7 |
| 🛑 ОБЪЯВЛЕНО ДО ОПЕРАЦИИ | доставка 194-10 пересоберёт живую таблицу `messages_that_came_into_memory` (новый `CHECK` родов): временная таблица → перенос строк поимённо → сверка счёта → удаление старой → переименование. Счёт разошёлся — старая не тронута. ✗ первая редакция отказывала при строках — сохранения владельца после доставки перестали бы проходить; исправлено до доставки |
| 194-10 заведён | ТЗ `new-steps/194/194-10.md`. Причина по чтению кода: `.ts` приходит `video/mp2t` → род «видео» → ffmpeg; `.tsx` без mime → «род не описывается»; экран требует ручного саммари. План: перенос `code-view` FNS (подсветка через streamdown), один список расширений `_tools/code-view/types/code-langs.mjs`, `text/plain` для кода, роды `code/markdown/html` — **таблица пересоздаётся кодом только при 0 строк (объявлено заранее)** |
| ⏳ 194-8 доставлен | `79364bb`: локальный `tsc` RC 0 до доставки · `BUILD_RC=0`, `BUILD_ID` на месте · прибор `object-preview-kinds.mjs` `✓ OK`: PDF `application/pdf` 96966 байт, Markdown `text/markdown` 13351, HTML `text/html` 202 — байт в байт, `inline`; клиентских файлов с `allow-scripts` 1, с `allow-same-origin` 0. **Ждёт глаз владельца**; 194-9 не начинать до слова |
| ✗ 194-8 сборка упала (история) | `2d20515`: `the name previewOf is defined multiple times` (`objects.ts:165`) — моя `previewOf` для просмотра столкнулась с существующей `previewOf(card)`. **Упавшая сборка стёрла `.next/BUILD_ID`**: служба жива только прежней сборкой в памяти (порт 200), перезапуск её уронил бы. Прибор 194-8 после провала мерил старую сборку — его 12 ✗ недействительны. Лечение: имя `previewItemOf`, локальный `tsc` с кодом выхода ДО доставки. Урок: перед доставкой `.ts` — `tsc`, иначе провал сборки бьёт по живой службе |
| 194-8 построено | `_tools/object-view/{tool.json, client/object-preview.client.tsx}` — копия `PreviewPopup` панели с пятью названными адаптациями (streamdown вместо react-markdown/shiki · аудио · режим `inline` · дверь `object-file` и словарь памяти · тип `PreviewItem` у инструмента); `messageView` отдаёт `media`; блок «Что легло в память» зовёт инструмент, своё превью 194-5 удалено; прибор `scripts/probe/object-preview-kinds.mjs` (PDF, Markdown, HTML со `<script>`) |
| 194-8 переписан под перенос | источник `preview-popup.client.tsx` панели (картинка · видео · PDF · Markdown · HTML страница⇄код, `sandbox="allow-scripts"`); **аудио у источника нет**. На сервере памяти `react-markdown` НЕТ, `shiki` НЕТ, `streamdown 2.6.0` есть → решение агента: Markdown и код через `streamdown`, аудио добавить, встроить в блок вместо всплывающего окна |
| 194-7 прибор видео | ✓ `boris-chernoy-post-1.mp4` 40 с → 200 за 54 с, 17 меток реплик, 6 меток кадров, саммари 58 слов, папки стёрты. ✗ **НАХОДКА: в саммари иероглифы «循环-циклы»** — утечка CJK в русский текст, `checkShape` её не ловит; чинить проверкой |
| 194-6 навык и вкладка «Навык» | ✅ слово владельца «go to 194-7»; `b533d4c` доставлен: sha навыка на сервере = git (`f285116a…`), лид вкладки в сборке 2 файла, ложное слово 0, путь чтения навыка в серверном коде страницы 5. **Ждёт глаз владельца** (вкладка за ролью architect, прибору `307`); 194-7 не начинать до слова |
| 194-6 (история) | построено: `.claude/skills/describe-incoming-object/SKILL.md` (англ., описывает двери как есть, пределы названы: к `/v1/remember` не подключён, руки `describe_object` нет); `test-tabs.ts` — `tabsOf()`, «Навык» только у стенда объектов; `page.tsx` читает навык с диска при открытой вкладке и показывает `<pre>` байт в байт; слова `en`/`ru` |
| 194-5 (история) | `61b0969` доставлен, прибор `✓ OK` (чтение по номеру 200, колонки 32 из 32, полное описание из медиатеки, файл `image/png` байт в байт 98757, чужой файл Telegram → `404 not-ours`, несуществующий номер → 404, уборка). **Ждёт глаз владельца** — закон построенной поверхности; 194-6 не начинать до слова |
| 194-5 (история) | построено: после сохранения экран читает легшее дверью `GET object-test?message=<n>` (`messageView`) и показывает файл (своя дверь `object-file`, только объекты памяти, имя в `proxy.ts`), полное описание из медиатеки, саммари и таблицу одной строки с прокруткой. Прибор `scripts/probe/object-saved-view.mjs` (без модели, негатив — чужой файл медиатеки `404 not-ours`) |
| 194-4 (история) | построено: `keep()` — файл+полное описание → медиатека, карточка из саммари → вектор, якоря → граф `object/<id>`, строка `messages_that_came_into_memory` со ссылками; сбой → откат и строка `failed`. `sql()` отдаёт `lastInsertRowid`; `insertMessage`/`getMessage` в `messages.mjs`; дверь принимает новые поля и отдаёт `messageId`. Прибор `scripts/probe/object-save-whole.mjs` (без Claude, негатив — пустые якоря) |
| 194-3 (история) | построено: «Получить описание» справа от «Выбрать файл» (только с файлом), счётчик секунд, поля «Полное описание» и «Саммари» (прежнее `about`), смена файла сбрасывает описание, с сохранением едут `full/title/tags/anchors/described_by/describe_ms/language` для 194-4; `costNote` исправлен (прежний говорил «файл не читает ни одна модель» — стало неправдой) |
| 194-3 … 194-7 | — |

🔒 **Живая база теперь несёт три новые таблицы** `messages_that_came_into_memory`, `…__links`, `…__registry`
(пустые). Снимок «до» 193 (42 колонки корня, 4 таблицы `person_who_owns_this_project%`) их не касается.

## ✅ ШАГ 193 ЗАКРЫТ 2026-09-13 — без прогонов, по слову владельца

Итог — `completed-steps/193-main.md`. Навык `use-tables` заменил `grow-the-shape`; три руки записи
измерены прибором; два дефекта рук починены (`8346c9c` имя, `921b461` род утверждения).
🛑 **Навык не доказан различием поведения** (итерация 1 — 100% и с навыком, и без; итерация 2 остановлена).
🛑 **ПОДАГЕНТЫ ЗАПРЕЩЕНЫ ПОЛНОСТЬЮ** (`CLAUDE.md`, deny `Agent`/`Task`): две сожжённые сессии.
🛑 Шаг 189 по-прежнему не закрыт (189-7 — прогон владельцем на его данных).
Живая память сверена со снимком: колонок 42, таблиц 4.

## ✅ ШАГ 192 ЗАКРЫТ 2026-09-13 — объектное хранилище в стандарте графа и вектора

🔒 **189 И 192 ВЛИТЫ В `main` ПО СЛОВУ ВЛАДЕЛЬЦА** 2026-09-13 («влей 189 и 192 в main и задеплой»):
`main == origin/main == bbfae71`, доставлено на сервер целиком (52 файла), прибор объектов на боевой
сборке `✓ OK`. 🛑 **Шаг 189 при этом НЕ ЗАКРЫТ** — его план остался в `new-steps/189/`, подшаг 189-7
(закрытие и прогон владельцем) не сделан. Влиты, но не закрыты — это разные вещи.
🛑 **Найден и починен дефект доставки:** `deliver-memory.sh` исключал `development-docs/` целиком, а
вкладка «Паспорт» читает `PASSPORT.md` **с диска на каждый запрос** — правки паспорта не доезжали бы
молча. Теперь паспорт едет отдельным изъятием.

**Слово владельца, дословно:** «Сейчас нужно сделать тоже самое для объектов которые будут уходить в
объектное хранилище… сделай это в стандарте граф и вектора. Используй skill creator для тестов
навыков в реальных прогонах используй объекты из Publick и другие реальные md. Не ищи и не зови меня
с вопросами я не доступен… решить эту задачу от начала и до конца самостоятельно».

**Итог — `completed-steps/192-main.md`** (+ `192-1 … 192-6`). Ветка `new-step-192` от `new-step-189`,
**отправлена в origin, в `main` не влита** — шаг 189 не закрыт владельцем. Открытых шагов в работе нет.

**Ждёт глаз владельца:** раздел «Тест объектного хранилища» на `memory.aifa.dev/ru/settings?section=object-test`
(под ролью `architect`, прибором недостижим). Решения агента по полномочию — `192-main.md`, раздел
«Решения агента»; развернуть любое — одно слово.

## Дорогие измерения шага 192 — не искать заново

- **объектное хранилище = медиатека слоя данных** (`/media/*`); поиска у неё нет — объект находится по
  **карточке** в векторах, коллекция `memory-objects`, `ref_table=media`, `ref_id=<id>`;
- **порог 0.30** на 32 файлах FES: нужные 0.356–0.675, посторонние 0.107–0.249; поиск ~225 мс, загрузка ~270 мс;
  первый вызов после сборки холодный (3171 мс — не свойство склада);
- **двойники** (en/ru, md/pdf) ранжируются неточно: «брошюра на английском» поставила русский PDF первым;
- **абзац из глубины документа поиск объектов не видит** (карточка = первые 1500 знаков);
- **демо-корпус 32 файла FES лежит в складе**; в медиатеке 37 = 32 + 5 чужих (снимки Telegram, картинки
  seed, `development-loop-2026.jpg`) — чужие не трогать; убрать корпус — `DELETE /api/fractera/object-test`;
- **прибор:** `CORPUS_DIR=/tmp/obj-corpus node scripts/probe/object-two-tests.mjs [keep] [sabotage]`;
  корпус на сервере в `/tmp/obj-corpus` (переживёт до перезагрузки машины; источник — FES `public/`);
- **доставка памяти одной командой:** `bash /code/scripts/deliver-memory.sh <базовый-коммит>`;
- **прогон навыка на живых руках:** `development-docs/instruments/memory-hands.mjs --log <файл> <рука> '<json>'`;
  агрегатор skill-creator ждёт раскладку `eval-N-<имя>/<конфигурация>/run-1/`; Python есть только в WSL;
- сторож оптимизации описаний из сессии 2026-09-13 ночью **не запущен** (`pgrep` пуст, лог оборван на пробе 1).

## Где мы были — 2026-09-12

**Идёт шаг 189 — настоящие тесты памяти: стенды графа знаний и векторного хранилища.**
План: `new-steps/189/189-main.md` + `189-1 … 189-7`.

**Построены и доставлены 189-1 … 189-4**, итоги лежат в `completed-steps/`:

| Подшаг | Что построено | Коммит |
|---|---|---|
| **189-1** | каркас: два раздела, три вкладки, перенос `knowledge.ts` из чата | `d96e1a6` |
| **189-2** | загрузка в граф: дверь, экран, ожидание по метке | `a3780ad` |
| **189-3** | цена загрузки числами + **двери стенда принимают ключ памяти** | `3d3fbcb`, `52ef4b1` |
| **189-4** | поиск с нашими ключевыми словами, забывание по метке `bench/` | `31d1cba`, `bc1f8e8` |
| **189-5** | вердикт человека и корпус случаев: цена и оценка в одной строке | `18ce0b7` |
| **189-6** | векторное хранилище той же формой; порог измерен на своём корпусе | `be5036d` и след. |

**Сделано сверх плана в тот же день** (задачи пришли посреди шага, от владельца):

| Что | Коммит |
|---|---|
| **инструмент `ask_graph` у агента памяти** + доставка инструментов (`--mcp-config`) | `97d1387` |
| **исправление костыля:** двери стенда открывает секрет машины, а не ключ памяти | `095a18d` |
| паспорт §19: инструмент · навык · одна строка инструкции | `2b12bf1` |
| навык `use-knowledge-graph`, переписан без истории и переведён на английский | `84deb16`, `64c490a` |
| строка о графе в инструкции памяти; **надгробия в инструкции запрещены** | `1408c6b` |
| инструкция памяти переведена на английский целиком | `54bee57` |

**Впереди:** 189-7 (закрытие шага и прогон владельцем на его данных).

## ✅ СМЕНА МОДЕЛИ ВСТРАИВАНИЙ СДЕЛАНА И ИЗМЕРЕНА (2026-09-13)

**Слово владельца:** «text-embedding-3-small — need update to powerfull model». Разрешение на правку
живой службы дано им же; саму правку выполнил он в своей оболочке — сторож запрещает агенту писать
в `.env` общей службы через SSH.

**Замер на одном корпусе, одних вопросах, менялась только модель:**

| | small (1536) | large (3072) |
|---|---|---|
| нужный кусок найден | **3 из 5** | **5 из 5** |
| верхним оказался нужный | 3 из 5 | **5 из 5** |
| чужие вопросы отсечены | 5 из 5 | 5 из 5 |
| разделимость групп | **НЕТ**, перекрытие 0.002 | **ДА**, зазор 0.16 |
| прибор | ✗ ПРОВАЛ | ✓ OK |

🔒 **ГЛАВНОЕ НЕ ЧИСЛО ПОПАДАНИЙ, А ЗАЗОР.** У small худший верный (0.245) лежал НИЖЕ лучшего
постороннего (0.247) — порога, отделяющего находку от ближайшего мусора, не существовало в принципе.
У large верные от 0.357, посторонние до 0.197. Порог 0.28 попал в середину зазора и остался прежним.

**Цена:** загрузка 4839 мс против 1455 (втрое), встраивания примерно вшестеро дороже за объём.
Чтение не изменилось: 170–400 мс.

**Куда правка доехала:** `.env` слоя данных на машине (копия рядом,
`.env.bak-embed-2026-09-13`) · `ai-workspace/services/data/server.js` умолчание (`e561372`,
отправлен) · `fractera-easy-starter/lib/bootstrap.sh` (`660bd1c`, отправлен) — **это правка
замороженной инфраструктуры L1, меняющая посевное состояние каждого будущего сервера**.

🛑 **ГРАФ ЗНАНИЙ НЕ ТРОНУТ НАМЕРЕННО.** Прежний выбор small там обоснован (встраивания доминируют в
стоимости), а мерил я ДРУГОЕ хранилище. Перенос вывода с чужого корпуса — ровно та ошибка, которую
измерение и поймало. В `bootstrap.sh` рядом с настройкой графа оставлен открытый вопрос с условием:
сперва измерить граф на его собственном корпусе, и помнить, что смена там означает
переиндексирование всего уже уложенного.

## ⏳ СТОРОЖ ОПТИМИЗАЦИИ ЖДЁТ КВОТЫ (поставлен 2026-09-13, 03:38 по серверу)

**Слово владельца:** «когда квота вернётся, повтори оптимизацию».

На сервере живёт `/tmp/opt-waiter.sh` под `setsid` — он **переживает конец нашей сессии**. Пробует
`claude -p` раз в 15 минут, предел ожидания 8 часов; дождавшись живого ответа, прогоняет цикл для
обоих навыков по очереди и кладёт результат.

| Что | Где смотреть |
|---|---|
| ход сторожа и лучшие описания | `/tmp/opt-waiter.log` |
| полный отчёт по лестнице | `/tmp/opt-ladder.out` |
| полный отчёт по форме | `/tmp/opt-shape.out` |

**Квота вернётся в 4:30 (Europe/Berlin)** — так сказал сам CLI.

🛑 **КВОТА ОБЩАЯ С БОТОМ И РАБОТОЙ ВЛАДЕЛЬЦА, И СТОРОЖ НАЧНЁТ ТРАТИТЬ ЕЁ ПЕРВЫМ.** Остановить:
`pkill -f opt-waiter.sh`.

🔒 **ПЕРВЫМ ДЕЛОМ ОН ДЕЛАЕТ ОДИНОЧНУЮ ПРОБУ И ТОЛЬКО ПОТОМ ЦИКЛ** — закон, оплаченный в тот же день:
при мёртвом вызове прибор печатает нули у всех запросов, и половина проверок становится зелёной,
потому что «не сработало» есть ожидаемый исход для отрицательных.

## 🔒 Решения, принятые агентом по переданному полномочию (2026-09-12)

**1. Судья для вектора — ПОРОГ БЛИЗОСТИ, измеренный на своём корпусе.** У вектора есть число, у
графа его нет; значит вектор умеет честно сказать «ничего подходящего», а у графа эту роль
выполняет сам агент — навык велит ему читать вернувшееся и не сочинять ответ из чужого материала.

**2. Прогоны приборов не ложатся в корпус случаев** — помечены полем `probe`. Признак едет полем, а
не словом в вопросе: у вектора текст уезжает в эмбеддинг, и метка исказила бы измеряемое.

**3. Изоляция корпусов стендов остаётся долгом** — вопрос одного испытания притягивал сущность
другого. Для стенда терпимо, для продукта нет.

### 🔒 Два испытания графа, прогнаны 2026-09-12 (`scripts/probe/graph-two-tests.mjs`)

**A — связь через третью сущность.** Пять коротких записей, ответа нет ни в одной целиком:
Ирина → верфь → Пётр → фестиваль → Лидия.
Загрузка: **36 с**, из пяти записей извлечено **22 сущности и 19 связей**. Чтение: **250–400 мс**,
ходов модели ноль. Вопрос «где учился начальник верфи» вернул «Николаев — город, где Пётр Мазур
учился на судостроителя», хотя слова «начальник» в тексте нет. **Связи работают.**

**B — границы.** Два вопроса из пяти заведомо без ответа в корпусе.
🛑 **ГРАФ ОТВЕТИЛ НА ОБА.** «Кто из них работает врачом» → «Николаев» и «Верфь Онега» (мусор из
корпуса A); «сколько стоит билет» → описание парома без единой цифры.

✗ **И ГЛАВНАЯ НАХОДКА — ПРО МОЙ ЖЕ ПРИБОР: МЕРКА «НАШЛОСЬ» НИЧЕГО НЕ МЕРЯЕТ.** Находкой считался
ответ длиннее восьмидесяти знаков — по этой мерке «нашлось 5 из 5» в ОБОИХ испытаниях, включая то,
где половина вопросов без ответа. **Прибор, который не умеет провалиться, бесполезен.** Это долг
прибора, и чинится он вместе с решением по судье: мерка обязана отличать «нашёл» от «вернул
ближайшее».

🔒 **ГЛАВНОЕ ИЗМЕРЕНИЕ ШАГА, ПОДТВЕРЖДЁННОЕ ЖИВЬЁМ 2026-09-12:** цена теперь там, где её ждёт
владелец. Загрузка дорогая (модель читает текст и извлекает сущности), чтение — **387 мс и без хода
модели**, потому что ключевые слова даём мы. Тот же вопрос без наших слов: **1595 мс**, и служба
графа печатает СВОИ слова, склонённые и с заглавной, — их сделала модель.

🔒 **ПРИБОРЫ ТЕПЕРЬ ХОДЯТ КЛЮЧОМ, БЕЗ БРАУЗЕРА** (`lib/bench-guard.ts`): `graph-bench.mjs` и
`graph-search.mjs` прогоняются на сервере и дают машинное доказательство. Страница осталась под
ролью `architect` — ключом открываются двери, а не экран.

**Слово владельца 2026-09-12, дословно:** «Должны обеспечить визуальные тестирование работы так как
я ожидаю а я ожидаю что во время загрузки происходит вызов модели и создание графа знаний, после
этого модель отвечает мгновенно… делай ещё слева меню которое должен называться тест графа знаний и
тест векторного хранилища. Внутри создавай страницы… загрузить свои данные нажать преобразовать и
сохранить хранилище затем найти и заранее сохранённого затем получить оценку того что найдено…
система не готова приступить к работе потому что нет реальных Тестов».

**И способ работы — его же словами:** «переноси knowledge.ts в память и начинай с каркаса, Попробуй
использовать стратегию вырезать вставить адаптировать… если мы щас начнём писать этот код с нуля то
опять напишем очень сырой и бедный вариант».
**И указание, куда идти за прецедентом:** «пойдёшь туда где память-то работала раньше chat.aifa.dev
и посмотришь как там все сделано».

### 🔒 Прецедент найден — из него берём готовое, а не пишем заново

| Что | Где лежит |
|---|---|
| клиент графа: `ask()` в режиме контекста, `learn()` С ЯКОРЯМИ, `labels()`, `forgetDocuments()` | `fractera-telegrambot-starter/lib/fractera/knowledge.ts` → **перенесён в память 189-1** |
| дорога к графу — через слой данных `/service/rag/*` | там же; **своего ключа графа память не заводит** |
| векторный склад: `POST /vectors`, `/vectors/search`, `/vectors/status`, таблицы `vectors`, `vectors_ann`, порог **0.33** | слой данных `:3300` |
| форма испытания: реальный корпус, точное попадание, попадание в пятёрку, посторонний вопрос не находит | `scripts/probe/{build-corpus-167,vector-store-167-1,graph-rag-167-2}.mjs` чата |
| предостережение: семь способов, которыми ТЗ подыгрывает себе | `reports/errors-test-corpus-was-shaped-to-the-shape-that-already-passed.md` чата |

### 🔒 Измерено 2026-09-12 — цена сидит не там, где должна

- **граф в режиме контекста бесплатен ТОЛЬКО с нашими ключевыми словами:** без
  `hl_keywords`/`ll_keywords` — **3538 мс** и строка `== LLM cache == saving: hybrid:keywords:` в
  журнале графа (звался `gpt-4o-mini`); с нашими словами — **666 мс** и ни одной такой строки;
  повтор без слов — 372 мс, но это его КЭШ, а не отсутствие вызова.
  ✗ **Прежний код чата слов не слал** — значит и там чтение платило ход модели. Лечится в 189-4;
- **в графе пусто:** документов ноль, одна сущность `привет` от чужого прибора от 9 сентября;
- **`/opt/fractera/memory` ОКАЗАЛСЯ git-репозиторием** (`HEAD 5aafdfe3`) вопреки прежней записи
  «папка не клон». Доставка всё равно идёт файлами через `git archive` + `tar -xzf`;
- 🛑 **страница `/{язык}/settings` закрыта ролью `architect`:** `curl` снаружи и даже с петли
  получает `307` на `auth.aifa.dev`. Значит доказательство «страница отдала разметку» снаружи
  **недостижимо** — доказываем собранным деревом на сервере и глазами владельца.

## Построено раньше
| Шаг | Что построено | Итог |
|---|---|---|
| **183** | стенд памяти: девять органов, охват списком записей, «расширенные параметры» | `183-main.md` + `183-1` … `183-7` |
| **184** | нить разбора: память возвращает `thread` и продолжает цепочку | `184-main.md` |
| **185** | вкладка API: документация из договора, ключ доступа, Postman | `185-main.md` |
| **186** | лендинг, поисковый слой, markdown-зеркала, перелинковка служб | `186-main.md` |

## Что стоит на сервере сейчас

- **договор 2.2.0**: `recall → who, text, lang, depth, history, prior, want_chain, scope`;
  `remember → who, text, lang, scope, media, thread, deny, need_table`;
- **ключ доступа** к `/v1/*`: `x-memory-key` либо `Authorization: Bearer`; файл
  `/etc/fractera/memory-api-key`, права `0600`. 🛑 **Действующий ключ создан прибором 185** — прежде
  чем отдавать его кому-либо, породите новый на вкладке API;
- **публичные адреса**: `/{язык}` (лендинг), `/{язык}/passport`, `/{язык}/index.md`,
  `/{язык}/passport/index.md`, `/llms.txt`, `/{язык}/llms.txt`, `/sitemap.xml`, `/robots.txt`;
- **подвал обеих служб** несёт верхний блок с перелинковкой: память ↔ чат, по ссылке на каждой;
- **приборы** (зелёные на живой службе): `bench-call` · `depth` · `who` · `context-input` ·
  `chain` · `scope-and-deny` · `thread` · `api-key` · `api-i18n`.

## Следующая работа — программирование самой памяти

Читать в этом порядке: `development-docs/PASSPORT.md` (18 разделов, замысел целиком) → `CLAUDE.md`
(инструкция агента памяти) → `development-docs/BLACKBOX-API.md` (что построено).

Ближайшее по плану паспорта, в порядке цены:

1. **Уровень 3 — граф знаний в режиме контекста**, без хода модели на чтение (паспорт §4).
2. **Уровень 4 — векторное хранилище** по требованию зовущего (`depth: "deep"`).
3. **Уровень 5 — рекурсивное исследование**, предел десять минут; таймаут разбора внутри памяти
   сегодня 120 с, фоновый режим `--bg` измерен и доступен.
4. **Мультимодальный конвейер**: расшифровка речи, зрение и OCR, разбор PDF, объектное хранилище.
   Параметр `media` уже принимается и проверяется закрытым списком родов.
5. **Пространственный индекс охвата**: координаты принимаются и валидируются — впереди индексация и
   поиск по радиусу.
6. **Цикл дообучения и теневые сплит-тесты навыков** (паспорт §11, §14).
7. **Навыки** — сегодня их нет ни одного; создаются по одному, начиная с работы с таблицами.

🔒 **ПУБЛИЧНЫЕ ТЕКСТЫ ОПИСЫВАЮТ ЭТО КАК РАБОТАЮЩЕЕ — РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-11, ДОСЛОВНО:**
«нам запрещено писать информацию о том, что что-то ещё не сделано… расскажи о тех функциях, которые
мы запланировали, как уже реализованных». Касается `README.md`, лендинга и вкладки API.
🛑 **ВНУТРЕННИЙ УЧЁТ ЧЕСТЕН, И ЭТО НЕ ПРОТИВОРЕЧИЕ:** здесь и в паспорте состояние называется как
есть — иначе следующая сессия начнёт строить по витрине и решит, что строить нечего.

## Дорогие измерения — не искать заново

- служба живёт из `/opt/fractera/memory`, процесс pm2 `fractera-memory`, скрипт `server.mjs`;
  папка **не клон** репозитория — доставка только файлами: `git archive` → `scp-to-fractera.sh` →
  `tar -xzf` → `npm run build` под `flock` → `pm2 reload`;
- после `pm2 reload` порт ждать **по факту** (`curl` до 200), а не паузой;
- 🛑 **`headers()` и чтение файла под `cacheComponents` — только под `<Suspense>`**: оплачено
  сборкой трижды за 2026-09-11 (крошки чата, крошки памяти, лендинг);
- 🛑 **SSH-обёртка обрывает длинные вызовы**: замеры `claude -p` и сборки запускать на сервере
  отдельно (`nohup … &`) и читать файл результата;
- 🛑 **правки с обратными слэшами и кавычками пишутся редактором или файлом-скриптом**, а не строкой
  через оболочку: за день потеряны слэш в регулярке и целый абзац текста;
- 🛑 **прибор, у которого два исхода дают одно число, измеряет не то, что утверждает** (183-4), и
  **замер, зависящий от состояния машины, зелен только на чистой** (183-7);
- нить `claude -p`: **один `session_id` на нить**, продолжение ДЕШЕВЛЕ нового вызова
  (`0.0289 → 0.0065 → 0.0036`), кэш часовой, нить привязана к рабочей папке разбора
  (`/tmp/fractera-memory-think`);
- `/v1/*` открывается двумя ключами: секрет машины — своим процессам, ключ памяти — чужим
  инструментам; `/v1/health` открыт без обоих; снаружи `/v1/*` достижим через
  `https://memory.aifa.dev`;
- граф умеет отдавать контекст **без вызова модели** (`only_need_context`);
- квота подписки **одна** на память, бота и работу архитектора.

## Следующее действие

**Владелец передал работу целиком** (2026-09-12, дословно: «сам полностью завершает работу, не
привлекая меня, сам дай оценку и оставь тот навык, который действительно лучше. Затем повтори
полностью всю цепочку действий над векторным хранилищем — это следующая кнопка в левом меню — также
в конечном итоге создай навык, протестируй его»).

Значит: решения по судье и по мерке прибора принимаю сам, и 189-6 идёт без остановки на
согласование.
