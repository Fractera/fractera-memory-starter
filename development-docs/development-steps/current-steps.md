# Состояние работы — служба памяти (порт 3700)

> **Читается ПЕРВЫМ в каждой сессии, раньше кода и раньше паспорта.**

### ▶ ШАГ 206: закрыты 206-1, 206-2, 206-9, 206-7 (2026-09-15, ночь)

Ветка `new-step-206`. Договор живой службы — **4.0.0**. Всё доставлено, `DELIVERED_COMMIT` совпадает.

| Подшаг | Что сделано | Коммит |
|---|---|---|
| **206-1** | запись без колонок и дочерних таблиц: факт стал событием графа | `a0d223e` |
| **206-2** | порог 2000 слов; короче — связи **и вектор**, длиннее — только объект и **без вызова модели** | `4737cc9` |
| **206-9** | вход урезан: сняты `need_table`, `thread`, `deny`, `history`, `prior` и поле `thread` в ответе | `4737cc9` |
| **206-7** | паспорт §3 §4 §5 §6, главная ru+en, README, `CLAUDE.md`, `capabilities.json`, стенд | `dba2716` + правка по замеру |

**Слова владельца этой сессии, дословно (полностью — в `new-steps/206/206-main.md` и `206-9.md`):**
«память вообще прекратила работать с какими-либо специализированными таблицами кроме
одной-единственной который фиксирует все входящие сообщения… память это события вокруг базе… для граф
для вектора и для въездного хранилища» · «вход теперь это просто текст и или без какие-то объекты» ·
на вопрос о границе: **«text, odject, calendar records, geo, channel must to be save»** ·
«Удалить вместе с кодом» (старые данные) · «Убрать тоже» (`memory_bench_cases`) ·
«update memory.aifa.dev and passport for new goals».

🔎 **Дорогие факты сессии (не мерить заново):**
- вектор до 206-2 **не получал ни одной фразы человека** — его звали только стенды;
- доставка памяти **не возила удаления** (13 файлов реестра жили на сервере, где работает строитель
  мастерской) — исправлено в `/code/scripts/deliver-memory.sh`;
- удаление раздела панели «Реестр признаков» **лежало недоставленным**: панель на сервере собиралась
  из старого коммита; влито и пересобрано;
- правка по шаблону может **не примениться молча** — два таких случая в 206-7 нашёл только счётчик по
  живой странице.

▶ **Следующее — 206-3 (чтение) и вместе с ним инструменты агента.** Сегодня `what_i_already_know`,
`write_value`, `make_new_kind`, `promote_to_list` (`scripts/agent/memory-tools.mjs`) пишут в корневую
таблицу, которую память больше не наполняет; в `CLAUDE.md` они помечены снимаемыми, навык
`use-tables` — устаревшим. 🛑 **Устаревший закон в рабочем корпусе опаснее отсутствующего.**
Дальше по плану: 206-4 (удаление старых данных, деструктивное — объявляется списком ДО), 206-5
(кейсы стенда в файл), 206-6 (мёртвые приборы), 206-8 (доставка и живая проверка).
🛑 **Долг 206-3:** имя человека жило колонкой и уходило в шапку документа связей — сейчас там ключ.

---

### ▶ ШАГ 206 ИДЁТ: план подтверждён, 206-1 закрыт

Ответ владельца на план формой, дословно: **«Подтверждаю, начинай с 206-1»**. План и восемь ТЗ —
`new-steps/206/`. Ветка `new-step-206` (от `new-step-205`, где 205 закрыт итогом `205-main.md`).

✅ **206-1 ПОСТРОЕН, ДОСТАВЛЕН И ЗАКРЫТ** — `a0d223e`, `DELIVERED_COMMIT=a0d223e`, `BUILD_RC=0`, порт
за 3 с. Запись больше не заводит ни колонок, ни дочерних таблиц: сняты ворота записи
(`lib/write-gate.mjs`), создание корневой таблицы и чтение строки человека в начале записи (второе ещё
и ПИСАЛО пустую строку на каждого говорящего), повышение рода в таблицу, требование `need_table`
(объявлено неработающим, из договора уходит в 206-6) и список «что уже заведено» в подсказке модели.
Факт стал **событием графа**. Разбор ответа модели переехал в `lib/model-json.mjs`.
Доказательства: счётчик по исходнику (семь запрещённых вызовов внутри `remember` — нули, негатив
`toGraph` 3) и прибор на сервере `scripts/probe/one-table-206-1.mjs` — таблиц 59 до и 59 после живой
фразы с пятью фактами, негатив видит созданную таблицу. Итог — `completed-steps/206-1.md`.

🛑 **ДОЛГ 206-3: ИМЯ ЧЕЛОВЕКА.** Оно жило колонкой `name_he_is_called` и уходило в шапку документа
графа (требование владельца 2026-09-14). Брать его негде — шапка называет ключ.
🛑 **УСТАРЕВШИЙ ЗАКОН В РАБОЧЕМ КОРПУСЕ:** навык `use-tables` и раздел «Когда колонка, когда таблица»
в `CLAUDE.md` памяти описывают снятый механизм; инструменты агента `write_value`, `make_new_kind`,
`promote_to_list`, `what_i_already_know` — тоже. Переписываются в 206-7 (и 206-6 по договору).

▶ **Следующее: 206-2** — порог 2000 слов (слово владельца: «max 2000 words for vector and for
lightRAG , if more only for object store»).
🔎 **Дорогой факт, измеренный при разведке:** векторное хранилище (`lib/fractera/vectors.ts`) зовут
ТОЛЬКО стенды `app/api/fractera/vector-{search,test}`; путь записи в него не пишет ничего. Значит
206-2 — это не только порог, но и **подключение вектора к записи**.

---

## ▶▶ 2026-09-15, НОЧЬ — САМЫЙ СВЕЖИЙ БЛОК. ВСЁ НИЖЕ ЭТОГО РАЗДЕЛА — ИСТОРИЯ

### Реестр признаков удалён целиком, и это уже в коде

Коммит `27a9bf7` (ветка `new-step-205`, **отправлена** 2026-09-15), доставлен на сервер:
`DELIVERED_COMMIT=27a9bf7`, договор **3.0.0** (MAJOR), `health` 200. Слово владельца дословно:
«мы не будем использовать реестр признаков в проекте вообще… очисти любое упоминание памяти о реестре
признаков… в том числе из пункта как память обрабатывает запрос. Я дам тебе новую стратегию».
Взамен реестра — `lib/kinds-pick.mjs`: один вызов модели по ЗАКРЫТОМУ списку того, что у человека уже
записано. ✗ Потеря названа: суммы («сколько я потратил» возвращает значения по одному).

🛑 **Найдено и починено в эту сессию: ДОСТАВКА НЕ ВОЗИЛА УДАЛЕНИЯ.** `scripts/deliver-memory.sh`
собирал архив по `--diff-filter=ACMR`, и на сервере после удаления реестра остались **13 файлов** —
`AGI-CONFIG/agi-config.json`, `lib/features.mjs`, `features-index`, `features-discover`,
`feature-consumers`, сторож и четыре прибора. Служба их не исполняет, но **в этой же папке работает
строитель мастерской `/build`, и он читает удалённое как живое**. Убрано с сервера: было 13 → стало 0,
`AGI-CONFIG` исчезла, живые файлы на месте, `health` 200. Скрипт доставки теперь возит удаления
(`--diff-filter=D` → `rm` на сервере с `rmdir` пустой папки). Тот же класс уже был оплачен выше на
учёте шагов — там он лечился, для кода не лечился.

### ▶ НОВАЯ СТРАТЕГИЯ ВЛАДЕЛЬЦА 2026-09-15, ДОСЛОВНО — РАБОТА НАЧИНАЕТСЯ С НЕЁ

«Восемь весь день работаем над памятью, разом переписываю инструкции , мы зашли в тупик потому что я
решил твою работу с реестром признаков. Более того я хочу чтобы наша память вообще прекратила работать
с какими-либо специализированными таблицами кроме одной-единственной который фиксирует все входящие
сообщения и записи от базы данных. Мы её уже сделали. Таким образом что бы не говорили другие
пользователи памятник не создают свои таблицы и не читают другие кроме одной единственной таблицы
сообщений. Почему это будет сделано? Потому что будут другие приложения которые будут заниматься этими
задачами а память это события вокруг базе для обычной таблицы которые можно создаем для граф для
вектора и для въездного хранилища то есть всё делаем проще понятнее быстрее с фокусом на интеллект
агенты которые этим занимаются».

🔎 **Разведка (чтением кода, без модели):** сегодня память создаёт и читает
`messages_that_came_into_memory` (+ `__links`, `__registry`) — **та самая единственная таблица**,
а кроме неё: корневую `person_who_owns_this_project` с 46 родами-колонками и её историю
(`lib/store.mjs`), **дочерние таблицы рода** (`lib/promote.mjs`, переезд первого значения),
`memory_bench_cases` (`lib/cases.mjs`). Ворота записи `lib/write-gate.mjs` решают «колонка или
таблица». Граф, вектор и объектное хранилище — отдельные хранилища, их решение не трогает.

▶ **Следующее действие: завести шаг 206 «Память живёт одной таблицей», развернуть подшаги и
предъявить план владельцу формой (этап 9).** Кода до его слова не писать.

## ▶▶ ПЕРЕДАЧА СЕССИИ 2026-09-15 — ЧИТАТЬ ЭТОТ БЛОК ПЕРВЫМ, ОСТАЛЬНОЕ НИЖЕ — ИСТОРИЯ

**Окно предыдущей сессии исчерпано (93%). Всё, что нужно для продолжения, — здесь и в `new-steps/203/203-main.md`.**

### Где работа стоит

▶▶ **2026-09-15, вечер: шаги 203 и 204 ЗАКРЫТЫ** (итоги `completed-steps/203-1…203-5.md`, `203-main.md`, `204-main.md`; репорт
`reports/feature-memory-instruction-for-its-manager-development-skill-and-full-install-on-new-servers.md`). `main` памяти = рабочая память
(201 → 202 → 204 → 203 влиты; хэш — в коммите закрытия), FES `671df71` (установщик ставит память целиком), `/code` `3b334c3`.
**Шаг 201 остаётся приостановленным** (ждёт выводов владельца по отчёту 30+30; 201-8 — удаление `server.js`, теперь разблокировано).
В очереди: 199, федеральный 198; навыки под красными метками главной (9 штук и правило глубины в `use-tables`) — без слова владельца не
начинать. Таблица ниже — состояние до закрытия, история.

▶ **Новая задача, слово владельца 2026-09-15, дословно:** «tests not now , now we must find all Элементы реестра признаков, в смысле их их
понять как там довести до ума память чтобы мне все было закрыто что все навыки присутствовали вызывали справед ли последовательности и
памяти отрабатывало правильно. Все элементы реестре признаков сейчас описаны в вкладке чат посмотри тебе достаточно их описания если их
будет возвращать чат то что ты будешь делать ну например пришло тебе какой-то определённое название что ты будешь делать? Разве это не
дыра? Насколько я помню в прошлом сессии мы как попытались это описать но чем это закончилось я не помню в итоге ты можешь сейчас начать
работать с элементами реестра признаков или нет?» Перед этим прогон на `/v1/people` отклонён владельцем: тесты не сейчас.
▶ Разведка: реестр памяти `AGI-CONFIG`, итог 201-3, `checkGiven`, где описан реестр «во вкладке чат».
🔎 **Измерено 2026-09-15 (дорогой факт):** реестр чата — `fractera-telegrambot-starter/REGISTRY-CONFIG/registry-config.json`, **49 записей,
все `enabled: off`**; уровни: initiator 3 · material 9 · intent 8 · entity 6 · destination 3 · field 6 встроенных (relation, happened, money,
geo, forwarded, facets) · `person.*` 14 (`storedIn: fact_person_*`, у многих `onMissing: ask`). Вкладка — настройки чата
(`app/[lang]/settings/_lib/facts-handoff-text.ts`: «Прошу добавить новый элемент в реестр признаков проекта Telegram-бота»).
Реестр памяти — `AGI-CONFIG/agi-config.json`, **21 запись**, поля: key, title, valueType, aggregate, subject, depth, tags, triggers, answers,
howToFind. **Пересечение ключей чата и памяти — 0** (`person.name` у чата ↔ `person.name-he-is-called` у памяти). `checkGiven` отвергает
ключ, которого нет в реестре памяти, как `unknown-feature` → **признак, присланный чатом по своему ключу, память сегодня отвергнет всегда**.
35 признаков чата не `person.*` (род входа, намерение, сущность, куда уехало, поля) у памяти в реестре не представлены вовсе; у записи чата
есть поля, которых нет у памяти: level, description, requires, onMissing, fn, produces, derivedFrom, lifecycle, schedules, model, scope.
✅ **Шаг 205 «Реестр признаков закрыт целиком» заведён.** Ответ владельца на форму, дословно: **«Да, начинай с переписи (Recommended) …
//Предлагаю запланировать переиспользования элементов реестр признаков внутри нашей административной панели. Это позволит в любом
существующими элементом подключаться и извлекать свои собственные элемент реестра признаков для своих собственных нужд»**.
▶ Первый результат — перепись 70 элементов (49 чата + 21 памяти) с предложенным решением по каждому: признак реестра · механизм памяти ·
снять; владелец утверждает, потом подшаги и код. План — `new-steps/205/205-main.md`. Развилка к уточнению: «административная панель» =
панель проекта `:3002` или страницы самой памяти.
✏️ Перепись записана: `new-steps/205/205-census.md` — 70 элементов, решения A (признак реестра памяти) · B (механизм памяти вне реестра) ·
C (не память / дубль / долг); 14 фактов о человеке сведены ключ в ключ; 3 новых признака на решение (задачи, места, идеи); у записи памяти
не хватает: при отсутствии значения, порядок вопроса, пример, что теряется, охват, навык, потребители. ▶ Коммит и показ владельцу.
✅ Коммит `4a48527` (ветка `new-step-205`, отправлена). Ответы владельца, дословно: перепись — **«Утверждаю как есть»**; подключение к
реестру — **«Панель проекта на порту 3002»**. ▶ Разведка панели (`ai-workspace/bridges/app`): одна дверь к памяти, меню, ключ каталога →
план подшагов 205 на согласование.
🔎 Одна дверь слоя данных (`services/data/server.js:1443`) проводит rag, geo, channels, ai-browser — **памяти нет**; каталог памяти `/v1/*`
открывается ключом памяти или секретом машины; страниц панели, читающих службу через `/service/*`, нет ни одной; вкладка реестра чата —
`_components/facts-registry.tsx`. ✏️ План `205-main.md` и ТЗ `205-1…205-9` написаны (решение агента: определения — файл реестра,
подключения элементов — данные памяти; псевдонимов ключей нет; возраст — долг). ▶ Коммит и согласование плана формой.
✅ План закоммичен `33b8c7f` (отправлен). Ответ владельца на план, дословно: **«Подтверждаю»**. ▶ **205-1** — полная форма записи реестра
памяти (`lib/features.mjs`, сторож, прибор 201-3, договор, 20 записей скриптом по переписи).
✅ **205-1 построен и доставлен** `e2787bb` (от `c3550c4`, `BUILD_RC=0`, порт 4 с). Плоскость 1 локально: сторож 0 нарушений, прибор видит 5
новых негативов + выдуманный навык, эталон со всеми полями принят, `npm run check` — договор 2.11.0. Плоскость 2 на сервере: `GET
/v1/features/person.name-he-is-called` → 200, `ask`, порядок 2, пример «Алекс», навык `use-tables`; **негатив** `person.name` → 404.
🛑 **Находка прибора (старая проверка 201-3 покраснела):** «живых родов 46, покрыто признаками 19» — 27 родов корневой таблицы заведены моделью
в обход реестра (пищевые аллергии, правила о собаке, список вещей в поездку, машины, планы на месяц и др.); в 201-3 было 14/14. Перепись 205
считала только файлы реестров и их не видела. ▶ чтение `/v1/tables` (без модели, без удаления): чьи роды и сколько значений.
🔎 `/v1/tables`: корневая таблица 10 строк; таблиц родов 10, непустые только язык (11), «что он рассказывал раньше» (3), цвет (1).
`/v1/tables/person_who_owns_this_project` отдаёт описание 46 родов (`name`, `reads_as`, `explained`) — **счёта значений нет, заполненность
не измерена** (строка прибора «outside with values: 0» — пустое поле, не измерение). **Дубли смысла в данных:**
`money_he_spent_on_purchases_by_item` ↔ `spent_on_a_purchase`; `currency_he_counts_his_money_in` ↔ `currency_he_counts_money_in`.
**Нарушение §21.2:** `how_long_he_has_known_his_friend_denis`. Итог `completed-steps/205-1.md` написан. Перепись дополнена §9 (27 родов:
4 слить · 11 → 5 новых признаков · 12 → граф) — предложение. ▶ Решение владельца о 27 родах (форма), коммит.
✅ Ответ владельца, дословно: **«Добавить сверку в 205 (Recommended)»** → подшаг **205-10** (`new-steps/205/205-10.md`): счёт значений по
46 родам только чтением → показ владельцу → по его слову слияние дублей, 5 новых признаков, 12 родов в граф; колонки не удаляются, помечаются
снятыми; путь мимо реестра закрывается правилом 205-4. Идёт после 205-2 и 205-4, до 205-9.
**205-1 закрыт** (итог `completed-steps/205-1.md`, коммит `e2787bb`). ▶ Коммит итога, переписи §9 и плана → **205-2**.
✅ **205-2 закрыт** `460edfd` (доставлено): 3 новых признака, возраст снят (`retired`), реестр 23 действующих + 1 снятый. Прибор
`features-205-2` локально и на сервере «всё сошлось»; позитивный контроль индекса — задачи на месте 1 для «надо позвонить в банк».
🛑 Живая запись фразы НЕ делалась: у памяти нет двери уборки строки по метке — проверка записи перенесена в 205-10 и 205-9. Итог
`completed-steps/205-2.md`. ▶ **205-3** — чужой ключ получает подсказку ближайшего ключа памяти; `features` у `recall`.
✏️ 205-3 написан (не закоммичен): `suggestNearest()` в `lib/features-discover.mjs` (ближайшие ключи памяти поиском по словам ключа, без
модели; зовут `discover()` и запись `verbs.mjs:295`), `recall` читает `features` и передаёт в `discover`, договор 2.12.0, прибор
`scripts/probe/features-given-205-3.mjs` (только сервер; живое ЧТЕНИЕ от пустого `probe-205-3`, записи нет). Квота названа владельцу:
последний отказ 00:45 UTC, после — 3 вызова без отказа; прибор делает 1 вызов модели. ✗ Поймано до запуска: `verbs.mjs` не импортировал
`suggestNearest` — дописано. Проверка «ключ памяти первым» ослаблена до «в первых трёх», место называется числом.
🔎 Доставлено `9f2da4a` (от `460edfd`). Прибор 205-3 на сервере: **плоскость A зелёная — 14 из 14 ключей чата получили ключ памяти
ПЕРВЫМ**, негатив чист. ✗ **Плоскость B — два дефекта:** `recall` с `features: [money.spent-on-a-purchase]` → `used_model: true` (подбор
не пропущен); `recall` с ключом чата — подсказки в ответе нет (`meant.rejected` не выносится в ответ). ▶ Разбор `recall` (`verbs.mjs`
729–1000) и `readParams`.
🔎 **Диагноз:** ① в трёх ответах `recall` после подбора (`verbs.mjs` ~903, ~943, ~976) `used_model: true` было ВПИСАНО — подбор с
присланным признаком модель не звал, а ответ говорил «звалась» (исправлено: `meant.used_model === true`); ② `meant.rejected` не выносится
ни в один ответ `recall` — подсказка теряется (правка после проверки схемы ответа `lib/output-schema.mjs`). `readParams` принимает
`features` верно.
✅ **205-3 закрыт** `5636210` (доставлено): прибор 205-3 на сервере «всё сошлось» — 14 из 14 ключей чата получают ключ памяти ПЕРВЫМ;
`recall` с признаком `used_model false`, с ключом чата — подсказка в ответе, негатив без признака `true`. Итог `completed-steps/205-3.md`
(не закоммичен). ▶ **205-4** — правила переиспользования и заведения признаков: вопросы владельцу формой → паспорт §23.
🛑 **Ответы владельца на 205-4 — дословно в `205-main.md` §5.** Главное: **правило 1 отвергает предложение агента** — модель продолжает
заводить колонки и таблицы сама («позволяет работать с базами данных минуя граф… найди требования которые заставляли создавать колонку и
признаки по которым создавалось вместо колонки новые таблицы это критически важное правило… записи были абсолютно разнородные… думай об этом
с максимальной ответственностью»); дубли — отдельная некритичная задача разнородности. 2 — агент разработки шагом; 3 — разбор по
кандидатам; 4 — «снят» с заменой. Отменяет: «закрыть путь мимо реестра» в 205-10 и «12 родов в граф» в переписи §9. Правила живы и записаны:
паспорт §8, §21.2, навык `use-tables`. ▶ проверка механизма колонка/таблица по коду (`verbs.mjs`, `lib/promote.mjs`) → §23 → переписать 205-10.
🔎 Механизм жив: рода нет → `addColumn(ROOT, kind)` (+3 колонки: значение, claim, basis; `lib/store.mjs:178`); род стал таблицей →
`appendToTable`; таблица рода рождается `promote` с переездом первого значения (`came_from_column`, `lib/promote.mjs:70`). ▶ дочитать
цикл записи (`verbs.mjs` 461–570) и **ворота `lib/write-gate.mjs` (201-9) — не запрещают ли они новые роды вне реестра**.
🛑 **Попутно: прибор 205-3 оставил пустую строку `probe-205-3` в корневой таблице** — `recall` зовёт `rowOf`, а тот `INSERT OR IGNORE`
(`lib/store.mjs:195`); двери уборки нет; вероятно, так же родились 10 строк людей.
✅ **Проверено чтением кода: правило владельца 1 исполняется.** Ворота 201-9 (`lib/write-gate.mjs`) пускают род вне реестра в таблицу при
значении ≤ 5 слов (длиннее — рассказ, в граф); рода нет → `addColumn`; добавление ко второму значению → `promote` (таблица); исправление →
UPDATE + история; `need_table` → `promote`. Недостаёт: сверки нового рода с кандидатами (сноска *⁴) и видимого списка родов без признака.
✏️ Паспорт §23 «Рост памяти и реестр признаков» (ответы дословно, таблица правил с адресами в коде, *) · ТЗ 205-10 переписано: реестр
догоняет хранилище (21 признак + 6 снятых с заменой), значения не переносятся · перепись §9 переписывается. ▶ коммит, согласование §23.
✅ Ответ владельца, дословно в `205-main.md`: **«Утверждаю §23 и список … // Именно сейчас … фундаментальный рефакторинг паспорта … проверить
нашу основную документацию если она Верна перенести её в паспорт а затем сохранив архитектуру расширить каждый пункт техническим описанием
… это неотъемлемая часть задачи»** → подшаг **205-11** (`new-steps/205/205-11.md`). **Порядок: 205-4 закрывается → 205-11 → 205-5 → … → 205-10 → 205-9.**
✏️ Закрытие 205-4: `CLAUDE.md` управляющего — четыре правила §23 вместо «правила ещё обсуждаются»; навык `memory-development` — закон «память
растёт сама, реестр догоняет»; главная (ru/en), README, паспорт §22 п. 2.3 и сноска *⁵ — по правилу 1; перепись §9 переписана (21 признак +
6 снятых). 🔎 **Паспорт до рефакторинга:** 1211 строк · 146 КБ · надгробий 13 · строк с подсветкой 35 · 23 раздела и ~40 подразделов,
замысел вперемешку с отчётами шагов («ПОСТРОЕНО ШАГОМ 183», «ПЕРЕСТРОЕНО ШАГОМ 200»).
✅ **205-4 закрыт** (итог `completed-steps/205-4.md`; коммит и доставка — следующим действием): счёт устаревших фраз 0 в `CLAUDE.md`, README,
главной, паспорте; прибор инструкции зелёный (навык 12280/12288 после сжатия).
✅ Структура нового паспорта (`new-steps/205/205-11-structure.md`, 15 разделов, форма «что это → как устроено → решения → не построено»,
история в `PASSPORT-HISTORY.md`) — ответ владельца, дословно: **«Утверждаю структуру»**. ▶ коммит + доставка главной → **205-11**.
✅ **205-4 закрыт и доставлен** `22acb4c` (от `5636210`, `BUILD_RC=0`, порт 5 с). Живая страница: ru «колонки и таблицы: колонка под одно
значение» 2, «Реестр догоняет хранилище» 4, «Архитектору сохраняется предложение» 0; en «column-or-table rules» 2, «proposal for a new feature
is kept» 0. ▶ **205-11** — рефакторинг паспорта: чтение `BLACKBOX-API.md`, `LAWS.md`, README (хранилища → конец), разделов главной →
таблица сверки с кодом → `PASSPORT-HISTORY.md` → новый паспорт 0–14 → прибор ссылок `§N`.
🔎 **205-11 пункт 1 сделан: сверка написана** (`new-steps/205/205-11-audit.md`, чтением кода, без модели). Верно: два замка, ключ `0600` и
постоянное сравнение, `health` открыт, один вызов по 8 кандидатам (`features-index.mjs:37 K=8`), запрос без вопроса без модели, суммы кодом,
граф в режиме контекста, колонка→таблица с переездом, `said`/`guess` с основанием, стенд показывает тело запроса, кнопка ключа.
🛑 **Неверно (утверждения о построенном, кода нет):** поиск по радиусу (координаты только у строки сообщения `messages.mjs:88–121`,
вычисления расстояния в коде ноль) · охват в суммах (`read.mjs:114–131` складывает всё) · **ядро эволюции чемпион/претендент — только
в текстах главной и README** · мемоизация «0,2 с и ноль токенов» (чтение с вопросом всегда зовёт `discover`) · пять уровней (`BUILT_LEVELS=3`)
· отложенные действия · сборка отчёта-артефакта · `thread` у `recall` в договоре нет · консоль не 6 экранов, а 10 разделов.
🛑 **README спорит сам с собой:** верхние разделы обещают радиус, пять уровней, мемоизацию и эволюцию как работающее, а сноски \*⁶ \*⁹ \*¹⁰ \*¹¹
того же файла говорят, что этого нет. У главной механизма сноски нет вовсе — непостроенное там без пометки.
🛑 **`LAWS.md` устарел ровно в разделе «Устройство»:** `server.js`/три маршрута/ноль методов/«замок только секрет машины»/«сборки нет» — всё
неверно; законы первый…шестой верны. В паспорт переносятся законы, «Устройство» пишется заново по коду.
✅ Сверка закоммичена `dcbb163` (ветка `new-step-205`, отправлена).
✅ **205-11 построен и доставлен** `33f14dc` (от `22acb4c`, `BUILD_RC=0`, порт 6 с). **Паспорт переписан: 777 строк вместо 1211, 15 разделов
(0–14) вместо 23, надгробий 0 (было 13), подсветки `==` нет.** Форма каждого раздела одна: что это · как устроено технически · решения
владельца дословно с датой · не построено звёздочкой со сноской. **История целиком** — `development-docs/PASSPORT-HISTORY.md` (1233 строки,
прежняя редакция без потерь, своя нумерация названа в шапке); указатели — в §0 паспорта и в `CANCELLED.md`.
✅ **Ссылки `§N` перенумерованы** в коде, навыках, `CLAUDE.md`, `BLACKBOX-API.md`, `AGI-CONFIG`. Прибор
`scripts/probe/passport-refs-205-11.mjs`: 73 законных · 10 исключений на чужие документы с причиной · **0 висящих** · негатив «§99» ловится;
себя прибор не считает (его текст несёт и «§21.1» как пример, и «§99»).
✅ Плоскость 2 на сервере: страница `/{ru,en}/passport` — новые разделы по 2 совпадения, указатель на историю 3, **негативы: надгробий 0,
«Пять уровней глубины» 0, «Схема в центре» 0**; файл истории на сервере 147 065 байт; `DELIVERED_COMMIT=33f14dc`. `npm run check` — договор
2.12.0, методов 8, реестр 24; `npx tsc --noEmit` чист.
🔎 **Попутная находка:** комментарий `contract.mjs` говорил «МЕТОДЫ — ПЯТЬ» при восьми — стоял с 194-15, а сторож всё это время печатал
«методов 8». Исправлено вместе с перечислением (пятый случай рукописного числа в проекте).
🔎 В `/code` правлен `scripts/deliver-memory.sh`: `PASSPORT-HISTORY.md` едет на сервер вместе с паспортом — иначе указатель §0 на сервере
вёл бы в никуда.
✅ **205-12 построен и доставлен** `8f5aedf` (от `1d69dec`, `BUILD_RC=0`, порт 3 с). Слово владельца: **«правь публичные тексты — README и
главную, readme use english»** (исход A маршрутизации: тот же предмет, что сверка 205-11). Правлены **README** (английский), **главная
ru+en**, и попутно найденные **`capabilities.json`** (`radius_search: true`, `ab_split_testing: true`, пять уровней) и **`llms.txt`**.
🔧 У раздела главной появилось поле `building` и блок под лидом (`_components/landing.tsx`): пометка стоит **там, где читают обещание**, а не
сноской внизу страницы, и говорит ДВЕ вещи — что есть сегодня и что строится. Одно «в разработке» без «что есть» читается как «не работает
ничего».
✅ Плоскость 1 (счёт по исходникам): все ложные строки сверки — 0 совпадений в README и на главной; **негатив** — живая строка «one short
model call» даёт 5 и 3. Плоскость 2 (живая страница): `ru` 17 пометок, `en` 27, негативы по нулям (радиус, «0,2 с», «самоэволюционирующее»),
позитив 12 и 14; `llms.txt` несёт пометки. `tsc` чист, `npm run check` зелёный, прибор ссылок `§N` — 0 висящих.
🛑 **Честно о порядке: ТЗ 205-12 написано ПОСЛЕ работы** — указание владельца было прямым, предмет готов из сверки; записано в самом
`205-12.md`, а не умолчано.
✅ **205-11 и 205-12 ЗАКРЫТЫ** словом владельца: **«принято, закрывай оба итогами и иди дальше по 205-5»**. Итоги —
`completed-steps/205-11.md` и `205-12.md`. ✅ **205-5 ЗАКРЫТ И ДОСТАВЛЕН** — память `f5f5298`, слой данных `ai-workspace` `c0baacd` (файлом + `pm2 restart fractera-data`). Одна дверь
`/service/memory/*` с секретом машины, предел 300 с (предел разбора внутри памяти — 120 000 мс на вызов модели, общий 120 с обрывал бы
работу на её границе). Подключения: `lib/feature-consumers.mjs`, таблица `feature_consumers`, закрытый список элементов
`telegram-chat · guest-app · automations · memory-stand · probe`, набор заменяется **целиком**; двери `GET /v1/features/consumers?element=`
и `PUT /v1/features/consumers` стоят **до** общей ветки `/v1/features/{ключ}`; каталог называет `consumers` у каждой записи. Договор 2.13.0.
Плоскость A: с секретом 200 · без секрета 401 · несуществующая служба 404 · маршрут в живом файле 1, негатив 0. Плоскость B: прибор
`consumers-205-5.mjs` «всё сошлось» — 3 записаны и прочитаны, каталог называет элемент, чужой ключ и чужой элемент отвергнуты, набор после
отказов цел, уборка по метке. Итог — `completed-steps/205-5.md`.
✗ **Ошибка, стоящая внимания:** мой счётчик сказал «правки на сервере нет» при живом маршруте — искал `service/memory`, а в исходнике стоит
`'^\\/service\\/memory…'` (между словами ТРИ знака). **У счётчика по коду обязан быть негатив ТОЙ ЖЕ ФОРМЫ**, иначе ноль неотличим от
слепоты. Разрешено живым запросом — но только после того, как названа причина расхождения.
▶ **205-6 ПОСТРОЕН И СОБРАН НА СЕРВЕРЕ, ЖДЁТ ГЛАЗ ВЛАДЕЛЬЦА.** `ai-workspace` `848643b` (отправлено), на сервере влито `git merge --ff-only`,
`BUILD_RC=0`, панель поднялась за 2 с. Раздел `/{lang}/feature-registry` в группе «Данные»: серверное чтение каталога и подключений **через
одну дверь** (`/service/memory/*`), островок подключения (набор заменяется целиком, сказано словами), тонкая дверь панели
`api/config/feature-registry`, словарь `en`+`ru`, `check:i18n` зелёный, `tsc` чист.
✅ Достижимые доказательства сняты: каталог через ту же дверь — **24 признака, у всех 24 поле `consumers`**, список элементов приходит из
памяти (`telegram-chat · guest-app · automations · memory-stand · probe`); замки — страница `307` без сессии на обоих языках, дверь панели
`401` на `GET` и `PUT`; число признаков **не зашито** в странице (рукописных 23/24 — 0; атрибут `data-feature-key` — 1 шаблон; негатив на
выдуманный атрибут — 0); страница присутствует в сборке (`.next/server/app/[lang]/feature-registry`).
🛑 **ДВА ДОКАЗАТЕЛЬСТВА ИЗ ТЗ НЕДОСТИЖИМЫ МНЕ И ЭТО НАЗВАНО, А НЕ ПОДМЕНЕНО:** «счёт строк `data-feature-key` на ОТДАННОЙ странице» и
«сохранение сквозь дверь панели» требуют **сессии архитектора**; у меня её нет, а включать `shouldBypassAuth` на боевой машине значит
ослабить замок — это запрещено. Негатив `307` при этом общий для всей панели (у соседней `agentic-rag` тоже `307`): он доказывает, что замок
держит, и **не доказывает**, что моя страница существует, — это доказано присутствием в сборке.
▶ **Решает владелец:** открыть `admin.aifa.dev/ru/feature-registry` самому (это и есть согласование построенной поверхности) — или разрешить
провести замер его Chrome с сервера (`claude -p --chrome`, способность измерена шагом 195).
▶ **ВОПРОС ВЛАДЕЛЬЦА 2026-09-15, ДОСЛОВНО:** «я всё равно не могу понять, для чего мы их создали, никакой они несут пользу… перечитай ещё
раз эту последовательность и согласно этой последовательности реши, что дальше делать с реестром признаков… стандартизировать использование
элементов реестра признаков, добавления элементов реестра признаков и опиши это в главном документе и на главной странице… максимально
простой язык… а потом я ещё раз прочитаю, и после этого ты не будешь задавать никакие вопросы, а просто возьмёшь и сделаешь».
✅ **Разобрано по последовательности §4 и по случаям запросов; стандарт написан и доставлен** `2853079`. **Вывод, к которому привёл разбор:
у признака ровно четыре пользы — считать и держать актуальным · одно слово вместо вызова модели · знать, о чём переспросить человека ·
видеть, кого сломает правка. Пятой нет: признак НЕ место хранения, НЕ разрешение на запись и НЕ условие доступа к данным.** Отсюда две
правки замысла: **чтение обязано доставать всё записанное, а не только названное** (сегодня 27 родов из 46 недостижимы вопросом — паспорт
§6 \*³) и **память сама предлагает признак**, когда род перестал быть случайным (\*⁴).
✏️ Паспорт §6: «Зачем признаки существуют», «Стандарт использования» (5 правил), «Стандарт заведения» (3 ступени). Главная ru+en: раздел
«Названные вещи, которые память понимает» сразу после схемы обработки запроса — четыре пользы, четыре случая запроса, как пользуются и как
заводят, пометка «в разработке». Тот же текст — в `llms.txt`.
✅ Живое: `ru` и `en` — раздел на месте (1) и в оглавлении (ссылок 2), случаи про собаку и такси по 3 совпадения, негатив выдуманного
раздела 0; паспорт на странице несёт оба стандарта; `llms.txt` — 1. Гейты: `tsc` чист, договор 2.13.0, прибор ссылок 0 висящих.
✏️ **Раздел переписан простым языком** `f2291d8` — слово владельца: «это же издевательство какое-то, шифровка, а не текст». Термины («названные вещи», «накопление», «зовущий») заменены одним сквозным примером: 300 рублей на такси → 4200 за месяц. Живое: пример 2, сумма 2, случай про собаку 3, прежние термины 0. 🛑 В СОСЕДНИХ разделах главной слово «зовущий» ещё 9 раз — не трогал, это их текст.
▶ **Владелец читает. После его слова — строить без вопросов:** чтение по живым родам + реестру (§6 \*³) и предложение признаков (\*⁴).
▶ **Дальше по плану 205:** 205-7 → 205-8 → 205-10 → 205-9.
🪦 Прежняя строка «В работе 205-5 …» снята. Прежняя строка о чтении владельцем снята:
🪦 **Осталось до закрытия 205-11 и 205-12: владелец читает паспорт и публичные тексты** (условие «готово» — согласование построенной поверхности). Дальше по плану
205: 205-5 → … → 205-10 → 205-9. Долг вне подшага: README и главная обещают непостроенное (радиус, эволюция, мемоизация) — решение владельца.

| Шаг | Состояние | Ветка / хэш | Что осталось |
|---|---|---|---|
| **203** главная инструкция: управлять памятью (99%) + навык `memory-development` (1%) | **ПЛАН НА СОГЛАСОВАНИИ (этап 9)**: ответы на 5 вопросов получены и записаны дословно (`203-main.md` §6), подшаги 203-1…203-5 развёрнуты в файлы, закон владельца внесён в `PASSPORT.md` §0 | не заведена (будет `new-step-203` от `new-step-202`) | получить «подтверждаю» → ветка → 203-1 |
| **202** мастерская `/{lang}/build` | **закрыт** (202-1…202-9), итоги и репорт фичи в `completed-steps/`, `reports/feature-build-workshop-…` | `new-step-202` = origin (`cb73ae6`) | **НЕ влит в `main`**: стоит поверх незакрытого 201; порядок вливания 201 → 202 |
| **201** схема в центре | **приостановлен** | `new-step-201` (202 — поверх неё) | выводы владельца по отчёту `reports/live-run-thirty-writes-and-thirty-reads-over-public-api.md` (измерено 13 из 60 до квоты: 12/0/1; четыре дефекта записи исправлены `lib/write-gate.mjs`, модель на исправлении не проверена) → повторный заход 30+30 (`scripts/probe/live-201-9.mjs`, сам останавливается на квоте) → 201-8 (стенд, API, `MEMORY-AGENT.md`/навыки, **удаление `server.js` — только после правки установщика**, см. 203 §2.5) |

**`main` памяти = `455e53d`** (без 201 и 202). **Сервер:** `DELIVERED_COMMIT=baee113` (код 202 целиком), служба на `server.mjs`.
**`/code`:** последние коммиты `83021ac` (федеральный `CLAUDE.md`: мастерская и закон «служба с терминалом спит»), `db54237`
(`scripts/deliver-memory.sh` возит учёт шагов целиком) — `/code` не пушится. **`fractera-telegrambot-starter`** `main` = `990ecc4`.

### Решения владельца этой сессии, дословно (полные — ниже по файлу и в итогах 202)

- Граница доверия к присланному признаку (201-7): **«Доверять боту (Recommended)»**.
- 201-9: **«Сначала чинить, потом прогон (Recommended)»**; отчёт: **«Сохрани итоговый отчет и мы завтра подумаем какие выводы из этого нужно сделать»**.
- `server.js`: **«Удалить в 201-8 (Recommended)»** — 🛑 но установщик запускает именно его (203 §2.5), удалять только после правки установщика.
- Шаг 202: «Я не зови меня … используй самостоятельно-то решение которое считаешь лучшим»; пути — полные от корня процесса; терминал называет рабочее пространство; красная карточка свёрнута.
- Главная инструкция — дословно в `new-steps/203/203-main.md` §1.

### Дорогие факты (не мерить заново)

- Квота подписки сервера общая с ботом и кончалась ДВАЖДЫ за ночь (сброс печатается в журнале `…/projects/*/…jsonl` полем `resetsAt`); большой прогон 30+30 ≈ 70 ходов.
- Claude Code выходит за ~4,3 с после «Остановить»; доставка памяти перезапускает службу и останавливает терминал мастерской.
- У Chrome нет входа на `memory.aifa.dev` — страницы проверяются приборами с секретом машины (`/ru/build`, `/en/build` открыты секрету машины).
- `MEMORY-AGENT.md` кодом не читается; `bootstrap.sh` запускает `server.js` без сборки.
- Приборы мастерской: `scripts/probe/{build-session-202-2,build-tasks-202-3,build-page-202}.mjs` — все зелёные на `baee113`.

### Законы, оплаченные сессией (кратко)

- Негативный контроль был слеп **пять раз** (двойной счёт HTML Next · поток меню отдельным куском · `pgrep -f` находит себя · снимок причины закрытия · пауза вместо ожидания по факту) — каждый раз ловил его же негатив.
- Правило, записанное только в подсказке модели, модель нарушает — исполняет код (`write-gate.mjs`).
- Адрес для человека — полный, корень берётся у процесса, не вписывается.
- Инструкцию нельзя писать по памяти агента — сверять с кодом (`MEMORY-AGENT.md` оказался неподключённым).

### ▶ СЛЕДУЮЩЕЕ ДЕЙСТВИЕ

1. ✅ (2026-09-15) Вопросы заданы, ответы дословно в `203-main.md` §6; закон «память на запуске работает целиком · 99/1 · компактно ·
   спрашивать о результате, не об именах» — `PASSPORT.md` §0; подшаги 203-1…203-5 в файлах.
2. План 203 предъявлен — **НЕ подтверждён.** Ответ владельца 2026-09-15, дословно: «Также нужно обновить главный документацию в корне
   проекта Memory потому что мы вчера обсуждали фундаментальные изменения процесса движения запроса а главной странице отображает все ещё
   предыдущие результаты … Поэтому мне нужен предварительный шагисправь в документации абзац который называется экономический закон
   архитектуры опиши итогах работает сейчас архитектура а потом я тебе дам добро или запрет на дальнейшую разработку». Следом: «нигде в
   этой документации не описано правила переиспользования элементов реестра признаков? … я вообще не понимаю какой ты собираешься писать
   документацию и о чем?»
3. 🔎 Измерено: абзац — `app/[lang]/_i18n/landing.i18n.ts` (`ladder`, ru «Экономический закон архитектуры», en «Cost-first architecture»)
   и схема `router` там же; та же ложь в `README.md` («The deterministic router», «under 10 ms for zero tokens», «0.2 s at levels 1–3»).
   Правил переиспользования признаков нет нигде: реестр `AGI-CONFIG` (21 запись) правится только руками, ни один код его не пишет;
   фраза без признака → ответ «нет такого признака», факт остаётся только в графе; инструмент агента `make_new_kind` заводит род мимо реестра.
4. План шага 204 словами → форма: **«Да, пиши так»**. Тексты сделаны: `54576e2` (ветка `new-step-204` от `new-step-202`) — словарь главной
   en+ru (`ladder`, `router`, `problem.body`, FAQ «каждый запрос стоит токенов?», две строки сравнения, SEO-описание) и `README.md`
   (вступление, чек-лист, раздел «How a request travels», сравнение, FAQ). Счёт устаревших фраз: README 11 → 5, словарь 23 → 8 (оставшиеся —
   верные «no model at all» у запроса без вопроса, «Deterministic promotion» навыков, **заголовок первого экрана «deterministic…» — решение
   владельца**, **цикл мемоизации «0.2 s, zero tokens» — не сверен с кодом, не правился**). **НЕ доставлено** — сначала показать тексты.
5. Слово владельца 2026-09-15, дословно: «То что касается инструкции памяти давай её перепишем сейчас в общей чертах исходя из того что уже
   понятно но так как мы полностью можем закончить описание того что за касается разработки этой часть документации должна быть завершена
   полностью.» → ▶ **сейчас:** 203-1 (`CLAUDE.md` в общих чертах) и 203-2 (`memory-development` полностью) на ветке `new-step-203` от
   `new-step-204`; доставка после показа. 203-3…203-5 ждут слова.
6. ✅ (2026-09-15, коммит `c7fc4ac` на `new-step-203`; прибор `===INSTRUCTION_203_1_OK===`, негативы A/B/C зелёные) Написаны: `CLAUDE.md` управляющего (EN, 4492 байт, 10 инструментов, 7 навыков
   агента), `.claude/skills/memory-development/SKILL.md` (EN, 11766 байт), `development-docs/CANCELLED.md`; удалены `MEMORY-AGENT.md`,
   навыки `use-development-steps`/`use-testing` памяти; ссылки поправлены в `PASSPORT.md`, `LAWS.md`, `BLACKBOX-API.md`, словаре `/build`.
   **Попутный дефект:** `.claude/settings.json` разрешал управляющему 5 инструментов из 10 (`ask_graph`, `search_vectors`, `find_objects`,
   `open_object`, `keep_object` не разрешены → в `claude -p` молча не зовутся) — добавлены. Прибор `scripts/probe/instruction-203-1.mjs`:
   первая редакция была слепа к имени без подчёркивания (`answer`) — исправлено, добавлен негатив C. `tsc` RC=0.
7. ▶ (2026-09-15) Владелец: **«push to server»**. Доставка `new-step-203` (включает 204) от базы `baee113`, терминал мастерской не
   запущен. 🛑 `deliver-memory.sh` не возит удаления (`--diff-filter=ACMR`) — после доставки на сервере убираются `MEMORY-AGENT.md`,
   `.claude/skills/use-development-steps`, `.claude/skills/use-testing` скриптом `/code/scripts/rcmd-203-clean.sh`.
8. ✅ (2026-09-15) Доставлено на сервер: `DELIVERED_COMMIT=dbdaba8` (ветка `new-step-203`, включает 204), `BUILD_RC=0`, порт за 6 с, слушатель `3700` = pid pm2 `fractera-memory`. Уборка удалённого: `MEMORY-AGENT.md`, `use-development-steps`, `use-testing` — gone; навыки на сервере: describe-incoming-object memory-development use-depth-ladder use-knowledge-graph use-links use-object-store use-tables use-vector-store. Страница `memory.aifa.dev`: ru «Реестр признаков → 8 кандидатов» 2, «0 токенов, без ИИ» 0; en «The economics of the architecture» 4, «Zero tokens, no model» 0; выдуманная строка 0. **Слово владельца 2026-09-15 после доставки (дословно, суть):** «экономический закон архитектуры которая очевидно теперь должна называться по-другому есть в принципе ошибка … два глагола … Вся таблица у тебя сейчас выглядит так как будто все происходит на три модели … память получает запрос … зовущий отправил название глагола или не отправил · зовущее правила реестр признака или не отправил · Завучи отправил связь с предыдущими сообщениями или не отправил … предварительный: определить по смыслу к какому глаголу · каком реестре признаков · с какими сообщениями связан … основная фаза … 1.0 сценарий добавление записи · 2.0 сценарий извлечение из памяти … Завершающая фаза … формировании ответа. Прежде чем мы продолжим с тобой изобразить эти схемы используя создание блоков – карточек визуально соединённых линиями». Маршрутизация: исход A — подшаг шага 204 (описание пути запроса); черновик схемы — артефакт на согласование, в репозиторий после слова.
   🔎 Измерено для схемы (код `dbdaba8`): глагол всегда задаётся адресом (`/v1/remember` | `/v1/recall`), `discover().action` ничем не
   маршрутизирует · `features` есть только у `remember` (у `recall` параметра нет) · запись зовёт `think()` со ВСЕМИ родами реестра
   (`featuresByKind()`, `verbs.mjs` ~263), при присланных `features` модель не зовётся · чтение зовёт `discover()` — 8 кандидатов + один вызов
   (`verbs.mjs:850`) · связь с прежним: `remember.thread`, `recall.history`/`prior`; определения связанных сообщений по смыслу нет.
   ✗ **Доставленный текст 204 неверен в одном месте:** «8 кандидатов» написано и для записи — верно только для чтения. Правится вместе со
   схемой после слова владельца.
   Артефакт-схема https://claude.ai/artifact/7v6QHWzHaBZriSCRu9AAR2 → владелец: **«Схема мне не понравилась повтори ответы в качестве
   нумерованного списка можно использовать два или три уровня влажности.»** (вложенности) — ответ дан списком в чате.
   Затем, дословно: **«Мне не нравится твоя идея отказать записи то что не построено. Ты должен писать так как будто все построена чтобы мы
   понимали как это строить просто поставить звёздочку а внизу подпиши звёздочки комментариев в процессе разработки я хочу сейчас видеть
   реальной работающей кейс как если бы мы завершили как ты его видишь потому что если ты напишешь просто мы не построили то как же ты будешь
   это строить?»** → закон внесён в `PASSPORT.md` §0 п. 5; целевая схема дана списком в чате — на согласование, затем в паспорт.
   Владелец: **«add to memory.aifa.dev»** → ▶ целевая схема со сносками *¹…*⁷: `PASSPORT.md` §22 · главная (ключ `ladder` — фазы,
   `router` — тексты, ru+en; заголовок раздела предложен агентом: «Как память обрабатывает запрос» / «How memory handles a request») ·
   `README.md` · `lib/llms.ts` и `lib/markdown-mirror.ts` через общий `flowLines()` (✗ первый поиск читателей `ladder` смотрел только в
   `app/` — эти два нашла проверка типов). `tsc` RC=0. Доставлено `88e53cb` (от `dbdaba8`), `BUILD_RC=0`, порт за 4 с, слушатель = pid pm2.
   Доказательства: `memory.aifa.dev/ru` — «Как память обрабатывает запрос» 4, «В разработке — что есть сегодня» 2, «*⁴» 7, «Экономический закон
   архитектуры» 0; `/en` — «How memory handles a request» 4, старое название 0; выдуманная строка 0; `/ru/index.md` — пункт `2.2.1.` с отступом
   на месте (первый образец искал 3 пробела вместо 6 — ошибка образца). ✗ **`llms.txt` нёс вписанный руками заголовок `## Cost ladder`** над
   новой схемой (`lib/llms.ts:46`) — заменён на `w.ladder.title`, доставка второй раз (`65e955e`, llms.txt ru/en: новый заголовок 1, старый 0).
   ✗ **Владелец 2026-09-15, дословно:** «В добавлении записи не описано какие минус записи добавляются Кудай и в какой последовательности речь
   идёт об объектах которые проходят процесс создания Самари и кроме прочего записываются в разные таблицы и другие структуры памяти действует
   это проигнорировал проигнорировал вхождение признаков календаря и геометки, ты описал что происходит запись в строку базы данных но не описал
   при каких обстоятельствах ты добавляешь колонки а при каких обстоятельствах добавляешь новые таблицы. Там есть значения из памяти у тебя
   вообще нету пункта до обучения? У меня такое ощущение что ты вообще не понимаешь что мы делаем?» → ▶ сценарий записи и дообучение
   переписываются по паспорту §5, §8, §14, §15 и навыкам `describe-incoming-object`, `use-tables` (читать, не по памяти); показать владельцу
   до доставки.
   🔎 Измерено для переписывания: порядок `remember` — `note` (файл `journal.mjs`) → вложения/ссылки как объекты → корневая строка и
   `rowOf` → `think()` → по значению: таблица рода есть → `appendToTable`; рода нет → `addColumn`; значение есть → `UPDATE` с историей или
   `promote` → граф с указателями → `note`. Строку `messages_that_came_into_memory` пишет ТОЛЬКО путь объекта (`objects.ts:236`), фраза её не
   получает. У строки есть охват `scope_at/lat/lon/radius_m/place/source` и индекс по координатам; `scope` фразы идёт только в подсказку модели;
   чтения по координатам нет нигде. `linkMessages` зовёт только `link-ingest` (сниппет ↔ страница).
   🔎 `deny` действует только в той же нити (`verbs.mjs:279–282`, слова в подсказку), саммари не расширяется; сумма в `read.mjs:114–134`
   считается без охвата. Владелец на переписанный список: **«go to update , will reed in page»** → ▶ раздел на главной (скрипт
   `flow-204b.mjs`), паспорт §22, README: запись по порядку, цикл дообучения, сноски *⁸–*¹²; доставка.
   Владелец посреди работы, дословно: **«Возле каждого пункта(!) сериал цвета если для неё навык существуют. При наведении показывай название
   навыков. Для тех у которых не существует пусть эта иголка будет красная и при наведении можешь показать название навыка которые мы будем
   создавать»** (сериал цвета = зелёного цвета, иголка = иконка) → метка у каждого пункта и фазы: зелёная — существующие навыки в подсказке,
   красная — имя будущего навыка; карта номер→навык в `flow-204b.mjs` со сторожем «у пункта нет навыка / лишний номер».
   ✅ Доставлено `3ed179b` (от `65e955e`), `tsc` RC=0, `BUILD_RC=0`, порт за 4 с, слушатель = pid pm2. На `memory.aifa.dev`: метки по
   атрибуту `title` — ru 28 зелёных / 49 красных, en 28 / 49 (= карта из 77 пунктов); выдуманная подсказка 0; «Запись 4 — строка журнала
   входящих» и «Цикл дообучения» на месте; `llms.txt` en — 77 строк с навыком (28/49), `/ru/index.md` — 28/49.
   Владелец: «go» → форма выбора, ответ дословно: **«Закончить шаг 203 (Recommended)»** (203-3 → 203-4 → 203-5, итоги и закрытие 203 и 204).
   ▶ 203-3: разведка запуска `claude` в терминале `/build` и флагов установленной версии на сервере.
   🔎 `server.mjs:270` — `claude --settings .claude/settings.build.json` набирается в оболочку; на сервере `claude` 2.1.272,
   `--append-system-prompt` есть. Правка: `BUILDER_START` (открыть `memory-development`, затем `current-steps.md` и `pre-steps`) +
   проверка аргументов живого процесса в приборе 202-2. `CONTRIBUTING.md` в корне нет — пункт плана 203-3 отпал.
   🛑 **Для 203-4:** ветка по умолчанию на GitHub — `main` = `455e53d` (без 201–204); установщик клонирует её. «Та же память, что на
   тест-сервере» требует вливания 201 → 202 → 203 в `main` — решение владельца (201 приостановлен).
   Доставлено `7bea5e8` (от `3ed179b`), `BUILD_RC=0`, порт за 4 с, терминал мастерской не шёл. Квота перед живым прогоном (названа
   владельцу): последний отказ `five_hour` 2026-09-15 00:45 UTC, сброс 03:50 UTC, отказов после нет; точный остаток журналы не несут.
   ▶ Прибор 202-2 на сервере → один `claude -p` строителя (Sonnet, ≤6 ходов) из `/opt/fractera/memory`.
   ✅ **203-3 доказан:** прибор 202-2 — «строитель несёт указание навыка: true», негатив 0; живой строитель — `tool#0 Skill
   memory-development`, `tool#1 Read current-steps.md`, ноль `Edit`/`Write` (предел 6 ходов). Итоги `completed-steps/203-1.md`,
   `203-2.md`, `203-3.md` написаны (203-1: живая сессия управляющего перенесена в 203-5; 203-2: страница заменена списком папки).
   Владелец спросил о навыках истории и дообучения — ответ: `link-related-messages` и `fold-back-learning-loop` не существуют (красные).
   ▶ 203-4 ждёт решения о вливании веток в `main` (форма выбора).
   Ответ владельца, дословно: **«Влить всё в main сейчас (Recommended)»**. ✅ `main` перемотан на `new-step-203` = `5e5f452` (201, 202, 204 —
   предки 203), отправлен, `main == origin/main`. Шаг 201 в учёте остаётся открытым.
   🔎 `pnpm-workspace.yaml` памяти несёт заготовку `allowBuilds: sharp: set this to true or false` — доставка `pnpm install` не зовёт, чистая
   установка может на ней споткнуться; проверяется чистым клоном 203-4. ▶ правка блока памяти в `fractera-easy-starter/lib/bootstrap.sh`.
   ✏️ Блок памяти установщика переписан (НЕ отправлен): клон `--branch main` · `memory_deps` (`pnpm install --frozen-lockfile`) ·
   `memory_build` (`pnpm build`) · `memory_start` на `server.mjs`, ожидание до 60 проверок. 🛑 FES отправляется ТОЛЬКО после зелёного чистого
   клона: отправка сразу даёт установщик новым серверам. ▶ Запущен в фоне на сервере `/tmp/memory-203-4-run.sh` (журнал
   `/tmp/memory-203-4.log`): клон `main` в `/tmp/memory-203-4`, установка, сборка под `flock`, pm2 `fractera-memory-203-4` на 3799, негатив
   `server.js` на 3798, уборка своего. Живая `/opt/fractera/memory` не трогается.
   ✅ **Чистый клон зелёный** (2026-09-15 14:41–14:43 UTC): `CLONE_RC=0 head=5e5f452` · `INSTALL_RC=0` (8,8 с; заготовка `allowBuilds` не
   мешает) · `BUILD_RC=0` · `HEALTH_UP=after 4 s`, health 200 · `/ru/build` 200, `data-build-workspace` 1 · **негатив** `server.js`: health 200,
   `/ru/build` 404, атрибут 0 · живая 3700 — 200 · временная папка удалена. ✗ негативный `server.js` пережил `kill` и держал 3798 — снят
   отдельным заходом по своей командной строке. FES отправляется; федеральные `CLAUDE.md` и `ARCHITECTURE.md` §3.6а поправлены, строка в
   `/code/development-docs/BACKLOG.md` (учёт уезжает клиенту). **Не проверено:** живая сессия `claude` с новым `CLAUDE.md` (квота). ▶ Ждём слова владельца по 203-3…203-5.


## ▶ ШАГ 200 ПЛАНИРУЕТСЯ — стенд «Тест памяти» поверх публичного API (2026-09-14, поздний вечер)

🔒 **Слово владельца, дословно:** «https://memory.aifa.dev/ru/settings?section=memory-test this page is dropted draft , why? it most important
page , which for test must have API request , but not direct call as another paged. finish it for all items and methods, using api only , after
you must to do test for all ui items for all existing api with report /plan». → отменяет прежнее «главный стенд теста памяти пока не трогаем» и
«точку в разработке памяти» — прямым словом. План: `new-steps/200/200-main.md` + `200-1 … 200-6`; этап 9 — до кода.
🔎 **Измерено чтением и экраном:** дверь `api/fractera/memory-test` зовёт исполнителей НАПРЯМУЮ (`lib/verbs.mjs`), минуя `/v1/*` — замок ключом,
проверка обязательных и отказы договора стендом не проходятся; стенд знает 5 методов из 8 (нет `keep_object`, `find_objects`, `open_object`) и не
знает `GET /v1/tables`, `/v1/tables/{имя}`, `/v1/objects/{id}/file`, `health`, `contract`; ряд «Загрузка» — 5 отключённых кнопок с ложной меткой
«не доезжает» (вложения работают с 194); органы написаны руками под два глагола.

✅ **Этап 9 пройден — ответы владельца из формы выбора, дословно (2026-09-14):**
«Подтверждаете план шага 200?» → **«Подтверждаю»** · «Каким адресом стенд зовёт API?» → **«Публичный домен»** ·
«forget_journal стирает журнал памяти целиком. Как его проверять в 200-5?» → **«Только подтверждение»** (в прогоне не отправлять; проверяется
экран подтверждения). Прошлая сессия оборвалась пределом окна сразу после ответов; работа идёт с 200-1 на ветке `new-step-200`.

🔎 **200-1, замер сервера до кода (2026-09-14):** публичный домен с самой машины отвечает — `https://memory.aifa.dev/v1/health` → `200`
за 67 мс через `213.199.61.7`; без ключа `people` → `401`, с ключом `tables` → `200`. **Таблицы стенда мертвы с 178-1:** журнал nginx —
14 × `GET /api/fractera/memory-test?what=tables` → `405` (у двери никогда не было `GET`). Предел тела nginx `200m`, `proxy_read_timeout 86400`.
Публичный IP стоит на `eth0`, домен разрешается в него → **ключ уходит только адресу этой машины** (защита от подделанного `Host`).
Доставлено на сервер `48b775e`. Ветка `new-step-200`, план закоммичен `9249485`.

🚚 **200-1 код и доставка (2026-09-14):** дверь `app/api/fractera/memory-test/route.ts` — проводник в публичный `/v1/*` (ключ маской на экране,
только адресу этой машины, без перенаправлений; `{ method, body }` и `{ get }`; `GET ?what=tables|table`). `tsc` 0 ошибок; импортов ядра в двери 0.
Коммит `84aa4a3` на `new-step-200`, доставлен `deliver-memory.sh 48b775e`: BUILD_RC=0, порт через 4 с. **Следующее:** доказательства A/B из Chrome владельца
+ журнал nginx, итог `completed-steps/200-1.md`.

✅ **200-1 ЗАКРЫТ (2026-09-14):** итог `completed-steps/200-1.md`. A — вкладка владельца + журнал nginx парами (браузер → дверь, `213.199.61.7` →
`/v1/people|contract|tables` 200); B — `remember` без `text` → `400 missing-params` договора, неизвестный → `501 not-built`, без куки `401`, полный ключ в ответах 0.
🛑 **Ждёт слова владельца по закону согласования поверхности:** видимое изменение одно — раздел «Что память построила» теперь наполняется.
**Следующее — 200-2** (экран из договора); попутно: `/v1/tables` отдаёт `records`, клиент читает `rows`.

▶ **200-2 ОТКРЫТ словом владельца «go» (2026-09-14)** — после показа 200-1; экран стенда из договора.

🧱 **200-2 код написан (2026-09-14):** `lib/contract-call.mjs` (сборка вызова из договора) · `memory-call-form.client.tsx` (форма по типам параметров) · стенд с четвёртым входом «Метод договора» и панелью адрес+заголовки+тело · таблицы по настоящей форме договора (`records`, `kinds`, `parent` — строк договор не отдаёт; прежний экран ждал `columns/rows`) · `page.tsx` порождает цели из `METHODS`+`CATALOGUE` · слова en/ru. `tsc` 0. Дальше — доставка и доказательства.

✅ **200-2 ЗАКРЫТ (2026-09-14):** итог `completed-steps/200-2.md`, код `c2ef8d3` доставлен. A — живой экран владельца: 13/13 целей и 35/35 полей = `GET /v1/contract`; таблицы — 4 карточки с `records`/`kinds`. B — полей не из договора 0; имён методов в форме 0. 🛑 Назначенный негатив «девятый метод в договоре» заменён (две пересборки и чужой метод в публичном договоре) — названо в итоге. 🛑 **Ждёт взгляда владельца:** вход «Метод договора» и раздел таблиц. **Следующее — 200-3** (файл и адрес для `keep_object`, `media` списком, убрать кнопки «Загрузка»).

🔒 **РЕШЕНИЕ ВЛАДЕЛЬЦА ПО ВХОДАМ СТЕНДА (2026-09-14), из формы выбора дословно:** «Убрать «Сказать» и «Спросить»» — остаются «Метод договора» и «Сырой вызов» (последний — для отказов договора `not-built`, `bad-json`). Вопрос владельца: «is it «Сказать», «Спросить» и «Сырой вызов» for remove ?». Исход ворот A — та же способность стенда → входит в **200-3** (органы быстрых входов и ряд «Загрузка» уходят вместе).

🔧 **ПОПУТНО ПО СЛОВУ ВЛАДЕЛЬЦА (2026-09-14): метка языка GitHub.** Вопрос: «why gitHub add for it label HTML but need Typescript , how change ?». Замер API: из 18 репозиториев Fractera HTML только у `fractera-memory-starter` — HTML 50.8% · TypeScript 28.9% · JavaScript 19.8%; причина — три отчёта прогонов навыков `review.html` в `development-docs/instruments/*-eval-runs/` (1,94 МБ против 1,14 МБ TS). Совет внешней модели `*.html linguist-documentation` отвергнут как широкий. Сделано: `.gitattributes` — `development-docs/instruments/**/*.html linguist-generated=true`, прямо в `main` `58d2764` (метка считается по `main`), влито в `new-step-200` `92d02cc`; `git check-attr` — отчёт `true`, код `unspecified`. Пересчёт GitHub — проверить позже.

✅ **Метка языка GitHub проверена первоисточником (API, 18:11 UTC, через 23 с после отправки):** `language=TypeScript`, раскладка TypeScript 58.7% · JavaScript 40.2% · CSS 0.9% — HTML из статистики ушёл. До правки: `language=HTML`, HTML 50.8%.

🧹 **СЫРЬЁ ПРОГОНОВ SKILL-CREATOR УДАЛЕНО (2026-09-14) — решение владельца, форма выбора: «Удалить всё».** Вопрос: «development-docs/instruments/…-eval-runs/: is it all trash ?».
Замер до решения: три папки `189-10-skills-eval-runs` · `192-5-object-skill-eval-runs` · `193-3-tables-skill-eval-runs` — 144 файла, ~2,24 МБ, плюс `193-4-trigger-eval.json`;
ни код, ни навыки, ни сервер на них не ссылаются; выводы — в сводках `189-10-skills-eval.md`, `192-5-object-skill-eval.md` и итогах `192-5`, `193-3`, `193-4`.
Сделано в `main` `ce31d55`: удалены папки и файл, `.gitattributes` снят той же правкой (его причина ушла), в двух сводках — надгробие пересказом с адресом
восстановления. 🛑 Итоги шагов `192-5.md`, `193-3.md`, `193-4.md` упоминают удалённое и НЕ правились — закрытия не редактируются; восстановление — `git log --diff-filter=D`.

🐞 **ДЕФЕКТ ВИДА, НАЙДЕН ВЛАДЕЛЬЦЕМ (2026-09-14):** «i can not see light green text on white bg» · «check two selectors». Раскрытые списки формы договора
(выбор метода, `lang`, `depth`) — светлые пункты на белом окне браузера: `bg-transparent` без цвета текста в тёмной теме. Лечение — `selectClass`
в `memory-call-form.client.tsx`: `bg-background text-foreground` у списка и у `option`/`optgroup`, `dark:[color-scheme:dark]`. Входит в 200-2 как его дефект.

✅ **Дефект списков закрыт (`95c15ff`, доставлен):** вычисленные стили на странице владельца — у выбора метода, `lang`, их `option`/`optgroup` фон `rgb(10,20,16)`, текст `rgb(220,252,231)`, контраст 17.07, `color-scheme: dark`; негатив — список старого вида в той же странице: фон пункта `rgba(0,0,0,0)`. 🛑 Раскрытое окно списка на снимок не попадает — глаза владельца. Метка GitHub после удаления сырья: TypeScript 59.6%. **Слово владельца «after go back to base plan» → идёт 200-3.**

🛑🛑 **ШАГ 200 ОСТАНОВЛЕН ВЛАДЕЛЬЦЕМ (2026-09-14, поздний вечер) — «fatal error».** Слова владельца дословно (голосом):
«На вкладке тест памяти мы же не тестируем какие-либо другие методы кроме двух: сказать и спросить? Так почему тогда внутренние методы определяют этот тестирование? Ты на мой взгляд творишь какую-то полную ерунду: ты же в документации писал что все что умеют чёрный ящик это только две вещи сказать и спросить только так к нему можно обратиться всё остальное это включение в эти два типа? Ты получаешь вместе с текстом по API дополнительные сущности и анализирующих? Память не должна была никогда ь отдельные опи которые выходят наружу просто чтобы загрузить неё что-либо и так далее натворил столько инструментов? И самое главное что я не могу понять как давно ты все сломал ты хотя бы вообще понимаешь что я имею в виду? Вернись на вкладку например сказать и посмотри расширенные параметры? Мы же планировали что каждый запрос может иметь или не иметь эти дополнительные расширенные параметры чтобы передать запрос? А ты что решил сделать 2000 разных запросов но зачем я в шоке ты похоже решил просто разрушить наш проект полностью?»
✗ **Ошибка агента, названная прямо:** 200-2 построил стенд как «все 8 методов договора» вместо двух глаголов с расширенными параметрами; вопрос «убрать Сказать/Спросить» был задан из ЛОЖНОЙ посылки «Метод договора умеет всё» — ответ владельца на него **недействителен и не исполняется**.
🔎 **Замер:** на сервере `95c15ff` — «Сказать»/«Спросить» с расширенными параметрами живы; лишнее — вход «Метод договора» (200-2). Незакоммиченная правка 200-3 (удаление Сказать/Спросить, файл multipart, предел Next 200mb) — в `git stash@{0}` «200-3 ОТМЕНЕНО владельцем — не исполнять», не доставлялась.
🔎 **Когда договор вышел за два глагола** (`git log -S` по `contract.mjs`): `remember`/`recall` 09-09 (175-2) · `people` 09-09 (175) · `journal`/`forget_journal` 09-10 (177) · `keep_object` 09-13 (194-15) · `find_objects`/`open_object` 09-13 (194-17). Документы при этом говорят «два глагола наружу» (`ARCHITECTURE.md:29`, `PASSPORT.md:734`) — код и замысел разошлись.
**Ничего не строить до решения владельца.**

🗑️ **ПЛАН ШАГА 200 УДАЛЁН ПО СЛОВУ ВЛАДЕЛЬЦА (2026-09-14):** «Запускай процесс который удалит весь твой план и повтори весь план заново». Прежний план восстанавливается `git show 9249485:development-docs/development-steps/new-steps/200/200-main.md` (и `200-1…200-6`). Итоги `completed-steps/200-1.md`, `200-2.md` остаются историей; код 200-2 (`c2ef8d3`, вход «Метод договора») на сервере жив, откат — в новом плане.
🔎 **Факт для развилки, найденный чтением итога 194:** методы объектов наружу введены словом владельца 2026-09-13 — «Делай полный вариант: методы объектов в API», план 194-15…194-18 — «Подтверждаю». Сегодня владелец: «Память не должна была никогда … отдельные опи которые выходят наружу». Противоречие называется владельцу, агент его не решает; в план стенда не входит.
▶ **Идёт перепланирование по навыку, этапы 1–9.**

✅ **ЭТАП 9 НОВОГО ПЛАНА — ОТВЕТЫ ВЛАДЕЛЬЦА ДОСЛОВНО (2026-09-14):**
1. План 200-3…200-7 → «Подтверждаю».
2. «Сырой вызов» → «Убрать».
3. Вложения внутри «Сказать» → «Не понял вопрос по этому просто говорю что мы разработали определённый виды вложений это просто перечислять такой же количество кнопок кнопку загрузить аудио кнопку загрузить видео кнопка загрузить и так далее и отдельно рассылки обычная ссылка YouTube ссылка сделай валидацию если в обычную ссылку ставит ссылку от YouTube а в YouTube ставит обычную ссылку чтобы ты не проходила».
4. Методы наружу → «Если ты спрашиваешь что сейчас должна отвечать память наружи? Только объект который включает в себя текст и и идентификаторы объектов которые любой другой инструмент сможет сам достать из data Ford. Мы в будущем Data Port сделаем API которая будет принимать идентификатор и возвращать объект. Поэтому в тексте просто делаешь перечисления всех фактов с которыми ты столкнулся, а вот схема предстоит тебе разработать потому что все что мы засунули внутрь должно быть так же типизированы в этом объекте и во сколько я понимаю наша PI уже должен был уметь описывать формат выхода поэтому я не понимаю что ты у меня спрашиваешь».
🛑 **ПРОТИВОРЕЧИЕ, НАЗВАННОЕ АГЕНТОМ:** план говорил «договор не меняется», ответ 3 требует, чтобы «Сказать» принимал файлы по родам и ссылки двух родов (сегодня `remember.media` — только адреса файлов, страница → `is-a-page`), ответ 4 — типизированную схему ответа с идентификаторами объектов. **200-4…200-6 переписываются**; разведка перед этим.

✅ **ПЕРЕПИСАННЫЙ ПЛАН ШАГА 200 ПОДТВЕРЖДЁН ВЛАДЕЛЬЦЕМ (2026-09-14): «Подтверждаю».** План — `new-steps/200/200-main.md` + `200-3…200-8`, коммит `1f83dd5`.
▶ **СЛЕДУЮЩЕЕ ДЕЙСТВИЕ ОДНОЙ ФРАЗОЙ:** начать **200-3** — на ветке `new-step-200` убрать из стенда «Метод договора», форму по 8 методам и «Сырой вызов» (удалить `memory-call-form.client.tsx`, `lib/contract-call.mjs`, `callTargets()` в `page.tsx`, пропы `targets`, слова `method/methodHint/missingTitle/badTitle/call/raw*`), оставить «Сказать»/«Спросить» с органами управления и панель адрес+заголовки+тело; `tsc`, доставка от `95c15ff`, доказательства по `200-3.md`, показ владельцу.
🛑 **НЕ применять `git stash@{0}`** — это отменённая правка прежнего плана. На сервере сейчас `95c15ff` (200-2: «Сказать»/«Спросить» живы + лишний «Метод договора»).
🔒 Ветки: `new-step-200` = origin `1f83dd5`+запись; `main` = origin `ce31d55` (сырьё прогонов удалено, `.gitattributes` снят, метка GitHub TypeScript 59.6%).

🧱 **200-3 код (2026-09-14, слово владельца «go»):** стенд — только «Сказать»/«Спросить» с органами управления; удалены `memory-call-form.client.tsx`, `lib/contract-call.mjs`, `callTargets()`, режимы `method`/`raw` и их слова en/ru; лид — о двух глаголах; список «от чьего имени» получил фон и текст токенами (тот же дефект, что у двух списков владельца). 19 якорей OK · имён удалённого в дереве 0 (в HEAD до правки — не 0) · `tsc` 0. **Идёт доставка от `95c15ff`** с удалением двух файлов на сервере.

✅ **200-3 ЗАКРЫТ (2026-09-14):** итог `completed-steps/200-3.md`, код `77e445f` доставлен. A — Chrome владельца: в видимой копии входы «Сказать»/«Спросить»; «Спросить» → лента `POST https://memory.aifa.dev/v1/recall` 200 за 91 мс; nginx 21:03:54 — пара браузер→дверь и `213.199.61.7 POST /v1/recall 200`. B — выбора метода и полей сырого вызова на странице 0; имён удалённого в коде 0 (в HEAD до правки 32); `tsc` 0. Попутно: на странице две копии стенда, одна скрыта — происхождение не выяснялось. 🛑 **Ждёт взгляда владельца.** **Следующее — 200-4** (каждый расширенный параметр доезжает до своего глагола, убрать ряд «Загрузка»).

▶ **200-3 ПРИНЯТ ВЛАДЕЛЬЦЕМ: «correct , go to next step» (2026-09-14). 200-4 ОТКРЫТ.** Найдено чтением до кода: `lib/bench-call.mjs` у чужого глагола подставляет `null` для `depth` (у «Сказать»), `deny` и `need_table` (у «Спросить») — выставленное значение исчезает молча, а не называется «не доезжает».

🧱 **200-4 код (2026-09-14):** `lib/bench-call.mjs` — исход значения решает только договор (снятые условия режима; стандартная глубина у записи не шумит); ряд «Загрузка» и слова `upload` en/ru удалены; «девять органов» → «восемь» в двух комментариях. Прибор `scripts/probe/bench-call.mjs` — «каждый орган × каждый глагол»: ДО правки 5 провалов (`remember·depth`, `recall·deny`, `recall·need_table` — ИСЧЕЗ МОЛЧА), ПОСЛЕ 48/48. `tsc` 0. **Идёт доставка от `77e445f`.**

🔎 **200-4 доставлен `9330b09` (BUILD_RC=0, порт 3 с); плоскость B частично:** Chrome владельца, «Спросить» с «Глубокий» + цепочкой → тело `{depth:"deep", want_chain:true}`, ответ памяти `params`: `depth accepted`, `want_chain accepted`, `depth_asked: deep`, `chain` есть; кнопок «Загрузка» 0; nginx 21:12:49 и 21:13:17 — пары браузер→дверь и `213.199.61.7 POST /v1/recall 200`. ✗ Ввод «Мадрид»/отрицания скриптом (`value`+`input`, затем `execCommand insertText`) до состояния React НЕ дошёл — поле показывает текст, тело без `scope`, «не доезжает» пусто; негатив `deny` ещё не доказан. Дальше — ввод инструментом `form_input` расширения.

✅ **200-4 ЗАКРЫТ (2026-09-14):** итог `completed-steps/200-4.md`, код `9330b09` доставлен. A — прибор «орган × глагол»: до правки 5 провалов (ИСЧЕЗ МОЛЧА), после 48/48. B — Chrome владельца: «Спросить» с deep+цепочкой+«Мадрид»+отрицанием → тело без `deny`, строка «не принимает: deny»; ответ памяти `depth/want_chain/scope accepted`; nginx 21:15:48 пара браузер→дверь и `213.199.61.7 POST /v1/recall 200`; кнопок «Загрузка» 0. 🛑 **Ждёт взгляда владельца.** **Следующее — 200-5** (вложения в «Сказать»: кнопки по родам, обычная ссылка и YouTube с проверкой рода; договор 2.6.0).

▶ **200-5 ОТКРЫТ словом владельца «go» (2026-09-14)** после показа 200-4 (агент советовал `/clear` — владелец продолжил). Замысел: `POST /v1/remember` принимает `multipart` (часть `payload` = JSON тела, части `files`), новые `links`/`youtube` с проверкой рода, внутренняя дверь кладёт вложения готовыми путями и зовёт `remember`; на стенде кнопки по родам и два поля ссылок. Идёт разведка.

🧱 **200-5, порция А (API), код написан, не закоммичен (2026-09-14):** договор 2.6.0 — `remember` `body: json | multipart`, параметры `files`/`links`/`youtube`; `server.mjs` — форма `/v1/remember` потоком в новую дверь `app/api/fractera/remember-ingest/route.ts` (обязательное до файлов тем же `missing-params`, файлы — `ingest()`, затем `remember()`, судьбы в `objects`); `lib/link-ingest.mjs` — `ingestLinks` (род по `youtubeId`: `link-is-youtube`/`youtube-not-youtube`; иначе двери `link-test/describe` → `link-ingest` по петле той же формой, что стенд, без обложки); `lib/params.mjs` — SPEC `links`/`youtube`; `lib/verbs.mjs` — вызов рядом с `media`; `proxy.ts` — имя двери; `next.config.ts` — `proxyClientMaxBodySize: "200mb"`. `tsc` 0; `check-contract` ✓ 2.6.0; прибор `api-i18n` — 3 провала (нет переводов `files`/`links`/`youtube`) → правка словаря API идёт. Попутно: перевод `media` на вкладке API в обоих языках обещал «метода для ссылок нет» — исправляется. Порция Б (стенд) — после.

🚚 **200-5 порция А: переводы `files`/`links`/`youtube` и исправленный `media` в `api.i18n.ts` en/ru; прибор `scripts/probe/remember-attachments.mjs` написан (цена до 5 ходов модели + 1 ед. YouTube + 1 страница браузером; уборка своих id). Идёт коммит и доставка от `9330b09`, затем прогон прибора на сервере.**

🔎 **200-5 порция А доставлена `8480601`, прибор `remember-attachments` на сервере (21:30 CEST):** сервер отдаёт договор 2.6.0. ✓ форма без text → `400 missing-params`, файл не лёг · ✓ 5 судеб в `objects` · ✓ НЕГАТИВ ролик в `links` → `link-is-youtube` · ✓ НЕГАТИВ страница в `youtube` → `youtube-not-youtube` · ✓ `params` links/youtube accepted · ✓ JSON: ролик в `links` → `link-is-youtube`. 🛑 **3 провала — ПОЛОЖИТЕЛЬНЫЙ ПУТЬ НЕ ДОКАЗАН:** файл, страница и ролик — `think-quota-exhausted` «You've hit your session limit · resets 10:10pm (Europe/Berlin)». Квота подписки Claude на сервере исчерпана — **общая с ботом**, бот тоже без модели до 22:10. Уборка: новых id 0. **Дальше:** порция Б (стенд) без модели; прибор повторить после 22:10 CEST.

🧱 **200-5 порция Б (стенд) код:** `lib/kinds.mjs` (роды из `describe.mjs`, `UPLOAD_KINDS`, `acceptOf`; `code-langs.mjs` — `codeExtensions()`); `lib/bench-call.mjs` — `files`/`links`/`youtube`, `invalid` по `youtubeId`; органы — девятый ряд «Вложения» (кнопки родов, список файлов, две ссылки с проверкой рода); стенд — форма при файлах, кнопка заперта при `invalid`, судьбы вложений под ответом; дверь стенда — форма потоком в `/v1/remember` (`duplex: half`); слова en/ru. 23 якоря OK · прибор `bench-call` 54/54 · `tsc` 0 · `api-i18n` и `check-contract` зелёные. **Идёт доставка от `8480601`.** Квота подписки — сброс в 22:10 CEST.

⏸ **200-5 ОСТАНОВЛЕН ПО КВОТЕ (2026-09-14, ~21:40 CEST) — ТОЧНОЕ СОСТОЯНИЕ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ:**
- **Код 200-5 целиком на сервере:** порция А (API) `8480601`, порция Б (стенд) `15d683a`; ветка `new-step-200` = origin.
- **Доказано:** прибор `remember-attachments` на сервере — форма без text → `400 missing-params`, файл не лёг; ролик в `links` → `link-is-youtube`, страница в `youtube` → `youtube-not-youtube` (форма и JSON); `params` links/youtube accepted; договор 2.6.0 на сервере. Прибор `bench-call` 54/54. Chrome владельца: у «Сказать» шесть кнопок загрузки из `kinds.mjs` (аудио · изображение · PDF · документ · видео · код, у каждой свой `accept`), файл через поле «документ» выбран, панель — `POST https://memory.aifa.dev/v1/remember` · `content-type: multipart/form-data` · `files: probe-200-5-stand.md · 50 B`; поля «Обычная ссылка»/«Ссылка YouTube» на месте; старых кнопок «Загрузка» 0.
- **НЕ доказано:** (1) ПОЛОЖИТЕЛЬНЫЙ путь — файл, страница, ролик действительно легли: в прогоне `think-quota-exhausted` (квота подписки Claude НА СЕРВЕРЕ, общая с ботом, сброс 22:10 CEST); (2) на экране — подсветка ссылки не своего рода и запертая кнопка; (3) отправка формы через дверь стенда → `/v1/remember` (пара в журнале nginx) и список судеб под ответом.
- 🛑 **Квота АККАУНТА владельца:** инструмент `find` расширения Chrome → `429 rate_limit_error` «Limit reached», пятичасовое окно 99 %, сброс 2026-09-14T22:00Z (00:00 CEST); недельное 58 %.
- ▶ **СЛЕДУЮЩЕЕ ДЕЙСТВИЕ:** после сбросов — (а) на сервере `timeout 540 node scripts/probe/remember-attachments.mjs` из `/opt/fractera/memory` (цена до 5 ходов модели); (б) в Chrome владельца: `form_input` фразы и ролика в «Обычная ссылка 1» → строка `invalid-links` и кнопка «Отправить» disabled; затем правильная ссылка + файл → «Отправить» → под ответом `attachment-fates`, в nginx пара браузер→дверь и `213.199.61.7 POST /v1/remember`; (в) итог `completed-steps/200-5.md`, показ владельцу.
- 🔒 Приёмы прибора в странице: видимая копия стенда (`getClientRects`), уступка хода `MessageChannel`, текст — только `form_input` (скрипт до React не доходит; повтор того же значения не считается — сначала сменить), файл — `DataTransfer` + событие `change` работает.

▶ **200-5 ПРОДОЛЖЕН словом владельца «go … continue» (2026-09-14)** после `/login` — учётная запись Claude Code сменилась, лимит сессии снят. Квота подписки на СЕРВЕРЕ — отдельная, проверяется замером.

🔎 **Замер 21:45 CEST:** на сервере `DELIVERED_COMMIT=15d683a`; `claude -p --model sonnet` из пустой `/tmp` → «You've hit your session limit · resets 10:10pm (Europe/Berlin)» — квота подписки сервера ещё не сброшена. До сброса — проверки экрана без модели (подсветка рода ссылки, запертая кнопка).

✅ **200-5, экран — проверка рода ссылки ДОКАЗАНА (Chrome владельца, ~21:50 CEST):** ввод настоящими нажатиями (`computer type` — доходит до React; `find` расширения недоступен: у учётной записи расширения лимит до 00:00 CEST). Ролик в «Обычная ссылка» → красная рамка + «Это ролик YouTube — ему место в поле «Ссылка YouTube»»; страница в «Ссылка YouTube» → красная рамка + «Это не ролик YouTube — …»; под панелью `invalid-links` «Исправьте ссылки не в своём поле — до этого ничего не отправляется: …»; кнопка «Отправить» `disabled: true`. Снимок `C:/Users/Usuario/AppData/Local/Temp/claude-chrome-screenshots-ebgCSN/screenshot-1789415364554-0.jpg`. Осталось: положительный путь (прибор + отправка формы со стенда) — после сброса квоты сервера 22:10.

🔎 **Квота сервера вернулась в 21:50:32 CEST** (`claude -p` из `/tmp` → `OK`); владелец: «check now all ok». **Идёт прогон `remember-attachments` на сервере.**

✅ **200-5, положительный путь API ДОКАЗАН (прибор `remember-attachments`, сервер, ~21:52 CEST): ВСЁ ЗЕЛЁНОЕ, RC=0.** Полная форма `POST https://memory.aifa.dev/v1/remember` → 200 за 75,5 с; `objects`: файл `markdown` messageId 39 · страница MDN (`links`) messageId 40 · ролик «Me at the zoo» (`youtube`) messageId 41 · `link-is-youtube` · `youtube-not-youtube`; JSON-путь тоже отвергает ролик в `links`. Уборка сняла 3 объекта из 3. 🛑 **Долг уборки:** строки 39–41 таблицы сообщений и документы графа остались → `getSavedByUrl` для MDN и ролика теперь отвечает `existing` при удалённых объектах. **Идёт отправка со стенда в Chrome владельца.**

✅ **200-5 ЗАКРЫТ (2026-09-14, ~21:55 CEST):** итог `completed-steps/200-5.md`. A — прибор `remember-attachments` зелёный (файл messageId 39, страница 40, ролик 41, оба отказа рода, missing-params); B — Chrome владельца: подсветка рода + запертая кнопка; отправка формы со стенда → `POST /v1/remember` 200, судьбы «markdown · 42», «web · 40 · existing», «youtube · 41 · existing»; nginx 21:54:16 пара браузер→`memory-test?method=remember` и `213.199.61.7 POST /v1/remember`. Объект стенда снят. 🛑 Долг уборки: строки сообщений 39–42 и документы графа остаются → `existing` при удалённом объекте. 🛑 **Ждёт взгляда владельца.** **Следующее — 200-6** (типизированный ответ обоих глаголов: `text` фактов + `objects` с идентификаторами, схема `output` в договоре и на вкладке API).

🔒 **РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-09-14): «добавь в 200-7 и go».** Вопрос: «…на этом этапе ты начинаешь тестирование связанное с диалогом? То есть ты и добавляешь что-то и задаешь вопрос связанный с поиском? У тебя есть это в плане?» — агент: в плане нет. В `200-7.md` §6 добавлены четыре сценария «сказал → спросил → нашёл» (факт · документ-вложение · ролик · негатив «не знаю»). ▶ **200-6 ОТКРЫТ** (типизированный ответ), идёт разведка.

🧱 **200-6 код (2026-09-14):** договор 2.7.0 — `output` у `remember`/`recall` из `lib/output-schema.mjs` (zod 4.5.4 → `z.toJSONSchema`; обязательны `ok`, `what_happened`, `text`, `objects`); `lib/answer-text.mjs` — строка на факт и на объект; `remember` (`withParams`) и `recall` (`answer`, теперь async) несут `text`/`objects` всегда; **`recall` с вопросом ищет объекты по смыслу** (`find_objects`, одно встраивание, сбой — `objects_error`) — найдено разведкой: без этого сценарии 2–3 из 200-7 не могли пройти; отказы `server.mjs` и двери `remember-ingest` — с `text`/`objects`; `check-contract` — функция `problemsOf` + требование `output`; вкладка API — схема в раскрывающемся блоке; стенд — `text` над сырым ответом, у объектов id; прибор `scripts/probe/verbs-output.mjs`. Локально: `check-contract` ✓ 2.7.0, негативы схемы и сторожа отвергают, `tsc` 0, `api-i18n` и `bench-call` зелёные. **Идёт доставка от `15d683a`.**

✅ **200-6 ЗАКРЫТ (2026-09-14, ~22:15 CEST):** итог `completed-steps/200-6.md`, код `28a3c54` + `2a56840` на сервере (договор 2.7.0). A — прибор `verbs-output` 16/16 (схемы по HTTP совпадают с порождёнными, живые ответы и отказы по схеме, негативы схемы и сторожа). B — вкладка API: схема у `remember`/`recall`; стенд: «Спросить» → `text` над ответом + объект с id; nginx 22:12:21 пара.
🛑 **ОТКРЫТЫЙ ВОПРОС ВЛАДЕЛЬЦУ (до 200-7):** поиск объектов в «Спросить» НЕ учитывает `who` — на вопрос `bench-1` вернулся скан паспорта с `who: "stand"`; близость 0.307 к несвязанному вопросу. Это следствие решения агента в 200-6. Варианты: только объекты этого `who` · все объекты · убрать поиск объектов из «Спросить». **200-7 не начинать до ответа.**

🔒 **РЕШЕНИЕ ВЛАДЕЛЬЦА ПО ПОИСКУ ОБЪЕКТОВ В «СПРОСИТЬ» (2026-09-14), ДОСЛОВНО:** «Учитывать о ком вопрос критически важно для этого после подготовки ответа производить дополнительную фильтрацию и убирать ответы которые оказались не точными. При этом мы помним о том что это инструмент архитектора а не публичный инструмент все что сюда загружается и все что отсюда достается делается одним-единственным человеком. По этой причине критической проблемы нету. А для создания чата пользователями и других заданий между различными людьми во избежание проблемы о которой ты говорил мы должны будем создавать работу по другой архитектуре. Так и было задумано так и надо но об этом надо написать в документе чтобы ты не входил в заблуждение. То есть мы просто усиливаем фильтрацию и делаем ответ более качественным но не ставим перед собой задачузащиты персональных данных. При этом если очевидно что пользователь запрашивает ссылку и у нельзя показывать паспорт об этом я говорю связывает нужно параметры которые имеют смысл связывать».
→ Закон (граница «инструмент архитектора») вносится в документы СРАЗУ. → Подшаг **200-9** «фильтрация объектов в «Спросить»: who · род по намерению вопроса · отсев неточных» — идёт ДО 200-7. Разведка идёт.

🔒 **СЛОВО ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Самое главное помни обещание о том что мы обещаем большую часть данная доставать без использования искусственного интеллектом мгновенно через греп и другие твои технологии это должно быть в отчётах вот тестировании чем именно ты доставал и давать этому оценку правильно неправильно абсолютно неправильно вот эти три шкалы». → `LAWS.md` закон пятый.
🔒 **СЛОВО ВЛАДЕЛЬЦА (2026-09-14), дословно:** «go to 207 finish , create report , После того как создашь отчёт проанализируй и дай оценку всей работе памяти нужно ли запускать новый цикл изменение навыков чтобы гарантировать более высокие показатели?»
→ `LAWS.md` законы четвёртый (инструмент архитектора) и пятый (без ИИ, чем достали, три ступени) внесены. ТЗ `new-steps/200/200-9.md` (фильтрация объектов) — В ОЧЕРЕДИ после 200-7. ▶ **200-7 ОТКРЫТ**: матрица «орган × глагол» и диалог через публичный API, отчёт с «чем достали» и оценкой по трём ступеням, затем оценка всей памяти и вопрос о цикле навыков. «Спросить» проверяется БЕЗ фильтра 200-9 — дефект войдёт в отчёт оценкой.

✅ **200-7 ЗАКРЫТ (2026-09-14, ~22:55 CEST):** итог `completed-steps/200-7.md`, отчёт `reports/feature-memory-bench-two-verbs-attachments-typed-output-over-public-api.md`, журнал `instruments/200-7-bench-matrix.log.txt`. Матрица 28 строк через публичный API, человек `probe-200-7`: ручная оценка **правильно 19 · неправильно 9 · абсолютно неправильно 0** (автоматическая 27/1/0 — слишком мягкая). Запись 14/14; схема 28/28.
🛑 **ГЛАВНАЯ НАХОДКА:** механический поиск фактов сравнивает слова вопроса с АНГЛИЙСКИМИ именами родов — русский вопрос не совпадает никогда → «вот всё, что известно» (7 из 9 «неправильно»). Прочие: нет честного «не знаю»; объекты только встраиванием, без фильтра who (200-9); `depth` на фактах не действует; уборка оставляет строки сообщений; род ролика youtube/web.
🔎 **Ответ владельцу на вопрос о цикле навыков: НЕТ, не сейчас** — дефекты в коде ящика, матрица шла мимо агента; нужен шаг кода «точность без ИИ» (опоры рода на языке человека при записи, честное «не знаю», 200-9), затем та же матрица со строгим судьёй.
▶ **Следующее:** слово владельца — (а) 200-9 и шаг «точность без ИИ», или (б) закрыть шаг 200 (200-8). Остатки прогона: факты `probe-200-7`, строки 43–46, документы графа.

🔒 **СЛОВО ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Я тебе сказал запусти эту часть Тестов на английском языке дай результат на русском». → прибор `scripts/probe/recall-english-200-7.mjs`: те же вопросы поиска фактов по-английски, строгий судья, без хода модели. Идёт прогон.

🔎 **Поиск фактов по-английски (21:14 UTC): правильно 6 · неправильно 3 · абсолютно неправильно 1.** Механика точна при совпадении языка; дефекты: служебное слово `with` → уверенный чужой факт (E8), нет словоформ `meet`/`meetings` (E7), нет «не знаю». Отчёт §6, итог 200-7 дополнен.

🔒 **ВОПРОС ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Меня в механике твоих есть и мало беспокоит тот факт что модель не ответила на первый вопрос без искусственный интеллект ты черт с ним. А почему с помощью искусственного интеллект при помощи запроса к базе данных на основе анализа схемы либо получения результат меня вот это беспокоит? Почему вместо того чтобыиспользовать навык для преобразования словесного человеческого запрос в запрос на языке схема базы данных ты пошёл сразу векторные хранилище да так было?»
🔎 **Ответ измерен кодом и прогоном:** (1) в `recall` ступени 2 «модель читает схему и выбирает, откуда брать» **НЕТ В КОДЕ** — после механического промаха стоит комментарий «память НЕ зовёт модель на чтение… отдельное решение» и выдача всего; `think()` зовётся только в `remember`. (2) `lib/params.mjs` `BUILT_LEVELS = 2` с комментарием «построены первый и второй (база с моделью)» — **ложь**: во всех 12 строках «Спросить» матрицы `used_model: false`, `depth_used: 1`. (3) Факты в вектор НЕ ходили; объекты — да, ходили в вектор сразу, **решением агента в 200-6** в обход лестницы. (4) Навык `use-depth-ladder` описывает ступень 2 агенту снаружи, внутри ящика её исполнителя нет.

🔒🔒 **РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Аннулируем обещание искать без искусственного интеллекта вообще. Лишний костыль который тратит время и отвечает только на английском языке в ряде случаев мне не подходят. Мне нужна стратегия которая ставит центр работу со схемой баз данных. Для этого нужно решить как именно существующую схему попадает в модель до того как она начинает делать записи чтобы понять хватает ли колонок и таблицы? Но я категорически против того чтобы вскармливать каждый раз в модель все имеющиеся таблицы может быть и будут 1000 со временем. Мы стандартизировали при написании таблицу при помощи длинных имён так? Давай подумаем может ли первичный запрос сразу же быть переведён на английский язык, и книгу извлечены необходимые ключи согласно какому-то стандарту которой нужно добавить инструкцию. Потом по полученным ключам подобрать нужные таблицу баз данных лучше несколько, получите схемы понять какие из них максимально близко подходят для работы выполнить процедуры чтение записи мутации? Если не нашли подходящего идём в граф знаний с помощью него находим нужную информацию и из неё получаем идентификатор. Надеюсь храним идентификатор и объекта и таблиц? Или это неважно об этом речь скажи как понял по поводу перефразировать еда этому критическую оценку»
→ `LAWS.md`: первая половина закона пятого 🪦 аннулирована (отчётное требование «чем достали · три ступени» осталось). 200-9 в части «сначала без ИИ» устарел.
🔎 **Измерено для ответа:** «Сказать» отдаёт модели ВСЕ роды корневой строки человека (сейчас 14; растёт без предела) · стандарт имён — английские фразы ≥4 слов, `__` уровни (решение 2026-09-09) · указателя/индекса по именам схемы нет · в граф ложатся только ОБЪЕКТЫ (`rag_source object/<id>`), ФАКТЫ и таблицы в граф не пишутся вовсе · корень — одна таблица, род = колонка.

🔒 **ДОПОЛНЕНИЕ ВЛАДЕЛЬЦА К СТРАТЕГИИ СХЕМЫ (2026-09-14), дословно:** «Оптимизации памяти я бы пробрал эти запросы отдельно от анализа чтобы обрабатывать их без истории. То есть вопрос для обнаружения подходящей базы данных это вопрос в котором мы передаем один одиночный запрос выполняем получаем ответ и больше не хранилось в истории не возвращаемся это избыточной информации которой не помогает накоплению полезного кэш». → поиск подходящих таблиц — отдельный одиночный вызов без нити; нить разбора (кэш) — только для анализа.

🔒 **ПРЕДЛОЖЕНИЕ ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Ты написал много условий и не совсем понял ты написал список того что позволит нашу работу сделать лучше? Или ты написал шесть причин почему я делать не надо честно? Скажи честно можешь предложить более удобный правильную схему? Сразу писать в граф знаний а таблице использовать на втором уровне для жёсткого перебора и накопления фильтрации?? То есть у нас в таблице используются в качестве входного инструмента а граф в качестве дополнительного может быть граф первичный а таблицы только для таких кейсов когда: мои расходы на молоко колбасу и воду сегодня были 200 300 и 400 руб.» → агент отвечает оценкой и схемой «всё в граф, точное и счётное — дополнительно в таблицы с указателем»; решения ещё нет.

🔒 **ЗАМЫСЕЛ ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Есть ещё одна вещь которую ты вообще забыл подумать. Я её держал. Пузырь в рукаве который может быть гораздо были вредные чем полезны. Но тем не менее иди в порт который мы создали для чата и найди там то что мы называем реестр признаков? Предполагалось что на основные инструмент который будет пользоваться памятью это чат и у чата будет технология которая позволит передавать вместе с запросом те признаки которые существуют. Эти признаки динамические они будут накапливаться по мере развития проекта мы можем стартовать имея 10 признаков через полгода иметь этих 1000. Но тем не менее давай посмотрим на регистр признаков чат дефакто признаем умеет определять нужные признаки а не классифицируются несколько групп включают признаки инструментов(возможно признаки инструментов это как раз например ии браузер который не создали на прошлом сессии) и мы можем расширить API для того чтобы принимать реестр признаков и мы можем на основе этого выбирать что будет первично ещё раз повтори критически анализ того того что я говорю»
🔎 **Измерено чтением `fractera-telegrambot-starter` (`9724c93`):** реестр признаков `REGISTRY-CONFIG/registry-config.json` — 49 записей; уровни initiator 3 · material 9 · intent 8 · entity 6 · destination 3 · field 20; у записи `key` (вечный) · `valueType` (flag 21, text 21, relation 4, date 1, geo 1, list 1) · `howToFind` · `storedIn` (таблицы ЧАТА: `tgdesk_*`, `fact_person_*`) · `tags`/`triggers` (русские слова человека)/`answers` · `aggregate` (last 11, list 3) · `subject` self 14; машинный указатель `index.json`. Реестр инструментов — ОТДЕЛЬНЫЙ `TOOLS-CONFIG`, 4 системных записи (inbox-store, fact-matcher, registry-evolution, link-finder); ИИ-браузера там нет (паспорт: «Глаза агента: браузер — замысел З18, не начат»). Первичный разбор по паспорту §1 «не построен»; работает ли fact-matcher в проде — не измерено. Чат зовёт память `/v1/recall` с экрана (`lib/architect/memory-service.ts`); реестр в память не передаётся.

🔒 **СЛОВО ВЛАДЕЛЬЦА (2026-09-14), дословно — задача и опоры плана:** «Прежде самое важное что я тебе забыл сказать: реестр признаков это сырая идея которая на сегодня можно сказать ещё вообще не используются и может быть полностью записано под память потому что память сейчас самый зрелый проект. Важно понимать что на уровне Telegram бота я всё равно всегда вызываю сильную модель и этотого нужно отталкиваться. На другую немаловажно потребителем памяти может быть что угодно любой следующий сервис или даже внешний сайт по этой причине несмотря на то что что может достигать самый высокой эффективности использования памяти другие ресурсы не должны получить полностью сломанная память понимаешь. Вот исходя из этого двигайся дальше классифицируем дополнительную информацию и атака есть от неё сначала напиши свои требования, перепиши документацию и при необходимости паспорт. В паспорт чата добавь необходимые модификации которые в будущем чат во время своей разработки должно будет принимать чтобы точно синхронизироваться с памятью вопросе совместного использования реестра признаков. Есть вероятность что реестр признаков будет вообще самостоятельно сущностью не связанный с чатом и уйдёт на собственной субдомен. Но пока всё это ещё остается под вопросом и будет решаться позже. Хотя выделения реестра признаков самостоятельной субдомена позволит всем службам нативно переиспользовать реестр признаков своих целях и даже наращивать его. Но даже в этом случае остается моё первичное требования о том что внешние потребители должны найти пользу в памяти и получить её в том же самом качестве пусть дольше, пусть при большем количестве запросов тем не менее. Скажи как ты мою задачу и вот теперь уже Приступай проектированию плана /plan»
🔒 **ОПРЕДЕЛЕНИЕ ВЛАДЕЛЬЦА (2026-09-14), дословно:** «Пару слов дополнительно о том что такое реестр признаков, как бы я его описал если бы он существовал на собственном су домене: – регистр признаков это то что подозревает или о чем думает собеседник во время того когда он задаёт обращение к памяти или чат боту или любой другой службеформате добавление или извлечение информации»
→ `LAWS.md` закон шестой внесён сразу. ▶ **ШАГ 201 СПЛАНИРОВАН** — `new-steps/201/201-main.md` + `201-1…201-8`; этап 9 — предъявление владельцу. Разведка: паспорт чата §3п (решения 2026-09-08 о глубине от корня: 0–1 личная таблица, ≥2 только граф с якорем, год рождения вместо возраста) — план на них опирается.

✅ **ЭТАП 9 ШАГА 201 — ОТВЕТЫ ВЛАДЕЛЬЦА (2026-09-14), форма выбора, дословно:** план 201 — «Подтверждаю» · имя конфига реестра признаков — «AGI-CONFIG» · шаг 200 — «Сначала закрыть 200 (Recommended)» · факты чата — «Хозяин фактов — память (Recommended)». ▶ **Идёт закрытие шага 200 (200-8)**, затем 201 на ветке `new-step-201` от `main`.

✅ **ШАГ 200 ЗАКРЫТ (2026-09-14):** итоги `200-8.md`, `200-main.md`; паспорт §13, описание API, `CLAUDE.md` памяти и федеральный обновлены; план `new-steps/200/` удалён этим коммитом; ветка вливается в `main` → `old-step-200`. `git stash@{0}` (отменённая правка 200-3) не сброшен — слова владельца нет. ▶ **СЛЕДУЮЩЕЕ: шаг 201 «Схема в центре», подшаг 201-1** на ветке `new-step-201` от `main`.

🔎 **Закрытие 200 доказано:** `main` = `origin/main` = `455e53d`; на сервере `DELIVERED_COMMIT=455e53d`, в `PASSPORT.md` раздел «ПЕРЕСТРОЕНО ШАГОМ 200» — 1 (негатив «ШАГОМ 999» — 0); `git log --diff-filter=D` плана → `455e53d`, по несуществующему пути — пусто. Ветка `old-step-200`. Федеральный `/code` — `f38d075` (не пушится). ▶ **Создана ветка `new-step-201` от `main`. СЛЕДУЮЩЕЕ ДЕЙСТВИЕ: 201-1 (документы: паспорт памяти §21, описание API, раздел синхронизации в паспорте чата) — после сброса контекста.**

▶ **201-1 НАЧАТ (2026-09-14, новая сессия после `/clear`, слово владельца «go»):** ветка `new-step-201`. Пишутся: паспорт памяти §21 «Схема в центре» (+ отсылки в §3, §4, §8; снята прежняя подсветка `==` по правилу паспорта), `BLACKBOX-API.md` — планируемое снаружи, паспорт чата — новый §3р «Синхронизация с памятью» + надгробия-указатели в §3о, §3п и шапке `MEMORY-STANDARD.md`. Разведка: паспорт чата §7 запрещает агенту чата читать чужие репозитории → обязательства чата пишутся в его паспорте целиком, договор памяти он берёт из `GET /v1/contract`, а не из нашего дерева. Доставка паспорта: страница читает файл с диска → достаточно файла на сервере, сборка не нужна. Приёмная `/code/pre-steps/` — только образец формы.

✅ **201-1 ЗАКРЫТ (2026-09-14):** итог `completed-steps/201-1.md`. Память `new-step-201` = origin: `fa4c0b5` (паспорт §21, `BLACKBOX-API` §3/§4), `009e4d2` (подсветка без инлайн-разметки — иначе `==` печатается знаками, закон разметчика `passport-marks.ts`); чат `main` = origin = `990ecc4` (§3р + надгробия §3о, §3п, строка карты). Сервер: паспорт памяти доставлен файлом (`DELIVERED_COMMIT` остался `455e53d` — код не менялся), клон `/opt/fractera/telegrambot` доведён ff до `990ecc4`. Доказано: `memory.aifa.dev/ru/passport` → «Схема в центре» 3, ложная строка 0, буквенных `==🔒` 0 (было 6). 🛑 Вид подсветки глазами не проверен. ▶ **СЛЕДУЮЩЕЕ: показ §21 и §3р владельцу (порядок паспорта: читает → утверждает), затем 201-2 — замеры до постройки.**

✅ **§21 ПАСПОРТА ПАМЯТИ И §3р ПАСПОРТА ЧАТА УТВЕРЖДЕНЫ — ответ владельца из формы выбора, дословно (2026-09-14):** «Утверждаю, дальше 201-2». ▶ **СЛЕДУЮЩЕЕ ДЕЙСТВИЕ: 201-2 — замеры до постройки** (`new-steps/201/201-2.md`; цену каждого прибора и квоту подписки сервера назвать до прогона) — после сброса контекста.

▶ **201-2 ИДЁТ (2026-09-14, 22:40 UTC):** прибор `development-docs/instruments/201-2-costs.mjs` (граф: запись/индексация/чтение local·hybrid с нашими словами + негатив отсутствующей сущности; модель: opus как сегодня с MCP и сохранением + контроль `--resume`, opus/sonnet по 2 раза с `--no-session-persistence` + негатив `--resume`, opus без MCP; вектор: 1000 записей реестра — 12 настоящих + 988 соседей, 9 вопросов RU/EN + посторонний). Уборка по метке `probe-201-2` (коллекция вектора и источник графа). Прибор НЕ зовёт `think()` — тот пишет в живой журнал памяти. 🔎 Разведка: CLI 2.1.271; флаг `--no-session-persistence` ЕСТЬ; в папке разбора 185 файлов разговоров — каждый вызов памяти сегодня остаётся на диске; квота: отказ `rate_limit` в 19:30 UTC с `resetsAt` 20:10 UTC — к прогону окно открыто; вектор `text-embedding-3-large`, 3072, `partitioned`. 💰 Цена: ~7 вызовов подписки, одно извлечение графа, ~40 тыс. токенов встраивания.

🔎 **201-2, ПЕРВЫЕ ЧИСЛА (22:45 UTC):** V — встраивание 1000 записей 11,5 с (39 990 токенов), запись в склад 10,8 с; поиск в складе 13–46 мс, встраивание вопроса ~170–230 мс; нужный признак на 1-м месте в 6 из 8, на 2-м в 2 (расходы — сосед «расходы: собака» 0,462 против 0,455; Денис 0,249 против 0,252); посторонний вопрос — лучшая близость 0,241. M — opus как сегодня: 4,2 с модели / 7,9 с стены, $0,126, ответ верный (три значения 200/300/400). ⚠ **ПОПУТНАЯ НАХОДКА: в вызове модели MCP-сервер памяти `memory:failed`** — `.mcp.json` называет `scripts/agent/memory-tools.mjs` относительным путём, а `claude` запускается из `/tmp/fractera-memory-think`; причина не проверена, записана до разбора.

✅ **201-2 ЗАКРЫТ (2026-09-14, 22:50 UTC):** коммит закрытия `aedb66e` (прибор `e9e69ca`), ветка `new-step-201` = origin; итог `completed-steps/201-2.md`, отчёт `instruments/201-2-costs.md`. Выводы для постройки: кандидаты реестра — **первые 8 числом, порога нет** (верный 0,249 vs посторонний 0,241); подбор схемы — **opus с `--no-session-persistence`**, ~6–8 с стены (Sonnet 1/2 верно и не быстрее); граф — **«не знаю» не говорит** (на отсутствующее имя отдаёт 5 ближайших), существование проверять поиском метки, ключевые слова — имена, `top_k 5 / chunk_top_k 3` (60→7 тыс. знаков, имя остаётся первым); запись в граф всегда стоит извлечения моделью графа, 2–6 с до готовности; шапка `learn()` рождает служебные сущности («Прибор 201-2», «Сказать»). Уборка чистая. Федеральный `/code` `06b46ce` — ANTI-PATTERNS про MCP.
⚠ **ПОПУТНЫЕ (2026-09-14):** ① **MCP-сервер памяти в вызовах модели не стартует** — `.mcp.json` `args: ["scripts/agent/memory-tools.mjs"]` относительно cwd `/tmp/fractera-memory-think`; измерено: как сегодня `memory:failed`, абсолютным путём `memory:connected`. Греп по коду: ни один вызов `think()` инструменты MCP не разрешает (только `Read` в `describe.mjs`) — работающее не пострадало, это мёртвая настройка и ложный комментарий в `think.mjs`. **Маршрут: исход A → строка в ТЗ 201-4** (вызов подбора без MCP, комментарий к правде). ② В графе владельца метки «прибор 200-5», «прибора 200-5» — след прибора шага 200; не удалялись. ▶ **СЛЕДУЮЩЕЕ: 201-3 — реестр признаков `AGI-CONFIG`** (`new-steps/201/201-3.md`) — после сброса контекста.

✅ **201-3 ЗАКРЫТ (2026-09-14, 23:10 UTC):** реестр признаков памяти `AGI-CONFIG` — 21 запись (14 живых родов + 6 своих у чата + траты), **без `storedIn`**: род выводится из ключа (`person.city-where-he-lives-now` → `city_where_he_lives_now`). Закрытые словари (тип 8 · накопление 3 · субъект 1 · глубина 0/1 · метки 10), сторож `scripts/check-features.mjs` **внутри** `npm run build`, порождённый `index.json`, каталог `GET /v1/features` и `/v1/features/{ключ}`, договор **2.8.0** (MINOR, только каталог). Коммиты `bc5392c` и `ab0d892`, `DELIVERED_COMMIT=ab0d892`. Прибор `scripts/probe/features-201-3.mjs` на сервере: 21/21, живых родов 14 покрыто 14, 11 испорченных записей роняют сторожа поимённо, сборка на плохом реестре — код 1, имени хранилища в ответе двери 0. Решение агента вслух: у трат `sum-able`, а не `list` (иначе «сколько потратил сегодня» не выразить). Долг в самой записи: `person.age-he-is-now` против правила «год рождения».
⚠ **ПОПУТНОЕ ③ (2026-09-14): в дереве памяти ДВА маршрутизатора `/v1/*`.** Живой вход — `server.mjs` (pm2, `package.json`); `server.js` — наследие поколения без Next, его никто не запускает. Маршруты реестра сначала уехали в мёртвый файл, поймано прибором (`501 not-built` при зелёной сборке). Удаление `server.js` — **решение владельца**, не самовольная уборка. 🛑 Глазами не проверена вкладка «API» на `memory.aifa.dev` (договор её уже отдаёт: `catalogue 5`, оба адреса). ▶ **СЛЕДУЮЩЕЕ: 201-4 — подбор схемы** (`new-steps/201/201-4.md`: K=8 числом, opus с `--no-session-persistence`, вызов без MCP) — после сброса контекста.

✅ **ОТВЕТЫ ВЛАДЕЛЬЦА (2026-09-14, форма выбора, дословно):** судьба `server.js` — **«Удалить в 201-8»** (строка внесена в ТЗ 201-8 сразу, вместе с правкой `CLAUDE.md` памяти «вход один — `server.mjs`»); следующее — **«Да, 201-4»**. ▶ **ИДЁТ 201-4 — подбор схемы.**

▶ **201-4 ПОСТРОЕН И ДОКАЗАН НАПОЛОВИНУ — ПРОГОН ОБОРВАЛА КВОТА ПОДПИСКИ (2026-09-14, 23:16 UTC).** Код: `lib/features-index.mjs` (индекс реестра по смыслу, K=8 числом без порога, перестройка по отпечатку), `lib/features-discover.mjs` (фраза → признаки и значения, проверка типов, присланные признаки пропускают подбор), `think()` получил `single` — `--no-session-persistence` и **без** `--mcp-config`; ложный комментарий о MCP заменён измерением 201-2. Коммит `61c13bc`, `DELIVERED_COMMIT=61c13bc`, сборка зелёная.
🔎 **Что доказано прибором `scripts/probe/discover-201-4.mjs` (3 фразы из 6):** «расходы на молоко, колбасу и воду 200/300/400» → `write`, **три значения 200, 300, 400** одним признаком, кандидат на 1-м месте, 9,3 с · «сколько я потратил сегодня?» → `read`, 5,5 с · «Мой друг Денис служил в президентском полку» → `write`, `person.people-he-calls-his-friends` на 1-м месте, его история названа в `not_in_list` (то есть в таблицу не пойдёт — §21.2), 7,6 с. Негативы зелёные: разговоров на диске было 186 и осталось 186 (одиночный вызов следа не оставляет) · присланные признаки неверного типа и неизвестного ключа отвергнуты с причиной, верный принят · присланный признак пропускает подбор (модель не звалась) · кандидаты находятся и для несвязанной фразы («ни одного» решает модель, а не число близости).
🛑 **КВОТА ПОДПИСКИ СЕРВЕРА КОНЧИЛАСЬ ПОСРЕДИ ПРОГОНА:** `think-quota-exhausted` — «You've hit your session limit · resets 2:40am (Europe/Berlin)», то есть **≈00:40 UTC 15-09**. Квота одна на двоих с ботом владельца: **бот в это окно тоже молчит.** Не проверены 3 случая: «я переехал в Лиссабон», «where do I live?», «отвечай мне по-русски» и негатив «почему корабли не тонут». ▶ **СЛЕДУЮЩЕЕ ДЕЙСТВИЕ: после 00:40 UTC повторить `node scripts/probe/discover-201-4.mjs` целиком и закрыть 201-4 итогом; вызовов модели до тех пор не делать.**

✅ **201-4 ЗАКРЫТ (2026-09-14, 23:22 UTC):** итог `completed-steps/201-4.md`, код `61c13bc`, `DELIVERED_COMMIT=61c13bc`. Владелец обновил подписку («updated»), полный прогон прошёл: **нужный признак первым в 6 случаях из 6** (RU и EN), кандидатов ровно 8, 5,0–9,7 с; кейс владельца даёт три значения 200/300/400; случай Дениса разделён верно — друг в признак, его история в `not_in_list`. Четыре негатива зелёные: разговоров на диске 186 → 186 · несвязанная фраза → 0 признаков при 8 найденных кандидатах (отсекает модель, не число) · присланное неверного типа и неизвестного ключа отвергнуто, верное принято и пропускает подбор · повтор индекса не перестраивает.
🔒 **ДОРОГОЙ УРОК ДНЯ:** цена прибора, названная заранее, обязана включать **остаток окна подписки** (`quotaLimits` в журнале сессии), а не только число вызовов — иначе половина замера теряется на «session limit», и квота эта общая с ботом владельца. ▶ **СЛЕДУЮЩЕЕ: 201-5 — запись** (`new-steps/201/201-5.md`: всё в граф с указателями, точное и счётное в таблицу по реестру) — после сброса контекста.

✅ **201-5 ЗАКРЫТ (2026-09-14, 23:42 UTC):** запись перестроена — **сказанное ВСЕГДА ложится в граф** (и когда фактов нет, и когда разбор отказал), таблица получает точное/счётное/текущее, указатель несёт номер строки; признаки от зовущего пропускают разбор целиком (модель не зовётся), присланное проверяется по типу и отвергнутое называется. Договор **2.9.0** (MINOR: `features`, `kept_whole`, `used_model`). Коммиты `07c0a43`, `4bb6174`, `37911af`; `DELIVERED_COMMIT=37911af`. Прибор `scripts/probe/write-201-5.mjs`: расходы → три строки #7/#8/#9 + документ графа; Денис → в таблицу «Денис», служба только в граф с якорем «Денис»; Толедо→Севилья; «много» как деньги отвергнуто и названо, фраза всё равно в графе; паритет по названному признаку; уборка по своей метке — 10 строк, чужих не тронуто.
🔒 **ГРАНИЦА, НАЗВАННАЯ ВСЛУХ:** путь с присланными признаками пишет ровно присланное, путь без них ловит и попутное (в замере — язык фразы). Закон шестой защищает путь БЕЗ признаков; обратного никто не обещал.
✗ **ДВА УРОКА ПОДШАГА:** ① проверять КАЖДЫЙ выход функции, а не главный — отвергнутый признак терялся на двух путях из трёх, и наружу шло «во фразе ничего не было»; ② мой негативный контроль на сервере снова был слеп (грепал строку в другом регистре и почти дал вывод «правка не доехала») — третий случай за три подшага. ▶ **СЛЕДУЮЩЕЕ: 201-6 — чтение** (`new-steps/201/201-6.md`: таблица или граф одним решением, указатель → строка, фильтр объектов, честное «не знаю») — после сброса контекста.

✅ **ВВОДНАЯ ЧАСТЬ ЗАПИСИ В ГРАФ — ТРЕБОВАНИЕ ВЛАДЕЛЬЦА 2026-09-14, ИСПОЛНЕНО (коммиты `dbfd6c0`, `4a8483a`, договор **2.10.0**).** Дословно: «нельзя писать в граф знаний без модификации… чтобы его вводная часть содержала признаки: кто отправил входящее сообщение (например телеграм от Рома Армстронг), элементы реестра признаков и прочее». Было: в шапке только якоря и технический ключ (`probe-201-5@local`). Стало: `Входящее сообщение из Telegram от Рома Армстронг (roma@telegram), 2026-09-15. Относится к: Денис, Рома Армстронг, roma@telegram. Признаки реестра: person.people-he-calls-his-friends.` Новый необязательный параметр `via` (канал) — память его не угадывает по ключу. Доказано на сервере: метка «Армстронг» → `["Рома Армстронг"]`, «Telegram» → `["Telegram"]`, несуществующее «Гиацинтов» → `[]`.
🔒 **ЗАКОН ЗАПИСАН В ТРЁХ МЕСТАХ, А НЕ ТОЛЬКО В КОДЕ:** паспорт §21.1 (четыре обязательные вещи вводной части + запрет служебных слов) и навык `use-knowledge-graph` (раздел «что память кладёт и почему твои вопросы вообще работают»). ✗ **Вопрос владельца «мы же писали навык графа — забыли там?» вскрыл настоящий пробел:** навык описывал только ЧТЕНИЕ, и правила записи не было нигде — ни в нём, ни в паспорте. Код без записанного закона исполняет его ровно до первой правки.
✗ **И ещё один слепой прибор (четвёртый случай за четыре подшага):** пара «было/стало» проверялась только вторым ответом — при провале нельзя было понять, подвело исправление или сама первая запись. Теперь проверяются обе половины; после правки обе зелёные («живу в Толедо» записано → было «Толедо», стало «Севилья»).

✅ **201-6 ЗАКРЫТ (2026-09-15, 00:20 UTC):** чтение перестроено — вопрос → один вызов модели по кандидатам реестра называет признак → отвечает **код** из таблицы (`sum-able` складывает сам) → нет признака или значений → имена из вопроса проверяются по меткам графа → связи только по известному имени → иначе **честное «не знаю»**. Механический поиск слов удалён (🪦 решение владельца об аннулировании «без ИИ»). `BUILT_LEVELS` = **3** (было 2 при живом графе — число не двигалось само). Коммиты `456b81d`, `155b22a`, `f204a8c`; `DELIVERED_COMMIT=f204a8c`. Прибор `scripts/probe/read-201-6.mjs` — всё сошлось: «сколько я потратил сегодня?» → **900 одной суммой** (`table+sum`, 8,0 с) · «где я живу?» и «where do I live?» → Севилья (5,3 и 5,1 с) · «как меня зовут?» → Рома Армстронг · «что было с Денисом?» → **graph, глубина 3** (7,4 с). Негативы: порода собаки → пустой `known` + названо, чего не хватает · вопрос о языке не отдаёт встречу (ложное совпадение матрицы 200-7 исчезло) · чужие объекты не приходят · без вопроса всё известное как прежде (8 значений, модель не звалась).
✗ **ТРИ ДЕФЕКТА, НАЙДЕННЫЕ ТОЛЬКО ПРОГОНОМ (ни типы, ни сборка, ни греп их не видят):** ① запись и чтение звали одно разными именами — модель придумывала род (`money_he_spent_on_purchases_by_item`), чтение искало реестровый → «не знаю» при трёх записанных тратах; лечение — роды реестра в подсказке записи; ② **осадок прежних прогонов**: пустая таблица со старым именем стояла в подсказке наравне с реестровым родом, и модель брала её — лечение: реестровые роды первыми, прочие помечены «вне реестра»; ③ «что было с Денисом?» промахивалось при живой метке «Денис» — поиск меток подстрочный и падежей не знает, лечение — основы имени (усечение до трёх букв, основа ≥4). ▶ **СЛЕДУЮЩЕЕ: 201-7 — паритет потребителей** (`new-steps/201/201-7.md`: одна матрица с признаками и без, оценки равны, цена названа числом) — после сброса контекста.

✅ **201-7 ЗАКРЫТ (2026-09-15, 00:28 UTC):** паритет потребителей доказан одной матрицей, прогнанной двумя зовущими через публичный API. **Оценки совпали построчно: 0 расхождений из 5, у обоих 5/5 «правильно»** (включая «сколько потратил» → 900, EN-вопрос и честное «не знаю» про собаку). **Цена: бот 6 ходов модели против 10 у сайта; медиана записи 272 мс против 8190 мс** — разница живёт на ЗАПИСИ (признаки пропускают разбор), на чтении её нет по устройству: `recall` признаков не принимает. Прибор `scripts/probe/parity-201-7.mjs` (`ab158c6`), отчёт `reports/parity-consumers-with-and-without-feature-registry-measured-on-one-matrix.md`, итог `completed-steps/201-7.md`.
🛑 **ИЗМЕРЕННАЯ ГРАНИЦА ДОВЕРИЯ — РАЗВИЛКА ВЛАДЕЛЬЦА, АГЕНТОМ НЕ РЕШАЕТСЯ:** значение не того типа память отвергает, а **подмену СМЫСЛА принимает** — «бот» прислал город под ключом «любимый цвет», и память записала город цветом (прежнее знание при этом цело). Заметить подмену можно только собственным разбором фразы — то есть тем ходом модели, ради экономии которого признаки и присылают. Варианты: оставить доверие (быстро, ответственность на зовущем) либо сверять своим разбором (экономия хода исчезает).
🔎 **Честно названный предел замера:** время чтения шумит (16 992 мс против 6595 мс на одном вопросе) — это разброс вызова модели; выводы о времени чтения по одному прогону делать нельзя. ▶ **СЛЕДУЮЩЕЕ: 201-8 — закрытие шага** (`new-steps/201/201-8.md`: стенд с органом «Признаки», вкладка API, правда о глубине, `MEMORY-AGENT.md` и навыки, удаление `server.js`, итоги и репорт фичи, вливание ветки) — после сброса контекста и слова владельца по развилке.

✅ **ДВА РЕШЕНИЯ ВЛАДЕЛЬЦА (2026-09-15, форма выбора, дословно).** ① Граница доверия к присланному признаку — **«Доверять боту (Recommended)»**: память проверяет тип, а не смысл; ответственность за верность ключа на зовущем; граница записана в отчёте паритета, договоре и паспорте. ② Вместо перехода к 201-8: **«Ты еще не имитировал работу памяти в режимедо диалогов с запросами на чтение и запись через api .. 30 разных вопрос на запись самых разных данных в тч сзагрузкой в объектное хранилище на разных форматах . И 30 запросов на извлечение самым разным способом. Абсолютно разные темы от 50 слов в запросе и отчет сколько правильных кейсов, неправильных и абсолютно не правильных и свежи это в отчет»** → заведён подшаг **201-9 «Большой живой прогон: 30 записей и 30 извлечений через публичный API»**, он идёт ДО 201-8.

🛑 **201-9, ПЕРВЫЙ ПРОГОН НЕДЕЙСТВИТЕЛЕН — ЕГО ОБОРВАЛА КВОТА ПОДПИСКИ, А НЕ ПАМЯТЬ (2026-09-15, 00:40–00:50 UTC).** Квота кончилась в **00:45:22 UTC** (20 отказов `rate_limit` за час), сброс **03:50 UTC**. Прибор это не различал и засчитал 48 из 60 строк как «абсолютно неправильно» — **эту сводку (12/0/48) выдавать за качество памяти нельзя**. Прибор исправлен тем же часом: отказ квоты (`think-quota-exhausted` и родня) теперь ОСТАНАВЛИВАЕТ прогон, у строк сохраняется код отказа, сводка считает только измеренное. Бот в Telegram с 00:45 до 03:50 UTC тоже молчит — окно общее.
🔎 **Что успели честно измерить первые 12 записей (до квоты) — и там НАСТОЯЩИЕ дефекты записи, противоречащие §21:**
① **W8 «валюта в евро» — отказ разбора «модель ответила не по форме» при правильном JSON:** модель обернула ответ в ограду ```json и дописала что-то после неё; `stripFence` в `lib/think.mjs` снимает ограду, только если ответ ею и заканчивается → честный факт потерян. Дефект кода, не квоты.
② **Модель по-прежнему свободно ПРИДУМЫВАЕТ роды мимо реестра:** W4 завёл `how_long_he_has_known_his_friend_denis`, W9 — `time_after_which_he_stops_answering_work_messages`, `rules_he_keeps_about_working_on_weekends` и догадку `rules_he_asks_us_to_follow_when_writing_him`. Это прямо против §21.1 («таблица — только точное, счётное, текущее») и против правила глубины: срок дружбы с Денисом — второй порядок, место ему в графе.
③ **Значения — длинные фразы, а не значения:** `city_where_he_lives_now = «Севилья (юг Испании), последние несколько месяцев»`; во W2 **второй** «город» — «снимает квартиру в тихом районе…» — то есть одна фраза даёт два значения одного текущего признака, и род `last` рискует уехать в список.
④ **W11 план на сентябрь из файла лёг в `projects_he_is_working_on`** — содержимое вложения разобрано как факт о проектах.
Сами объекты W11–W13 (txt, md, csv) приняты; 3 объекта прибора остались в хранилище — удаления договором нет.
▶ **СЛЕДУЮЩЕЕ: слово владельца — чинить ①–④ до повторного прогона или сначала прогнать как есть; повтор не раньше 03:50 UTC.**

✅ **РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-09-15, форма выбора, дословно): «Сначала чинить, потом прогон (Recommended)».**
✅ **ЧЕТЫРЕ ДЕФЕКТА ЗАПИСИ ИСПРАВЛЕНЫ (коммит `7d93918`, `DELIVERED_COMMIT=7d93918`, 01:04 UTC).** Новый `lib/write-gate.mjs` — правило §21.1 теперь исполняет КОД, а не подсказка: ① `parseModelJson` — ограда с хвостом, скобка в строке; им пользуются `think.mjs` и `features-discover.mjs` (старый разбор на ответе W8 давал «Unexpected non-whitespace character after JSON») · ② поле `shape: value|story` в подсказке записи; `story` → только граф · ③ род ВНЕ реестра ложится в таблицу только коротким значением (≤5 слов, число выбрано по живым примерам, не измерено на корпусе — перемеряется прогоном) · ④ у признака с накоплением `last` одна фраза даёт одно значение, остальное → граф. Отсеянное не теряется: строкой «что осталось только здесь» в документе графа и полем `kept_in_graph_only` в ответе. Прибор без модели `scripts/probe/write-gate-201-9.mjs` на ответах из журнала первого прогона — **10/10 локально и на сервере**, с негативами (текст без JSON → null; у списочного признака оба значения остаются; короткое значение нового рода законно).
🛑 **Не проверено моделью:** подчиняется ли модель новому полю `shape` — это покажет только повторный большой прогон. Квота подписки сервера пуста до **03:50 UTC**. ▶ **СЛЕДУЮЩЕЕ ДЕЙСТВИЕ: после 03:50 UTC — проверить остаток окна и прогнать `node scripts/probe/live-201-9.mjs` целиком (прибор теперь сам останавливается на отказе квоты); затем отчёт `reports/live-run-thirty-writes-and-thirty-reads-over-public-api.md` и итог 201-9.**

✅ **ИТОГОВЫЙ ОТЧЁТ ЗАХОДА 201-9 СОХРАНЁН — слово владельца (2026-09-15), дословно: «Сохрани итоговый отчет и мы завтра подумаем какие выводы из этого нужно сделать».** `reports/live-run-thirty-writes-and-thirty-reads-over-public-api.md`: измерено 13 строк из 60 (квота кончилась в 00:45:22 UTC) — **правильно 12 · неправильно 0 · абсолютно неправильно 1 (W8)**; 47 не засчитано с причиной у каждой; прямо сказано, что судья записи мягкий и три «правильных» несут дефекты ②③④; четыре вопроса на завтра (квота, строгость судьи, новые роды, удаление объектов). **Шаг 201 ПРИОСТАНОВЛЕН:** 201-9 ждёт повторного захода после сброса квоты и выводов владельца; 201-8 не начат. ▶ **СЛЕДУЮЩЕЕ: новый шаг 202 — раздел `memory.aifa.dev/{lang}/build` (слово владельца того же сообщения).**

▶ **ШАГ 202 СПЛАНИРОВАН И ИДЁТ (2026-09-15) — мастерская разработки на `/{lang}/build`.** План `new-steps/202/202-main.md` + `202-1…202-7`. Слова владельца дословно — в плане; **этап 9 заменён его прямым словом**: «Я не зови меня ты не найдёшь … используй самостоятельно-то решение которое считаешь лучшим». Ветка `new-step-202` **от `new-step-201`** (шаг 201 не влит, файловая доставка иначе затёрла бы его правки в `server.mjs`/`proxy.ts`); вливание: 201, затем 202. 🔎 Измерено: на сервере в `development-docs/development-steps/` 17 итогов и очередь с шагом 189 — учёт не доставлялся, страница показала бы неправду → 202-7 чинит доставку. Папка службы на сервере — не свой git, а часть клона `/opt/fractera`. ▶ **Идёт 202-1 — раскладка.**

✅ **ШАГ 202 ЗАКРЫТ (2026-09-15) — мастерская разработки `/{lang}/build`.** Итоги `completed-steps/202-1…202-7.md`, `202-main.md`, репорт `reports/feature-build-workshop-terminal-that-sleeps-tasks-steps-skills-instruction.md`; план `new-steps/202/` удалён коммитом закрытия. Доказано на сервере (`DELIVERED_COMMIT` — последний перед закрытием, см. итог): `build-session-202-2.mjs` 13/13 (спит до запуска, pid жив через 5 с после закрытия сокета, возврат получает экран, остановка убивает, «not-running» ничего не рождает) · `build-tasks-202-3.mjs` 17/17 · `build-page-202.mjs` 30/30 (6 разделов, цитата, окно до 1000 px, шаги 4/11 = локальным, навыки 9/9, негативы путей). Негатив доставки: заявка, созданная на сервере, пережила замену учёта. `/code` — `db54237` (`deliver-memory.sh`: учёт шагов целиком) + коммит федерального `CLAUDE.md`.
🛑 **НАЗВАНО ВСЛУХ:** ① живым браузером страница не осмотрена — у Chrome нет входа, пароль вводить запрещено; ② `main` НЕ влит: ветка 202 стоит на незакрытой 201, порядок вливания 201 → 202; ③ доставка перезапускает службу и останавливает запущенный терминал; ④ мастерская и её двери открыты секрету машины ради приборов (решение агента по слову «решай сам»).
▶ **СЛЕДУЮЩЕЕ: слово владельца — выводы по отчёту 201-9 (`reports/live-run-thirty-writes-and-thirty-reads-over-public-api.md`), затем повторный заход 30+30 и закрытие 201 (201-8), затем вливание 201 → 202.**

🔎 **Финальная доставка после закрытия 202 (2026-09-15 08:25 UTC):** `DELIVERED_COMMIT=2fb6fee`; на сервере `new=3 done=85` файлов; прибор страницы — новые шаги 201·199·189, завершённые 202…183 (12 = локально), всё сошлось. `/code` — `83021ac` (федеральный `CLAUDE.md`), не отправляется.

✅ **ШАГ 202 ЗАКРЫТ ЗАНОВО С ПОДШАГОМ 202-8 (2026-09-15).** Слово владельца, дословно: «попрошу тебя каждому элемента кроме терминал на вкладке показать расположение в проекте этого файла. То есть нажали кнопку добавить задание открылась вкладка добавить задание и под заголовком рисуешь путь к файлу в данном случае pre-steps.md. Задача показать как в проекте устроен архитектуру и расположение файло». Маршрут: исход A, план возвращён из git (`2fb6fee^`), добавлен 202-8, шаг закрыт целиком, план удалён снова этим коммитом. Код `8877941`, `DELIVERED_COMMIT=8877941`: `BUILD_PATHS` в `_lib/build-sections.ts`; под заголовком семи разделов пути (`pre-steps/` + `pre-steps/README.md`, `current-steps.md`, `development-steps/`, `new-steps/`, `completed-steps/`, `.claude/skills/`, `CLAUDE.md`), в окне — путь открытого шага или навыка; у терминала пути нет. Прибор страницы на сервере — всё сошлось (пути существуют на диске). Допущение: «pre-steps.md» показан папкой и файлом формата — файла с таким именем нет.

✅ **202-8 ИСПРАВЛЕН, ШАГ 202 ЗАКРЫТ ЗАНОВО (2026-09-15).** Слово владельца, дословно: «Это же не полный адрес он же начинается с opt/fractera…?» Пути разделов мастерской теперь полные: `fullPath()` в `lib/build-docs.mjs` приставляет корень процесса службы (на сервере `/opt/fractera/memory`), а не вписанный адрес. Код `692a8e6`, `DELIVERED_COMMIT=692a8e6`; прибор страницы ждёт `/opt/fractera/memory/…` (корень вписан в приборе, не взят у процесса) — всё сошлось, адреса существуют на диске; негатив: ни одного пути от корня проекта без адреса машины. План возвращён из git и удалён снова этим коммитом.

✅ **ШАГ 202 ЗАКРЫТ ЗАНОВО С ПОДШАГОМ 202-9 (2026-09-15).** Слово владельца, дословно: «на вкладке терминала нужно написать : Агент будет запущен в рабочем пространстве :… Красную карточку : Что именно запускаете по умолчанию показать схлопнутый и показывает только заголовок что именно вы запускаете при нажатии v иконку показывает текущий текст». Код `00ea4c1`, `DELIVERED_COMMIT=00ea4c1`: `lib/fractera/workspace-dir.mjs` — одна функция папки у моста и страницы; «Агент будет запущен в рабочем пространстве: /opt/fractera/memory»; красная карточка — `<details>`, свёрнута, заголовок «Что именно вы запускаете» со стрелкой. Приборы на сервере: страница — всё сошлось; терминал 15/15, папка живого процесса (`/proc/<pid>/cwd`) = названной на странице, Claude Code выходит за 4,3 с после «Остановить». ✗ Прибор терминала сперва мерил выход фиксированной паузой — заменено ожиданием по факту. План возвращён и удалён снова этим коммитом.

💬 **ОБСУЖДЕНИЕ ГЛАВНОЙ ИНСТРУКЦИИ ПАМЯТИ (2026-09-15) — стадия обсуждения, код не менялся.** Слово владельца, дословно: «ты забыл что 99% времени наша платформа предназначена для того чтобы управлять памятью – необходимость разработки памяти это один очень редкий случай для очень незначительным количества разработчиков поэтому инструкция памяти она прежде всего должна ориентирована быть на том чтобы управлять памятью и опционально заявлять информацию о том что территорий может быть запущен в разработку и обновлён при помощи Cloud Code … Я предполагаю что это отдельный навык который будет называться что-то вроде бы memory Development Скилл … Разработка, использование документации разработки и прочие другие навыки это посути дело практически полное копирование нашей инструкции которые расположены в порт 3000 … исследовал … подверг критическому анализу мои требования показал сильное слабость стороны сказал что именно из главный инструкции по разработке кода мы должны перенести в эту».
🔎 **ИЗМЕРЕНО ПРИ РАЗБОРЕ:** ① `MEMORY-AGENT.md` не читает НИ ОДНА строка кода памяти (греп по `lib`, `app`, `scripts`, `server.mjs` — только текст страницы `/build`), и он устарел (пишет «изображения, PDF — пока не разбираю», хотя объекты работают с 192–195). ② `CLAUDE.md` памяти (18 КБ) — сегодня инструкция СТРОИТЕЛЯ; её получает каждый, кто откроет `claude` в папке. ③ Права двух ролей противоположны и уже разведены файлами: `.claude/settings.json` (агент: только инструменты памяти и чтение) и `.claude/settings.build.json` (строитель: Write/Edit, подаётся флагом терминала `/build`). ④ Навыки разработки памяти `use-development-steps` (536 строк) и `use-testing` — русские копии ФЕДЕРАЛЬНЫХ, а у порта 3000 свои английские (485 и 139 строк) плюс разделы инструкции «How you answer me · Your memory · You always evolve · Development modes · How we build · Testing». ⑤ 🛑 **`lib/bootstrap.sh` (FES) запускает память `pm2 start server.js` БЕЗ установки зависимостей и сборки** — новый сервер рождается со старой службой без страниц, стенда и мастерской; живой сервер работает на `server.mjs`. Значит удаление `server.js` в 201-8 без правки установщика сломало бы рождение серверов; установщик — замороженная инфраструктура L1, правка — решение владельца. ⑥ Учёт разработки (131 файл `development-docs`) едет каждому клиенту клоном — против федерального закона «осадок не уезжает». ▶ Ждём решения владельца по итогам обсуждения.

## ⏸ (ИСТОРИЯ) 2026-09-14, вечер

**Открытых шагов в работе нет.** Шаг 195 «ссылки в памяти» закрыт по слову владельца: «195-13 и навык, потом закрыть».
Итог — `completed-steps/195-main.md`, репорт фичи — `reports/feature-links-as-objects-whose-source-is-an-address-browser-and-youtube-api.md`.
Ветка `new-step-195` влита в `main` и названа `old-step-195`; план `new-steps/195/` удалён тем же коммитом и восстанавливается из git.

🔒 **СЛОВО ВЛАДЕЛЬЦА О ТЕМПЕ, дословно (2026-09-14):** «нужно разрабатывать другой метод который не относится к памяти и позволяет извлекать
транскрипцию видео. На этом поставим точку в разработке памяти». **Новую работу по памяти не начинать без его слова.**

## ⏳ Ждёт глаз владельца (построено, доставлено, доказано приборами)
- вкладка **«Навык»** стенда ссылок — `memory.aifa.dev/ru/settings?section=link-test&tab=skill` (навык `use-links`);
- **картинка сниппета** в блоке «что легло в память» — там же, вкладка «Загрузка»;
- **порядок по дате и удаление случая** на четырёх вкладках «Оценка» (граф, вектор, объекты, ссылки).

## 🛑 Что названо долгом и уехало в `development-docs/BACKLOG.md` федерального слоя
`keep_links` в договоре (195-3) · режим «сравни» (195-6) · расшифровка ролика (195-12 — отдельный метод вне памяти) · предел на число элементов
структуры в снимке (MDN без текста весит 57 703 знака) · порог поиска 0.30 не измерен на ссылках.

## 🔒 Дорогие измерения — не искать заново
- **201-2 (2026-09-14):** `claude -p` 2.1.271 знает `--no-session-persistence` (разговор не пишется, `--resume` → «No conversation found»); стена вызова длиннее модели на 2–3,7 с; встраивание вопроса `text-embedding-3-large` ~200 мс, поиск в складе 13–46 мс; граф на отсутствующее имя возвращает ближайшее, «не знаю» — только поиском метки; MCP в `.mcp.json` памяти — относительный путь, в вызовах модели `failed`. Отчёт — `instruments/201-2-costs.md`.
- `captions.download` по ключу API → `401` «API keys are not supported by this API»; `captions.list` дорожки показывает. **Текста чужих субтитров
  официальный API не отдаёт никогда.**
- `videos.list` — 1 единица из 10 000 в день, ~0,5 с; **главы лежат в `snippet.description`** и отвечают «на какой минуте».
- **API называет размер обложки, которого нет:** `fhd` → `404` телом-картинкой 1097 байт; живой максимум `maxres` 1280×720.
- Vercel проверяет адрес сервера **выборочно**: ИИ-браузер 403 три раза подряд, `curl` в те же минуты 403/200/403.
- Расширение Claude в Chrome владельца управляемо С СЕРВЕРА (`claude -p --chrome`, rc 0, `isLocal:false`): читает страницу за **110 с и 14 ходов
  модели** против 7 с и 0 ходов у ИИ-браузера → шаг **199** в очереди (`new-steps/199/199-main.md`, план не развёрнут).
- Терминал памяти исправен: сокет `/pty` жив, неверный билет → `1008 bad-ticket`, оболочка поднимается, `claude` 2.1.270, подписка `pro`.
- Поиск по написаниям на одной записи: `ilon mask` 0.141 · `Elon Musk` 0.428 · `Илон Маск` 0.437 · длинная фраза 0.650.
- Ключ YouTube: `fractera-easy-starter/.env.local` → `YOUTUBE_API_KEY` (шаг 47, проект Google `336373493361`); на сервере — в складе секретов
  машины `0600`, вставлен владельцем 2026-09-14.
- Доставка памяти: `bash /code/scripts/deliver-memory.sh <базовый-коммит>`; **доставка перезапускает службу** и рвёт идущий запрос владельца.

## 🔒 Законы, добытые этим шагом (годны вне памяти)
1. **Что положено в снимок, то и станет описанием.** Объём снимка есть решение о том, что память будет знать.
2. **Отметка оставляет способность живой; «убрать» значит убрать.**
3. **Адрес, названный чужим API, проверяется фактом** — у отказа тело бывает картинкой, правду говорит код ответа.
4. **«Кто её зовёт» находит то, чего не находит греп по имени** — так нашлась таблица связей без писателя.
5. **Опора правки, тронутая раньше в той же сессии, устаревает молча.**
6. **Эталон негатива сам проверяется до прогона.**

## ⏸ Прежние шаги
189 открыт (ждёт 189-7 — прогон владельцем на его данных). 183–186, 189, 192, 193, 194 закрыты — итоги в `completed-steps/`.
Федеральный учёт службы ИИ-браузера и шагов 196–199 — в `/code/development-docs/development-steps/current-steps.md`.
