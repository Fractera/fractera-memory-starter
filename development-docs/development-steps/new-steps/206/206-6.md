# 206-6 — Договор, API и мёртвые приборы

## Что строится
- `contract.mjs`: версия **4.0.0** (MAJOR — из договора уходят роды и колонки), число методов печатает
  сторож, а не рукописная строка.
- Параметры глаголов и `development-docs/BLACKBOX-API.md` — без специализированных таблиц.
- Снимаются приборы, чей предмет удалён: `scripts/probe/consumers-205-5.mjs` (реестр), приборы ворот
  записи и родов. Список получается прогоном, а не памятью.
- `npm run check` зелёный; `npx tsc --noEmit` чист.

## Чем доказывается
1. `npm run check` и `tsc` на чистом дереве; негатив — намеренно сломанная запись договора роняет сторож.
2. Живой `/v1/health` на сервере отдаёт версию 4.0.0 после доставки.
