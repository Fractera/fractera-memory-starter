# 204 — главная страница и README описывают движение запроса так, как оно работает сейчас

**Ветка:** `new-step-204` (от `new-step-202` + план 203). **Заведён:** 2026-09-15. Короткий шаг, без подшагов. Предварительный к 203.

## Откуда взялась задача
Владелец 2026-09-15: «Также нужно обновить главный документацию в корне проекта Memory потому что мы вчера обсуждали фундаментальные
изменения процесса движения запроса а главной странице отображает все ещё предыдущие результаты и я не могу быть уверен в том что сейчас
работаю енот создание новых конструкций мы с тобой говорим об одном и том же. Поэтому мне нужен предварительный шагисправь в документации
абзац который называется экономический закон архитектуры опиши итогах работает сейчас архитектура а потом я тебе дам добро или запрет на
дальнейшую разработку». Затем: «нигде в этой документации не описано правила переиспользования элементов реестра признаков?». На
предъявленный словами план — форма: **«Да, пиши так»**.

## 1. Что строится
- `app/[lang]/_i18n/landing.i18n.ts`, en и ru: `ladder` («Экономический закон архитектуры» / «The economics of the architecture») —
  шаги пути запроса вместо лестницы «уровень 1 без ИИ»; `router` («Как проходит запрос»); одна фраза `problem.body` про «детерминированный
  роутер». Строка про реестр: 21 признак, правится руками, правил переиспользования нет.
- `README.md`: вступление («under 10 ms for zero tokens»), строка чек-листа «Fast with no AI…», раздел «The deterministic router».
- Источник правды — итоги 201-2…201-7 и код (`lib/params.mjs` `BUILT_LEVELS = 3`, `lib/verbs.mjs` `depth_used`, `lib/features-discover.mjs`).

## 2. Что считается готовым
На `memory.aifa.dev/ru` раздел «Экономический закон архитектуры» описывает путь «кандидаты → один вызов модели → граф и таблица → честное
„не знаю“», и ни один из двух разделов не обещает «0 токенов, без ИИ».

## 3. Чем доказывается (названо заранее)
- **Плоскость 1 — исходник:** счёт устаревших фраз (`zero tokens`, `0 токенов`, `sub-10`, `10 мс`, `no model at all`, `Deterministic`,
  `Детерминированный`) в словаре главной и в `README.md` → 0 в правленых местах. **Негатив:** тот же счёт на родителе коммита → больше 0.
- **Плоскость 2 — отданная страница после доставки:** `curl https://memory.aifa.dev/ru` — новая строка раздела есть, «0 токенов, без ИИ» — 0;
  **негатив:** выдуманная строка — 0.

## 4. На что опирается
Шаг 201 (построенное поведение), доставлен на сервер коммитом `baee113`.

## 5. Чего в шаг не входит
Правила переиспользования признаков (обсуждение до кода) · раздел «memoization» и «evolution core» (не сверены с кодом — названы, не правятся) ·
остальные разделы главной.
