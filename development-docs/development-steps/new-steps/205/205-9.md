# 205-9 — живая проверка «пришло название — что делаю», итоги, репорт фичи

## 1. Что строится
- Прибор `scripts/probe/feature-name-205-9.mjs` на сервере через публичный договор (квота названа до прогона, метка прибора, уборка своего):
  три зовущих — «чат» шлёт ключ памяти, «старый чат» шлёт ключ чата, «сайт» шлёт только фразу; затем чтение по признаку и без.
- Итоги `completed-steps/205-1…205-9.md`, `205-main.md`; репорт фичи; `current-steps.md`; федеральные `CLAUDE.md`/`ARCHITECTURE.md`;
  вливание в `main` и доставка во всех задетых репозиториях; сверка `main == origin/main`.

## 2. Что считается готовым
Ключ памяти, ключ чата и одна фраза дают одно и то же сохранённое значение; различаются только время и подсказка ключа.

## 3. Чем доказывается (названо заранее)
- **Плоскость 1 — ответы договора:** «чат» — признак принят; «старый чат» — отказ с `nearest` и значение, найденное разбором; «сайт» — то же
  значение разбором; **негатив** — выдуманный ключ `person.teleport` → отказ без `nearest` выше порога.
- **Плоскость 2 — хранилище:** строка таблицы под меткой прибора одна на каждого зовущего, значения совпадают; граф получил документ;
  уборка — строк с меткой 0.

## 4. На что опирается
205-1…205-8.

## 5. Чего в подшаг не входит
Переключение кода чата · сноски *¹, *³, *⁶, *⁸, *⁹.
