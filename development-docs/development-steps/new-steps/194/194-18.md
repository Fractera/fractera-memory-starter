# 194-18 — документы говорят правду об объектах: договор, страница API, README, паспорт ящика

## 0. Откуда подшаг
Сверка 2026-09-13: README §1 и `capabilities.json` обещают OCR и «media handles the pipeline itself»; `api.i18n.ts`
en/ru — «land in object storage»; `BLACKBOX-API.md` об объектах молчит и застрял на 175-6. Слово «Делай полный вариант».

## 1. Что строится
- `app/[lang]/settings/_components/api-doc.tsx`: у метода виден род тела (`multipart` · `JSON`), пример `curl -F` для
  `keep_object`, адрес файла в каталоге. Слова — `_i18n/api.i18n.ts` en/ru для трёх методов и новых параметров.
- `README.md` §1 и таблица параметров, `capabilities.json` `multimodal_input`: роды, как есть в коде (Claude читает,
  whisper-1 расшифровывает, шесть кадров; markdown, code), без OCR.
- `development-docs/BLACKBOX-API.md`: раздел «Объекты» — три метода, файл, происхождение; §3 и §4 приведены к сегодня.
- `lib/llms.ts` и примеры главной — если называют API объектов, то по договору.

## 2. Что считается готовым
Каждое утверждение об объектах в пяти документах подтверждается строкой кода; прибор `scripts/probe/api-i18n.mjs` зелёный.

## 3. Чем доказывается — названо заранее
- **A (сборка страницы на сервере):** чанк страницы `section=api` содержит `keep_object`, `find_objects`,
  `open_object` и `multipart` на обоих языках (страница за входом — счёт по сборке).
- **B (негатив, grep по репозиторию):** «OCR», «parsed directly», «land in object storage», «приняты и записаны в
  объектное хранилище» — 0 вхождений; `api-i18n.mjs` падает, если убрать перевод одного параметра.

## 4. На что опирается
194-15, 194-16, 194-17.

## 5. Чего в подшаг не входит
Новости и публичные страницы FES — только с отдельного «да» владельца.
