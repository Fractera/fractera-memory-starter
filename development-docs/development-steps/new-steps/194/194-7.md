# 194-7 — прибор, доставка, закрытие

## 1. Что строится
`scripts/probe/object-describe.mjs`: картинка, PDF, аудио и короткое видео из `/tmp/obj-corpus` или
`public/` FES; метка `who = probe-194`; уборка по метке со сверкой состава базы.

## 2. Что считается готовым
Прибор зелёный, состав базы вернулся, шаг влит в `main` и отправлен.

## 3. Чем доказывается
- **A:** вывод прибора `✓ OK`.
- **B:** состав базы после = до.

Цена: ≈ 4 хода Claude + 2 расшифровки OpenAI. Подагентов нет.
