# 194-3 — экран описания

## 1. Что строится
В `object-bench.client.tsx`: кнопка «Получить описание» справа от «Выбрать файл» — видна только при
выбранном файле; два правимых поля «Полное описание» и «Саммари» вместо одного; ожидание с секундами;
отказ словами. Слова `en` и `ru` в `memory.i18n.ts`.

## 2. Что считается готовым
Выбрал файл → нажал → оба поля заполнены и правятся.

## 3. Чем доказывается
- **A:** новые слова доехали в сборку; негатив ложным словом = 0.
- **B:** глаза владельца.

## 4. Чего не входит
Сохранение — 194-4.
