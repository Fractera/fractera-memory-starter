# 194-14 — навык `describe-incoming-object` под настоящую работу: API и Telegram

## 0. Откуда подшаг
Слово владельца 2026-09-13: «И сразу после этого замечательным образом исследуем навык. Потому что именно этот
навык будут делать эту работу. Когда она будет приходить не из стенда а из-за реального пространства это будет
либо API либо Telegram.»

## 1. Что строится
Переписанный `.claude/skills/describe-incoming-object/SKILL.md` (английский), сверенный с кодом после 194-13:
- откуда объект приходит и как это записывается: `source` (`stand` · `api` · `telegram`), `author` (для Telegram —
  имя человека, для API — `who` договора), дата; всё это уходит в шапку документа графа;
- четыре записи и их связи в строке таблицы; что проверить после сохранения (все четыре ссылки);
- код — анализ, не исходник; прочие роды — реконструкция; метки времени у звука и видео;
- честные пределы: к `/v1/remember` и к Telegram навык ещё не подключён, рук `describe_object`/`keep_incoming_object`
  у агента нет — сегодня путь идёт через двери стенда.

## 2. Что считается готовым
Каждое утверждение навыка подтверждается строкой кода или выводом прибора 194-13; противоречий с кодом нет.

## 3. Чем доказывается
- **A:** таблица «утверждение навыка → файл и строка кода» в итоге подшага, без пропусков.
- **B, негатив:** поиск в навыке прежних ложных утверждений («граф только с якорями», «summary — единственное в
  графе», «исходник дословно») — 0 вхождений; sha навыка на сервере = в git.

## 4. Чего не входит
Подключение навыка к `/v1/remember` и к Telegram — отдельная работа и отдельное решение владельца.

## 5. Поправка 2026-09-13, вечер
Опирается на 194-15…194-18: владелец выбрал «полный вариант: методы объектов в API», поэтому навык описывает настоящие
`keep_object` · `find_objects` · `open_object`, а не двери стенда. Строка «к `/v1/remember` навык не подключён» переписывается по итогу 194-16.
