# 194-11 — главная memory.aifa.dev говорит о новых родах объектов, и говорит правду

## 0. Откуда подшаг
Проверка, заказанная владельцем: «кроме PDF мы также можем работать с Markdown и HTML форматом проверь что
наш навык и наш архитектура позволяет это делать в равной степени. Также проверь что это присутствует в
описании на главной странице memory.aifa.dev». Затем — его «да» на правку публичного текста, дословно:
«and for update content about new object types».

**Измерено до правки:** Markdown как входящий формат не упомянут нигде; HTML — только в одном ответе FAQ;
код не упомянут. **Попутно:** текст обещал «локальный перевод речи конвейером Whisper» и «OCR для сканов» —
на деле речь расшифровывает OpenAI `whisper-1`, картинки, PDF, Markdown, HTML и код читает Claude, OCR нет.
Обещание, которого продукт не держит, человек проверяет в свой худший день.

## 1. Что строится
`app/[lang]/_i18n/landing.i18n.ts`, ветки `en` и `ru`, одинаково:
- строка сравнения «встроенная мультимодальность» — все роды;
- FAQ «что можно присылать» — роды, полное описание для реконструкции + саммари ~50 слов, кто что читает, код не запускается;
- описание для поисковика и абзац первого экрана — Markdown, HTML, исходный код;
- раздел «мультимодальность» — шесть карточек вместо четырёх: звук · изображения · видео · PDF · Markdown и HTML · исходный код, без «локального Whisper» и «OCR»;
- поток на схеме роутера и карточка объектного хранилища.
Зеркала `index.md` и `llms.txt` — если строят текст сами, правятся той же правкой.

## 2. Что считается готовым
На `memory.aifa.dev/ru` и `/en` названы Markdown, HTML и код; ложных обещаний про OCR и локальный Whisper нет.

## 3. Чем доказывается
- **A — отданная страница** `/ru` и `/en`: счёт «Markdown», «HTML», «исходный код / source code» > 0.
- **B, негатив:** счёт «OCR» и «Локальный перевод речи» / «Local speech-to-text» = 0 на тех же страницах; ложное слово = 0.
