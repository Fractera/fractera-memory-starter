# 195-5 — вкладки «Поиск», «Оценка», «Навык» стенда ссылок; главная и страница API; закрытие шага

## 1. Что строится
- Стенд ссылок получает «Поиск» и «Оценку» тем же видом, что у стенда объектов (общий блок «что легло в память» у найденного).
- Навык агента памяти о ссылках (английский, `.claude/skills/`), имя — в `stand-skills.ts` у `"link-test"`; сверка каждого
  утверждения навыка со строкой кода (закон 194-14).
- Главная memory.aifa.dev — раздел «Страницы и YouTube по ссылке» (`app/[lang]/_i18n/landing.i18n.ts`, все ветки); страница API и
  `BLACKBOX-API.md` — `keep_links`; `README`.
- Закрытие: итоги 195-1…195-5, `195-main`, репорт фичи, план удаляется тем же коммитом, ветка в `main`, `old-step-195`; обе главные
  инструкции (`CLAUDE.md` памяти и федеральный).

## 2. Что считается готовым
Архитектор на стенде кладёт ссылку, находит её другими словами и ставит оценку; главная и API описывают ссылки; шаг закрыт.

## 3. Чем доказывается — названо заранее
- **A:** прибор стенда — положить → найти другими словами → оценка записана; главная и API на ru/en отдают раздел ссылок (200 и счёт строк).
- **B:** бессмыслица — `found:false`, а не ближайший мусор; старых обещаний (метод без исполнителя) в документах 0 по образцам;
  навык: утверждений без строки кода 0.

## 4. На что опирается
195-1 … 195-4.

## 5. Чего не входит
Главный стенд «Тест памяти».
