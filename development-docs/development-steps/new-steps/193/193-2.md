# 193-2 — навык `use-tables`, поглощающий `grow-the-shape`

## 1. Что строится
- `.claude/skills/use-tables/SKILL.md` — английский, форма трёх соседей: четыре руки · **три метода
  записи и их следы в базе** · четвёртый путь без своего имени · решение формы (из `grow-the-shape`
  целиком) · имя как граница безопасности · цена · пределы.
- `.claude/skills/grow-the-shape/` удаляется тем же коммитом.
- `MEMORY-AGENT.md`: строка `grow-the-shape` заменяется на `use-tables`; надгробия там нет.
- `PASSPORT.md` §19: таблица навыков, надгробие поглощения с причиной и датой.

## 2. Что считается готовым
У каждого хранилища ровно один навык; ни один файл в репозитории не ссылается на `grow-the-shape`.

## 3. Чем доказывается — названо заранее
- **Плоскость A — счёт по репозиторию:** ссылок на `grow-the-shape` вне паспорта и учёта — **0**;
  навыков в `.claude/skills/` — прежнее число (поглощение, а не прибавление). **Негативный
  контроль:** заведомо ложная строка даёт 0 — счётчик умеет не находить.
- **Плоскость B — содержание не потеряно:** каждое правило прежнего навыка присутствует в новом,
  сверка построчно по списку из шести правил, названному в итоге. Проверка на деле — случай прогона
  193-3 об имени-ярлыке (то самое, что `grow-the-shape` покупал по замеру 189-10).

## 4. На что опирается
193-1 — следы методов берутся из измерения, а не из чтения кода.

## 5. Чего не входит
Правка механики записи; новый инструмент под «дописать в таблицу».
