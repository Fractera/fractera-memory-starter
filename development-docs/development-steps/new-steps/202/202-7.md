# 202-7 — доставка учёта, живая проверка, закрытие

## 1. Что строится
- `/code/scripts/deliver-memory.sh` — вместе с кодом возит `development-docs/development-steps/` (кроме заявок `pre-steps/*.md`, созданных на
  сервере) и `CLAUDE.md`; разовая синхронизация: устаревшие `new-steps/` и `completed-steps/` на сервере заменяются актуальными.
- Прогон приборов 202-2 и 202-3 на сервере, счёт HTML разделов 202-1, 202-4…202-6.
- Итоги `202-1…202-7`, `202-main`, репорт фичи; `CLAUDE.md` памяти и федеральный; план удаляется тем же коммитом; вливание по порядку.

## 2. Что считается готовым
На `memory.aifa.dev/ru/build` работает всё перечисленное, и сервер показывает те же шаги, что локальный репозиторий.

## 3. Чем доказывается
- **A:** число шагов в меню на сервере = числу шагов в локальном репозитории; приборы зелёные.
- **B (негатив):** заявка, созданная на сервере до синхронизации, после неё цела.

## 4. На что опирается
202-1…202-6.

## 5. Чего не входит
Перевод папки службы в свой git-клон на сервере.
