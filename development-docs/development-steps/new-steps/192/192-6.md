# 192-6 — закрытие шага

## 1. Что строится
Итоги `completed-steps/192-1 … 192-6` и `192-main.md` · `CLAUDE.md` строителя (таблица навыков) ·
`BLACKBOX-API.md` не трогается (договор не менялся — сказать явно) · `current-steps.md` службы и
`/code` · удаление плана тем же коммитом · ветка отправлена.

## 2. Что считается готовым
Шаг закрыт коммитом с хэшем; `origin/new-step-192 == new-step-192`.

## 3. Чем доказывается — названо заранее
- **Плоскость A — git:** `git rev-parse new-step-192` = `git rev-parse origin/new-step-192`.
- **Плоскость B — сервер несёт последний код:** прибор 192-2 зелёный на собранной службе после
  последней доставки. **Негатив:** план из `new-steps/192/` восстановим `git show <хэш>^:<путь>`.

## 4. На что опирается
192-1 … 192-5.

## 5. Чего не входит
Слияние в `main`.
