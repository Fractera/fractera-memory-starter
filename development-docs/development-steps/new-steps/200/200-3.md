# 200-3 — стенд возвращается к двум глаголам: «Метод договора» уходит

## 1. Что строится
- `memory-test.client.tsx` — входы «Сказать» и «Спросить» (+ «Сырой вызов» по развилке 1); режим `method`, `CallForm`, `buildContractCall`,
  проп `targets` удаляются. Панель «что уедет» (адрес · заголовки с маской ключа · тело) и адрес в ленте ответа остаются.
- Удаляются: `memory-call-form.client.tsx`, `lib/contract-call.mjs`; в `page.tsx` — `callTargets()` и импорты `CATALOGUE`, `placeholdersOf`,
  `apiDocWords`; в `memory-bench.client.tsx` — проп `targets`.
- Слова `en`/`ru`: удаляются `method`, `methodHint`, `missingTitle`, `badTitle`, `call`; лид говорит о двух глаголах через публичный API.

## 2. Что считается готовым
На живом стенде два глагола с расширенными параметрами; ни экрана, ни кода выбора из 8 методов нет; вызовы по-прежнему идут в `/v1/*`.

## 3. Чем доказывается
- **A (поверхность владельца + журнал nginx):** в Chrome владельца кнопки входов — ровно «Сказать», «Спросить» (± «Сырой вызов»);
  `#memory-call-target` отсутствует; «Спросить» без фразы → в ленте `POST https://memory.aifa.dev/v1/recall` `200`; в журнале nginx в ту же
  секунду `213.199.61.7 POST /v1/recall 200`.
- **B (негатив, код):** в дереве `grep` по `CallForm|contract-call|callTargets|buildContractCall` — 0; `tsc` 0; до правки — не 0.

## 4. На что опирается
200-1.

## 5. Чего не входит
Правка органов и вложений (200-4, 200-5); договор.
