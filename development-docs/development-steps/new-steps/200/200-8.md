# 200-8 — закрытие шага

## 1. Что строится
- `development-docs/PASSPORT.md` §13 — стенд: два глагола, расширенные параметры, вложения, типизированный ответ, путь через публичный API.
- `development-docs/BLACKBOX-API.md` — `remember` с формой и ссылками, схема `output`.
- `CLAUDE.md` памяти и федеральный — что стенд теперь такое и как им пользоваться; надгробие попытки «8 методов на стенде» с причиной.
- Итоги `completed-steps/200-3 … 200-8`, `200-main.md` (переписан целиком, включая 200-1, 200-2 и откат 200-2), репорт фичи — отчёт 200-7.
- План удаляется тем же коммитом; `new-step-200` вливается в `main`, отправка, сверка `main == origin/main`; `stash@{0}` сбрасывается по
  слову владельца.

## 2. Что считается готовым
Шаг закрыт; следующая сессия знает устройство стенда и договора после шага 200.

## 3. Чем доказывается
- **A:** коммит закрытия несёт итоги и удаление плана; `main == origin/main`; на сервере `DELIVERED_COMMIT` = закрывающий код.
- **B (негатив):** `git log --diff-filter=D -1 -- development-docs/development-steps/new-steps/200/200-main.md` находит коммит; по несуществующему
  пути — пусто.

## 4. На что опирается
200-3 … 200-7.

## 5. Чего не входит
Снятие прочих методов наружу — отдельный шаг.
