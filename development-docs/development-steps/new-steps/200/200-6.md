# 200-6 — закрытие шага

## 1. Что строится
- Паспорт §13 «Стенд памяти» — стенд проверяет договор публичным API, форма порождается из договора; прежние законы 183 — надгробиями, где отменены.
- `BLACKBOX-API.md` — стенд как первый потребитель договора.
- `CLAUDE.md` памяти и федеральный — что стенд теперь такое и как им пользоваться.
- Итоги `completed-steps/200-1 … 200-6`, `200-main`, репорт фичи (отчёт 200-5 входит в него), план удаляется тем же коммитом, ветка в `main`, `old-step-200`.

## 2. Что считается готовым
Шаг закрыт, и следующая сессия знает, что стенд ходит в `/v1/*` и порождается из договора.

## 3. Чем доказывается
- **A:** паспорт на сервере отдаёт новый §13; `main == origin/main`.
- **B (негатив):** в инструкциях и паспорте не осталось утверждения «стенд зовёт исполнителей напрямую» (счёт 0) и «загрузка не доезжает» (счёт 0).

## 4. На что опирается
200-5.

## 5. Чего не входит
—
