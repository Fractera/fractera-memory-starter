# 201-8 — закрытие шага

## 1. Что строится
- Стенд «Тест памяти»: орган «Признаки» у обоих глаголов (выбор из каталога реестра), в ответе `found_by` и указатели.
- Вкладка API: параметр `features`, каталог `/v1/features`, схема ответа.
- `MEMORY-AGENT.md` и навыки `use-tables` / `use-knowledge-graph` — строки о новом устройстве, сверенные с кодом (без прогонов).
- `CLAUDE.md` памяти и федеральный — что построено и как пользоваться.
- Итоги `201-1…201-8`, `201-main`, репорт фичи; план удаляется тем же коммитом; `new-step-201` вливается в `main`; сверка `main == origin/main`
  в обоих задетых репозиториях.

## 2. Что считается готовым
Шаг закрыт; следующая сессия и разработчик чата знают стратегию «схема в центре» и как ею пользоваться.

## 3. Чем доказывается
- **A:** на сервере `DELIVERED_COMMIT` = закрывающий код; стенд с органом «Признаки» — глазами владельца; `main == origin/main`.
- **B (негатив):** `git log --diff-filter=D -1 -- development-docs/development-steps/new-steps/201/201-main.md` находит коммит; каждое утверждение
  навыков о признаках сверено со строкой кода — утверждений без строки 0.

## 4. На что опирается
201-1…201-7.

## 5. Чего не входит
Вынос реестра на субдомен.
