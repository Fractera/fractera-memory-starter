# Ответ памяти: запись

**Род операции:** запись (recording). Фраза: «я не в Мадриде живу, я в Барселоне».

**Что распознано:** это ИСПРАВЛЕНИЕ уже известного значения, а не добавление второго.
У человека `eval-193-3-without` род `city_where_he_lives_now` уже нёс значение «Мадрид»;
человек отрицает его («не в Мадриде») и называет взамен «Барселона». Поэтому род остался
полем, а не превратился в список: списком он стал бы, если бы человек ДОБАВЛЯЛ второй
город, не отменяя первого.

**Что сделано со складами:**
- новых родов не заведено — подходящий уже существовал (`city_where_he_lives_now`);
- в существующий род записано новое значение: **Барселона**;
- прежнее значение **Мадрид** ушло в историю этого рода (не стёрто, а сдвинуто);
- род значения — `fact` (сказано человеком), основание — прямое исправление в самой фразе;
- список не порождён, `promote_to_list` не звался — оснований для второго значения нет.

**Текущее состояние рода:** `city_where_he_lives_now = Барселона` (актуальное),
`Мадрид` — в истории как прежнее.

**Уровень глубины, фактически достигнутый:** 1 — то, что уже известно о человеке
(`what_i_already_know`). Глубже подниматься не понадобилось: род нашёлся сразу, граф,
вектор и объектное хранилище не звались.

**Охват (scope):** не назван. Время перехода из Мадрида в Барселону из фразы не следует —
записано «сейчас живёт в Барселоне» без даты и без периода. Если зовущему нужна датировка
(с какого момента верно) — этого в памяти нет, и уверенного умолчания я не ставлю.

**Судьба присланных параметров:** прислан только `who` = `eval-193-3-without` — принят и
использован во всех обращениях. Других параметров не приходило. Цепочка поиска не
запрашивалась, поэтому поля цепочки в ответе нет.

**Не сделано и названо вслух:** оценку этой работы я не выставляю — вердикт за тем, кто
звал; цикл считается незакрытым, пока вердикта нет.
