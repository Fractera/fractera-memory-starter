// ДОГОВОР СЛУЖБЫ ПАМЯТИ — ЕДИНСТВЕННЫЙ ИСТОЧНИК ТОГО, ЧТО МИР О НЕЙ ЗНАЕТ.
//
// 🔒 ДОГОВОР — ЭТО И ЕСТЬ ЧЁРНЫЙ ЯЩИК. Решение владельца 2026-09-09, дословно:
// «мы делаем память как чёрный ящик, который должен принимать и возвращать
// данные, и те процессы, которые у них происходят внутри, она должна
// производить внутри самостоятельно. То есть это не агент какой-то снаружи
// должен уметь это делать… Агент снаружи говорит: на тебе это и сделай то, что
// надо».
//
// Отсюда: снаружи виден ТОЛЬКО этот файл. Сколько внутри хранилищ, есть ли
// реестр, когда заводится таблица — частное дело службы, и в договоре этого
// нет и не будет.
//
// 🔒 ИЗ ДОГОВОРА ПОРОЖДАЕТСЯ ВСЁ ОСТАЛЬНОЕ, И ЭТО ТРЕТЬЯ ГАРАНТИЯ ОТДЕЛЬНОСТИ.
// Схемы инструментов агента приходят из `GET /v1/contract` ПО HTTP, а не
// импортом файла. Пока схема берётся импортом — отдельность есть
// договорённость; когда по HTTP — факт.
// ✗ ОПЛАЧЕНО 2026-09-09: `scripts/agent/intake-preloader.js:721` в репозитории
// бота делал `import(path.join(__dirname,"..","..","lib","memory","decl.mjs"))`.
// Наш внутренний псевдотип `value` уехал через этот импорт в схему инструмента
// агента, API Anthropic её отверг — и `memory_write` был недостижим СУТКИ, при
// том что `tools/list` показывал все двенадцать инструментов, а дверь отвечала
// `ok:true`. Две вещи, которые не должны знать друг о друге, сломались вместе.

/**
 * Версия договора. Меняется по правилам semver:
 * major — сломали существующий метод · minor — добавили метод · patch — правка текста.
 *
 * 🔒 ВЕРСИЯ ЕСТЬ С ПЕРВОГО ДНЯ, А НЕ «КОГДА ПОНАДОБИТСЯ». Служба без версии —
 * служба, которую нельзя менять, не ломая потребителя: он не сможет ни узнать
 * о смене, ни объяснить свой отказ.
 */
// 🔒 2.5.1 — PATCH (195-13): исправлено ОБЕЩАНИЕ БЕЗ ИСПОЛНИТЕЛЯ. Описание `remember.media` говорило «страницы кладутся методом ссылок», а
// метода для ссылок в договоре нет и в этом шаге не появится (слово владельца: метод `keep_links` уезжает в очередь). Слой ссылок живёт
// внутри памяти и открыт человеку стендом, а не договором. Методы не менялись — только текст, поэтому PATCH.
// 🛑 ОБЕЩАНИЕ, КОТОРОГО ДОГОВОР НЕ ДЕРЖИТ, ХУЖЕ ОТСУТСТВУЮЩЕГО: зовущий строит на нём и узнаёт правду отказом.
import { outputSchemaOf } from "./lib/output-schema.mjs"

export const CONTRACT_VERSION = "5.0.0"

// 🔒 5.0.0 — MAJOR (шаг 207, 2026-09-16). Договор переписывается ИЗ ЗАМЫСЛА ВЛАДЕЛЬЦА
// (`development-docs/the architect's note), а не правится из прежнего. Его слово: «это не
// просто какая-то новая версия пять настолько сильно новая версия пять что я думаю что существующая
// будет нам больше мешать чем помогать».
// Что уже сломано этим шагом: у `remember` и `recall` УДАЛЁН обязательный `who` — человек у памяти
// один, и требование называть его не давало ни одного различимого ответа. Взамен пришёл `from` —
// путь источника (какая служба и какая её сущность прислала сообщение), у ответа появилось имя
// `answer_id`, а у ответа «Спросить» — обязательный атрибут твёрдости `certainty`.
// Что сломается дальше в этом же шаге: объектные методы и служебные глаголы станут адресами (207-5,
// 207-6), и глаголов останется три: сказать · спросить · прокомментировать.
// 🔒 4.1.0 — MINOR (207-2): у `remember` появился НЕОБЯЗАТЕЛЬНЫЙ `from` — путь источника: какая
// служба и какая её подчинённая сущность прислала сообщение. Прежние вызовы не изменились ни в чём.
// Решение владельца 2026-09-16: «Назначение этого поля это то какой микро servis прислал свое
// сообщение… про сервис Data имеет подчинённые сущности реляционная база данных объектные
// хранилище… могут быть запрос от Telegram бот владельцы например Рома Армстронг или запрос от
// агента этого чата».
// 🛑 ВЕРСИЯ ПОДНЯТА СЕЙЧАС, А НЕ ВМЕСТЕ С 5.0.0 В 207-6: параметр уже принимается живой дверью, и
// договор, молчащий о принимаемом параметре, — то же обещание наоборот.

// 🔒 4.0.0 — MAJOR, ПОТОМУ ЧТО ПАМЯТЬ ЖИВЁТ ОДНОЙ ТАБЛИЦЕЙ (шаг 206, 2026-09-15). Решение владельца
// дословно: «я хочу чтобы наша память вообще прекратила работать с какими-либо специализированными
// таблицами кроме одной-единственной который фиксирует все входящие сообщения… память это события
// вокруг базе… для граф для вектора и для въездного хранилища». И там же о входе: «Из параметров
// входящего сообщения перестаем ожидать не только реестр признаков а также тип сообщения с целью
// добавить записи — запись это теперь работа только памяти. Также не ждём информацию о том является
// ли это сообщение продолжение предыдущего… вход теперь это просто текст и/или какие-то объекты».
// Что остаётся на входе (его же слова): **text, объекты, записи календаря, гео, канал**.
// Ушли у «Сказать»: `need_table` (форму записи решает память), `thread` и `deny` (продолжение
// прежнего разбора). Ушли у «Спросить»: `history` и `prior` (предыдущий разговор). Из ответа ушло
// поле `thread`.
// Что пришло взамен: сказанное короче 2000 слов ложится в связи И в вектор; длиннее — целиком в
// объектное хранилище (206-2).

// 🔒 3.0.0 — MAJOR, ПОТОМУ ЧТО ПАРАМЕТРЫ И АДРЕСА УДАЛЕНЫ (2026-09-15). Решение владельца: «мы не
// будем использовать реестр признаков в проекте вообще… очисти любое упоминание». Ушли: параметр
// `features` у `remember` и у `recall`, адреса каталога `GET /v1/features` и `GET /v1/features/{ключ}`,
// адреса подключений элементов. Остальные вызовы не изменились ни в чём: кто не слал `features`,
// получает ровно то же, что и вчера.
// 🛑 ВЕРСИЯ ПОДНЯТА ЧЕСТНО: удаление параметра ломает того, кто его слал, даже если таких сегодня нет.
// 🪦 ВЗАМЕН ТОГДА ПРИШЁЛ ОТДЕЛЬНЫЙ МОДУЛЬ ПОДБОРА РОДОВ — модель сопоставляла вопрос с таблицами и колонками
// человека. Снят 206-6: с 206-3 чтение отвечает из одной таблицы без единого хода модели, и звать
// его стало некому. Цепочка сегодняшнего чтения — договор §4.

// 🔒 2.10.0 — MINOR (201-5, требование владельца 2026-09-14): у `remember` необязательный `via` — канал, которым
// пришло сообщение. Он и имя человека попадают во ВВОДНУЮ часть записи связей: «Входящее сообщение из Telegram
// от Ромы Армстронга, 2026-09-14», ниже — якоря. Без этого сущностью графа становится
// технический ключ человека, и вопрос о нём по имени не находит ничего. Прежние вызовы не изменились.

// 🔒 2.7.0 — MINOR (200-6): у `remember` и `recall` есть `output` — JSON Schema ответа, порождённая из `lib/output-schema.mjs`. В каждом
// ответе обоих глаголов, и в отказе тоже, — `text` (перечисление фактов и объектов) и `objects` (идентификаторы). `recall` с вопросом
// ищет и объекты по смыслу. Слово владельца: «Только объект который включает в себя текст и идентификаторы объектов». Прежние поля остались.

// 🔒 2.6.0 — MINOR (200-5): вложения — включения в «Сказать». `remember` принимает тело `multipart/form-data` (часть `payload` —
// прежний JSON, части `files` — файлы) и параметры `links` (страницы) и `youtube` (ролики) с проверкой рода: ролик в `links` —
// `link-is-youtube`, не ролик в `youtube` — `youtube-not-youtube`. Слово владельца 2026-09-14: «всё остальное это включение в эти
// два типа». Прежние вызовы не изменились ни в чём: JSON-тело и `media` работают как были.

// 🔒 2.5.0 — MINOR (194-17): методы `find_objects` и `open_object`, адрес каталога `GET /v1/objects/{id}/file`.

// 🔒 2.4.0 — MINOR (194-16): `keep_object` принимает и JSON с `url`; `remember.media` получил исполнителя — каждый
// адрес идёт тем же путём, и ответ несёт `objects`. Род вложения больше не называется: его определяет память.

// 🔒 2.3.0 — MINOR (194-15): метод `keep_object` — вещь целиком, телом multipart/form-data. У метода
// появилось поле `body`; прежним методам оно не нужно — их тело JSON, как и было.

// 🔒 2.2.0 — MINOR (185-3): охват принимает КООРДИНАТЫ (lat, lon, radius_m), а
// запись — ВЛОЖЕНИЯ (media). Прежние вызовы не изменились ни в чём.
// 🔒 ПОЧЕМУ КООРДИНАТЫ, А НЕ ТОЛЬКО НАЗВАНИЕ МЕСТА: телефон присылает точку, а
// не слово «Мадрид». Требуя слово, мы теряли бы точность ДО памяти и молча.
//
// 🔒 2.1.0 — MINOR (184): у `remember` добавлен НЕОБЯЗАТЕЛЬНЫЙ `thread` — имя
// нити разбора, — и тот же `thread` приезжает в ответе. Прежние вызовы не
// изменились ни в чём: кто его не шлёт, получает ровно то же, что и вчера.
// 🔒 ЗАЧЕМ ОН ЕСТЬ, ОДНОЙ ФРАЗОЙ: без возврата к прежней цепочке опровержение
// вывода не значит ничего — это слово владельца 2026-09-11, ставшее законом.
// Продолжение нити при этом ДЕШЕВЛЕ нового вызова (измерено, договор §10).

// 🔒 2.0.0 — MAJOR, ПОТОМУ ЧТО ПАРАМЕТР УДАЛЁН, А НЕ ПОТОМУ ЧТО ПРАВКА КРУПНАЯ
// (183-7). Решение владельца 2026-09-11: «событий календаря и геометок может
// быть множество». Одиночные `at` и `place` заменены списком `scope` —
// записями вида `{at, place}`.
// 🪦 `at` и `place` прожили один день (1.5.0, шаг 183-6). Оставить их рядом со
// списком «для простого случая» значило бы завести ДВА пути сказать одно и то
// же: зовущие разошлись бы, и половина охватов поехала бы мимо списка. Версия
// поднята честно — недолгая жизнь параметра не делает его удаление безобидным.
//
// 🔒 1.5.0 БЫЛ MINOR, И ЭТО ОСТАЁТСЯ ВЕРНЫМ (183-2…183-6): тогда параметры
// только ДОБАВЛЯЛИСЬ — `depth`, `history`, `prior`, `want_chain`, `deny`,
// `need_table`, — и ни один прежний вызов не изменился.
//
// 🔒 У КАЖДОГО ИЗ НИХ В ОТВЕТЕ ЕСТЬ СТРОКА СУДЬБЫ — поле `params`: принят и
// подействовал · принят, способности нет · не той формы. Параметр, о котором
// ответ молчит, неотличим от параметра, которого нет: агент шлёт, ничего не
// происходит, и ни отказа, ни записи (закон шага 143).

/** Имя службы — как она называет себя сама, а не как её зовёт кто-то снаружи. */
export const SERVICE = "fractera-memory"

/**
 * МЕТОДЫ — ВОСЕМЬ: сказать, спросить, «кого ты знаешь», два о самом журнале
 * и три об объектах (положить, найти по смыслу, открыть).
 *
 * ✗ ЗДЕСЬ СТОЯЛО «ПЯТЬ» — и стояло с 194-15, когда приехали объекты. Найдено
 * шагом 205-11 сверкой замысла с кодом: сторож `npm run check` всё это время
 * печатал «методов 8», а комментарий рядом говорил «пять». Пятый раз в проекте.
 *
 * 🔒 ЧЕТВЁРТЫЙ И ПЯТЫЙ ЗАВЕДЕНЫ 2026-09-10 (177-2) И ОТЛИЧАЮТСЯ ОТ ПЕРВЫХ ТРЁХ
 * ПРЕДМЕТОМ: те отвечают о ЧЕЛОВЕКЕ, эти — о САМОЙ СЛУЖБЕ. Решение владельца:
 * «каждый из этих слоёв [должен иметь] Output для логов, чтобы ты полностью
 * понимал, что внутри происходит». Агенту закрыт вход в слой — значит слой
 * рассказывает о себе сам, и рассказ этот такой же метод договора, как прочие.
 * 🛑 РАССКАЗ О РАБОТЕ НЕ ЕСТЬ ЗНАНИЕ О ЧЕЛОВЕКЕ, и путать их нельзя: `recall`
 * отвечает, что память ЗНАЕТ, `journal` — что она ДЕЛАЛА.
 *
 * 🪦 В 175-1 ЗДЕСЬ БЫЛ НОЛЬ, и это было правильным состоянием: пустой договор
 * лучше договора, обещающего непостроенное. Метод, который виден и не работает,
 * тратит ход модели на вызов и ход на разбор отказа.
 *
 * 🔒 ДВА ГЛАВНЫХ — СКАЗАТЬ И СПРОСИТЬ. Запись меняет состояние, чтение нет;
 * слить их в один «расскажи памяти что-нибудь» значило бы потерять и
 * повторяемость, и безопасность.
 * 🔒 ТРЕТИЙ ЗАВЕДЁН ПО НУЖДЕ, А НЕ ВПРОК: экрану надо кого-то спросить, а ключ
 * человека живёт в теге сообщения Telegram и больше нигде. Наружу уходят ЛЮДИ,
 * а не таблицы — вопрос о знании, а не о хранении.
 *
 * 🛑 ЧИСЛО В ЭТОМ КОММЕНТАРИИ ПРАВИТСЯ ВМЕСТЕ СО СПИСКОМ. Рукописное число не
 * двигается само никогда — в проекте это оплачено четырежды за три дня, и
 * дважды автором закона об этом.
 */
export const METHODS = [
  {
    name: "remember",
    // 🔒 2.6.0 (200-5): вложения едут ВНУТРИ «Сказать», а не отдельными дверями — слово владельца: «всё остальное это
    // включение в эти два типа». JSON — как прежде; с файлами — форма: часть `payload` — это же JSON, части `files` — файлы.
    body: "json | multipart",
    // 🔒 СХЕМА ОТВЕТА ПОРОЖДЕНА ИЗ `lib/output-schema.mjs` (200-6) — тем же объявлением прибор проверяет живые ответы.
    output: outputSchemaOf("remember"),
    about:
      "Сказать памяти то, что сказал человек — обычной фразой, как он её произнёс. " +
      "Куда это ляжет, заводить ли новое место и что делать с противоречием — решает память. " +
      "Вложения едут внутри этого же запроса: адреса файлов (media), страницы (links), ролики YouTube (youtube) и сами файлы — " +
      "телом multipart/form-data, где часть payload — JSON этого тела, а части files — файлы.",
    params: [
      // 🪦 `who` СНЯТ 207-3: человек у памяти один — архитектор, и требование называть его каждый раз не давало
      // ни одного различимого ответа (слово владельца: «зачем будем создавать колонку в которой нет никакой разницы»).
      // Взамен у записи есть `from` — откуда она пришла. Прежние вызовы с `who` не ломаются: лишнее поле игнорируется.
      { name: "text", type: "string", required: false, about: "Фраза человека как есть, без пересказа. Необязательна, когда есть вложение: вещь без слов — это тоже «Сказать» (207-5)" },
      { name: "lang", type: "string", required: false, about: "Язык слов для человека: ru или en. Не назван — русский; незнакомый — английский" },
      { name: "scope", type: "array", required: false, about: "Охват: СПИСОК записей вида {at, place, lat, lon, radius_m}. Дата — гггг-мм-дд; место — словом; координаты — парой чисел, с необязательным радиусом в метрах. Записей бывает много: у одной фразы может быть несколько дат и мест. Пустой охват значит «не знаю где и когда», а не «везде и всегда»" },
      { name: "media", type: "array", required: false, about: "Вложения: список {url} — адреса файлов. Каждый файл память скачивает и кладёт тем же путём, что keep_object: описание, объектное хранилище, карточка поиска, граф, строка таблицы. Род определяет сама память. Страница (text/html) отвергается кодом is-a-page: страницам место в links, роликам — в youtube" },
      { name: "files", type: "string", format: "binary", required: false, about: "Сами файлы — части files тела multipart/form-data; остальное тело — JSON в части payload. Каждый файл ложится тем же путём, что keep_object; род определяет память: аудио, видео, изображение, PDF, документ, код" },
      { name: "links", type: "array", required: false, about: "Страницы: список адресов http(s) строками. Каждую открывает ИИ-браузер, описывает память и кладёт слоем ссылок. Ролик YouTube здесь отвергается кодом link-is-youtube — его место в youtube" },
      { name: "youtube", type: "array", required: false, about: "Ролики YouTube: список адресов строками. Данные и главы читает официальный API YouTube, память описывает и кладёт слоем ссылок. Не ролик здесь отвергается кодом youtube-not-youtube — обычной странице место в links" },
      { name: "via", type: "string", required: false, about: "Канал, которым пришло сообщение: Telegram, почта, стенд. Попадает во вводную часть записи связей вместе с именем человека и датой — по ним запись потом и находят" },
      { name: "from", type: "array", required: false, about: "Откуда пришло: путь источника от службы к её подчинённой сущности — [\"chat\",\"telegram-bot\",\"roma-armstrong\"] или строкой через косую черту. Первый сегмент обязан быть именем службы из реестра сервера (SERVICES.json); незнакомое имя отвергает вызов целиком. Хвост свободен и растёт вправо: у службы бывает сущность, у сущности — своя" },
    ],
    returns:
      "what_happened — словами, которые можно произнести человеку; noted — что именно записано, " +
      "с родом утверждения у каждой записи. " +
      "При поправке сказано вслух, что она принята, и назван род значения. 🪦 Прежнее значение («было X, стало Y») больше не называется: оно жило колонкой, а с 206-1 факт стал событием графа. " +
      "params — судьба каждого необязательного параметра: принят и подействовал, принят без способности, либо не той формы. " +
      "objects — судьба каждого вложения — files, media, links, youtube: id объекта, messageId и род либо отказ с причиной " +
      "(у ссылки чужого рода — link-is-youtube или youtube-not-youtube; уже сохранённая ссылка — existing и её messageId). " +
      "kept_whole — судьба сказанного целиком: оно ложится в связи ВСЕГДА, даже когда фактов о человеке во фразе нет, " +
      "и якоря этой записи названы. used_model — звалась ли модель разбора. " +
      "🛑 Нить старше своего срока не находится: ответ тогда прямо говорит, что её больше нет.",
    onMiss:
      "Фактов о человеке во фразе нет — ok:true и пустой noted, это не ошибка; сказанное при этом сохранено целиком. " +
      "Если разобрать нельзя, приходит refusal с причиной: кончились деньги, ключ отвергнут или модель недоступна.",
  },
  {
    // 🔒 ТРЕТИЙ ГЛАГОЛ (207-4). Решение владельца 2026-09-16: запрос к памяти бывает трёх родов —
    // добавить · извлечь · «обратная связь по предыдущему ответу». Третьего входа у памяти не было
    // вовсе, при том что ответ с 206-10 сам приглашает человека поправить его.
    name: "feedback",
    output: outputSchemaOf("remember"),
    about:
      "Прокомментировать предыдущий ответ памяти — понравился он или нет и что с ним не так. " +
      "Комментарий ложится строкой в ту же таблицу и связывается с прокомментированным ответом.",
    params: [
      { name: "about", type: "string", required: true, about: "Имя ответа, о котором речь: answer_id, возвращённый «Сказать» или «Спросить» (ans_<номер>)" },
      { name: "text", type: "string", required: true, about: "Сам комментарий — словами человека, как он их сказал" },
      { name: "from", type: "array", required: false, about: "Откуда пришёл комментарий: тот же путь источника, что у «Сказать»" },
      { name: "lang", type: "string", required: false, about: "Язык слов для человека: ru или en" },
    ],
    returns:
      "message_id — строка, которой лёг комментарий; answer_id — имя уже этого ответа; " +
      "about — на какой ответ он ссылается; linked — записалась ли связь с ним; " +
      "evolution — что с комментарием произойдёт дальше. Сегодня это честное not-built: " +
      "навык эволюции памяти ещё не построен, и делать вид, что запущено обучение, нельзя.",
    onMiss:
      "Имя ответа не той формы — bad-answer-id; такого ответа нет — unknown-answer, и ничего не записано: " +
      "комментарий без предмета разбирать было бы нечем.",
  },
  // 🪦 «КОГО ТЫ ЗНАЕШЬ» СНЯТ 207-6 ВМЕСТЕ СО СВОИМ ПРЕДМЕТОМ. Он считал ЛЮДЕЙ, а человек у памяти
  // один — архитектор; с 207-3 ответ был бы всегда одинаковым. Разница есть у другого вопроса, и
  // он живёт адресом: GET /v1/sources — какая служба и какая её сущность мне писала.
  {
    name: "recall",
    // 🔒 СХЕМА ОТВЕТА ПОРОЖДЕНА ИЗ `lib/output-schema.mjs` (200-6).
    output: outputSchemaOf("recall"),
    about:
      "Спросить память о человеке. Без вопроса отдаёт всё, что знает, — это мгновенно и не стоит вызова модели. " +
      "С вопросом сначала ищет механически.",
    params: [
      // 🪦 `who` СНЯТ 207-3 и здесь: знание памяти общее, вопрос «о ком» отвечал сам себе.
      { name: "text", type: "string", required: false, about: "Вопрос человеческой фразой. Без него придёт всё" },
      { name: "lang", type: "string", required: false, about: "Язык слов для человека: ru или en. Не назван — русский; незнакомый — английский" },
      { name: "depth", type: "string", required: false, about: "Предел глубины словами: standard · deep · extreme. Номера уровней остаются внутри памяти — наружу идут слова и цена" },
      { name: "want_chain", type: "boolean", required: false, about: "Возвращать ли цепочку шагов поиска. Умолчание — нет: переполнять свой контекст решает тот, кто спросил" },
      { name: "scope", type: "array", required: false, about: "Охват вопроса: тот же СПИСОК записей {at, place, lat, lon, radius_m}, что у записи. Ответ подбирается под место и время вопроса: одно и то же спрошенное в другом городе — другой вопрос" },
      { name: "from", type: "array", required: false, about: "Откуда пришёл вопрос: тот же путь источника, что у «Сказать». На ОТВЕТ он не влияет — знание памяти общее; он остаётся следом того, кто спрашивал" },
    ],
    returns:
      "known — что известно; у каждого значения есть claim (сказано человеком или выведено) " +
      "и basis (из чего выведено). " +
      "not_yet_known — чего не хватает и почему это важно; used_model — звалась ли модель. " +
      "depth_asked и depth_used — какой предел просили и до какого уровня память дошла НА САМОМ ДЕЛЕ. " +
      "used_input — что из присланного контекста участвовало в поиске. " +
      "chain — шаги поиска, и только если их попросили. " +
      "params — судьба каждого необязательного параметра.",
    onMiss:
      "О человеке ничего не записано — ok:true и пустой known. " +
      "Вопрос не совпал ни с чем известным — приходит всё известное и сказано, что точного совпадения нет.",
  },
  // 🪦 ДВА ГЛАГОЛА О САМОЙ СЛУЖБЕ СНЯТЫ 207-6: рассказ памяти о своей работе и его стирание.
  // Они отвечали не о человеке, а о службе, и стояли в одном списке с «сказать» и «спросить» —
  // отчего конструкция владельца «два входящих действия и одно исходящее» снаружи не читалась.
  // Способность цела и живёт адресами: GET /v1/journal и DELETE /v1/journal.
  // 🪦 ТРИ ОБЪЕКТНЫХ ГЛАГОЛА СНЯТЫ 207-5, И ЭТО НЕ ПОТЕРЯ СПОСОБНОСТИ, А ПЕРЕНОС ЕЁ НА МЕСТО.
  // Здесь жили: положить вещь целиком, найти вещи по смыслу вопроса и открыть найденное.
  // Каждый из них был назван ПО ХРАНИЛИЩУ — «положи в склад», «поищи в складе», — а склад перестал
  // быть предметом разговора ещё шагом 206: память ведёт одну таблицу, всё остальное — события
  // вокруг неё. Метод, названный по хранилищу, воскрешал бы отменённую архитектуру каждым вызовом.
  // 🔒 КУДА ЭТО ПЕРЕЕХАЛО: вещь кладётся «Сказать» — с вложением и пустым текстом, если слов нет;
  // вещи ищутся «Спросить» — он и так возвращает objects по смыслу вопроса; открывается вещь
  // АДРЕСОМ GET /v1/objects/{id}, потому что это получение вещи по имени, а не действие над памятью.
  // Слово владельца 2026-09-14: «всё остальное это включение в эти два типа».
]

/**
 * ЧТЕНИЕ КАТАЛОГА — не методы, а адреса, и это намеренно.
 *
 * 🔒 КАТАЛОГ ОТВЕЧАЕТ НА ВОПРОС «ЧТО У ТЕБЯ ЕСТЬ», А МЕТОДЫ — «СДЕЛАЙ». Смешав
 * их в один список, мы предложили бы агенту думать о хранении наравне с делом.
 *
 * 🪦 ДВУХ АДРЕСОВ КАТАЛОГА ТАБЛИЦ ЗДЕСЬ БОЛЬШЕ НЕТ (206-6): память ведёт одну
 * таблицу входящих сообщений, а где искать — решает она сама, и спрашивать её
 * об именах хранилищ стало не о чем.
 */
export const CATALOGUE = [
  // 🔒 207-6: ТРИ АДРЕСА ВЗАМЕН ТРЁХ СНЯТЫХ ГЛАГОЛОВ. Они отвечают на вопрос «что у тебя есть» и
  // «покажи вот это», а не «сделай» — и потому живут здесь, а не среди глаголов.
  { path: "POST /v1", about: "слова человека, который не назвал глагол: память сама относит их к одному из трёх — запомнить, спросить, прокомментировать — и говорит в первой строке ответа, КАК она это поняла. Не четвёртый глагол: имени глагола в пути нет, потому что зовущий его не назвал" },
  { path: "GET /v1/state", about: "где я нахожусь: мой субдомен и порт по реестру служб, каким потоком меня позвали — защищённым по домену или голым адресом, — и род входа. Расхождение задуманного с увиденным называется, а не сглаживается" },
  { path: "GET /v1/signals", about: "что люди сказали о наших ответах: сигналы — пожелания, повторённые РАЗНЫМИ словами, — и единичные пожелания отдельно. Основания у каждого сигнала названы поимённо: слова людей читают, а не верят счёту" },
  { path: "GET /v1/sources", about: "кто мне писал: пути источников с числом сообщений и службы, к которым они относятся" },
  { path: "GET /v1/journal", about: "рассказ памяти о своей работе: что пришло, что решено, что отброшено и почему" },
  { path: "DELETE /v1/journal", about: "стереть рассказ памяти о работе. Знание о людях не затрагивается. НЕОБРАТИМО" },
  // 🪦 ДВЕ ДВЕРИ КАТАЛОГА ТАБЛИЦ УБРАНЫ 206-6 ВМЕСТЕ С ПРЕДМЕТОМ: список имён и
  // описание одного имени. Специализированных таблиц у памяти больше нет — список
  // измеренно отдавал пустоту, то есть обещал способность, которой нет.
  { path: "GET /v1/objects/{id}", about: "карточка вещи и её текст частями: id из поля objects любого ответа. Не глагол, а получение вещи по имени" },
  { path: "GET /v1/objects/{id}/file", about: "сам файл объекта — байты с его типом и именем; id из поля objects любого ответа" },
]

/**
 * Договор в том виде, в каком его отдаёт `GET /v1/contract`.
 *
 * 🔒 ОТДАЁТСЯ ПОРОЖДЁННОЕ, А НЕ ВТОРАЯ РУКОПИСНАЯ КОПИЯ. Список, написанный
 * руками рядом с кодом, расходится с ним молча — в этом проекте измерено
 * четырежды за три дня («пять инструментов» при шести папках и так далее).
 */
export function contract() {
  return {
    catalogue: CATALOGUE,
    service: SERVICE,
    version: CONTRACT_VERSION,
    methods: METHODS.map((m) => ({
      about: m.about,
      ...(m.body ? { body: m.body } : {}),
      name: m.name,
      onMiss: m.onMiss,
      ...(m.output ? { output: m.output } : {}),
      params: m.params,
      returns: m.returns,
    })),
  }
}
