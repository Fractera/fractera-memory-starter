// РАБОЧАЯ ПАПКА ТЕРМИНАЛА — ОДНА ФУНКЦИЯ ДЛЯ МОСТА И ДЛЯ СТРАНИЦЫ (202-9).
//
// 🔒 РАБОЧАЯ ПАПКА — ДЕРЕВО ПАМЯТИ, А НЕ ПАПКА БОТА (решение 180). Закон проекта: рабочая папка есть личность агента.
// 🎯 С 202-9 ЕЁ НАЗЫВАЕТ И СТРАНИЦА: «Агент будет запущен в рабочем пространстве: …» (слово владельца 2026-09-15).
// 🔒 ПРАВИЛО ЖИВЁТ В ОДНОМ МЕСТЕ: скопированное в страницу, оно разошлось бы с мостом на первой правке — страница назвала бы одну папку, а
// агент запустился бы в другой, и человек узнал бы это, когда Claude Code правит не те файлы.

import { existsSync } from "node:fs"

export function workspaceDir() {
  const named = process.env.AGENT_WORKSPACE || "/opt/fractera/memory"
  return existsSync(named) ? named : process.cwd()
}
