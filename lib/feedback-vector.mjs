// КОММЕНТАРИЙ ЧЕЛОВЕКА УЕЗЖАЕТ В ВЕКТОР — ЧТОБЫ СИГНАЛ СЧИТАЛСЯ ПО СМЫСЛУ (218-18).
//
// 🎯 ЗАКОН, РАДИ КОТОРОГО ЭТО СТРОИТСЯ, СУЩЕСТВОВАЛ С 213-1 И НЕ ИСПОЛНЯЛСЯ НИ ДНЯ: сигнал — это
// пожелание, повторённое РАЗНЫМИ СЛОВАМИ. Код при этом склеивал комментарии по совпадению
// ОДИНАКОВЫХ слов. Закон и механизм смотрели в противоположные стороны, и чем лучше человек
// перефразировал, тем меньше был шанс быть услышанным.
//
// ✗ ИЗМЕРЕНО 2026-09-17 ПРОГОНОМ САМОЙ ФУНКЦИИ НА ДВУХ ФРАЗАХ ОДНОГО ПОЖЕЛАНИЯ:
//   «в ответе нет должности Пети…» → ответе · должности · пети · хочу · видеть · работает
//   «не хватает роли человека…»    → хватает · роли · человека · укажи · петя · занимается
//   общих слов НОЛЬ, совпадение 0.00 при пороге 0.50.
// 🛑 Даже «Пети» и «Петя» не совпали: русский падеж разводит одного человека на двоих — тот же
// класс, что уже оплачен реестром признаков («Мише» ≠ «Миша»).
//
// 🔒 КОРЕНЬ БЫЛ НЕ В ПОРОГЕ, А В ОТСУТСТВИИ МАТЕРИАЛА. `putSaidVector` зовётся ровно в одном месте —
// в глаголе «Сказать». Комментарий вектора не получал вовсе, поэтому сравнивать по смыслу было
// НЕЧЕМ, и сравнение по словам стояло как временное.
//
// 🔒 КОЛЛЕКЦИЯ У ОТЗЫВОВ СВОЯ, И ЭТО ЗАЩИТА, А НЕ АККУРАТНОСТЬ. Лягут комментарии в `memory-said` —
// и «Спросить» начнёт находить отзывы о работе памяти как факты о жизни человека: «ответ слишком
// длинный» встанет рядом с «Петя работает в Мадриде». Разделение проверяется негативным контролем,
// а не обещается.

import { dataCall } from "./data-call.mjs"

/** Коллекция комментариев о наших ответах. Слова человека о жизни лежат отдельно (`memory-said`). */
export const FEEDBACK_COLLECTION = "memory-feedback"

/**
 * Имя куска = номер строки отзыва. Запись идемпотентна: повторная кладёт тот же id поверх.
 *
 * 🛑 БЕЗ СЛЭША ВНУТРИ, И ЭТО НЕ ВКУС: удаление у склада адресное — `DELETE /vectors/<id>`, — и слэш
 * в имени разрезал бы адрес. Так же устроены объекты (`obj-<id>`); у сказанного имя со слэшами, и
 * удалять его поштучно нечем.
 */
export const vectorIdOf = (messageId) => `feedback-${messageId}`

/**
 * Насколько два комментария об одном.
 *
 * 🔒 ПОРОГ ИЗМЕРЕН НА КОРПУСЕ КОММЕНТАРИЕВ, А НЕ ВЗЯТ У СКАЗАННОГО (`NEAR_SAID` = 0.40). Измеряется
 * не хранилище, а то, что в нём лежит, — тот же довод, по которому у объектов свой `OBJECT_NEAR`.
 * 🛑 ЖИВОЙ КОРПУС БЫЛ ПУСТ: 0 отзывов, измерено 2026-09-17 через `GET /v1/signals`. Значит замер
 * снят на собранном корпусе, и это сказано здесь, а не подразумевается.
 *
 * ЗАМЕР 218-18 НА ЖИВОЙ СЛУЖБЕ 2026-09-17 (`scripts/probe/signal-meaning.mjs`), шесть комментариев,
 * три пары «об одном разными словами» против двенадцати пар «о разном»:
 *   об одном разными словами: 0.530 · 0.597 · 0.646
 *   о разном, худшие случаи:  0.437 · 0.426 · 0.422 · 0.418 · … · 0.265
 *   граница:                  0.437 … 0.530 — пары НЕ пересекаются, одним порогом разделяются
 *   чужая фраза (борщ):       0.203 — негативный контроль самого прибора
 *
 * ✗ **И ПЕРВЫЙ ЖЕ НАСТОЯЩИЙ КОРПУС ПОПРАВИЛ ЭТО ЧИСЛО В ТОТ ЖЕ ДЕНЬ (218-20).** Живая пара об одном —
 * «в ответе нет даты, когда это было сказано» и «не хватает времени события: укажи когда это
 * произошло» — дала **0.473**, то есть НИЖЕ порога 0.50: настоящий сигнал не собрался, и оба
 * комментария остались единичными. Заявка из другой темы в том же замере дала 0.282.
 *
 *   об одном (7 пар):  0.473 · 0.530 · 0.597 · 0.646 …
 *   о разном:          0.437 и ниже
 *   граница СУЗИЛАСЬ:  0.437 … 0.473 — пары по-прежнему не пересекаются, но зазор мал
 *
 * ✗ **И ТРЕТИЙ ЖИВОЙ КОРПУС ПОПРАВИЛ ЧИСЛО СНОВА, В ТОТ ЖЕ ДЕНЬ (218-22).** Пара «укажи время события,
 * без него непонятно когда» ↔ «в ответе нет даты, когда это было сказано» — **0.418**, ниже порога
 * 0.45: обе о времени, обе с темой `time`, и сигнал опять не собрался.
 *
 * 🔒 **ПОРОГ ОПУЩЕН ДО 0.40 ИМЕННО ПОТОМУ, ЧТО У РЕШЕНИЯ ПОЯВИЛСЯ ВТОРОЙ ПРИЗНАК.** Пока числo решало
 * в одиночку, низкий порог означал ложные склейки; с запретом по теме ложное отсекается темой, а не
 * числом. Измерено прибором на корпусе из 8 фраз и 28 пар:
 *   при 0.40 + тема: пропущено пар об одном 0 из 4 · ложно склеено 0 из 24
 *   одним числом склеилось бы ложных 10 — **тема сняла все десять**
 * 🛑 СЛАБОЕ МЕСТО НАЗВАНО: пара с ПУСТЫМИ темами защищена только числом, и при 0.40 она склеится
 * охотнее. В корпусе прибора таких пар нет — значит этого случая замер не видел, и он ждёт первого
 * живого примера.
 * ✗ Урок шире числа, и он повторился трижды за день: **порог, измеренный на фразах, которые я
 * придумал сам, описывает мою фантазию о людях.** Каждый новый настоящий корпус двигал его вниз.
 */
export const NEAR_FEEDBACK = 0.4

/** Положить комментарий в вектор. Отказ возвращается словами: молчаливый промах неотличим от записи. */
export async function putFeedbackVector({ id, text }) {
  const body = String(text ?? "").trim()
  if (!body) return { ok: false, refused: "empty-text" }
  const r = await dataCall("/vectors", {
    collection: FEEDBACK_COLLECTION,
    id: vectorIdOf(id),
    refId: String(id),
    refTable: "feedback",
    text: body,
  })
  return r.ok === false ? { ok: false, refused: r.error ?? "store-refused" } : { id: vectorIdOf(id), ok: true }
}

/**
 * Найти комментарии, близкие по смыслу к данному.
 *
 * Возвращает ВСЕ попадания со счётом, включая дальние: решение о пороге принимает зовущий, а прибор
 * обязан видеть и то, что порога не прошло, — иначе границу не на чем измерить.
 */
export async function nearFeedback({ k = 20, text }) {
  const q = String(text ?? "").trim()
  if (!q) return { hits: [], ok: false, refused: "empty-text" }
  const r = await dataCall("/vectors/search", { collection: FEEDBACK_COLLECTION, k, query: q })
  if (r.ok === false) return { hits: [], ok: false, refused: r.error ?? "store-refused" }
  const hits = (r.body?.results ?? []).map((p) => ({
    id: String(p.id),
    messageId: Number(String(p.id).replace(/^feedback-/, "")) || null,
    score: Number(p.score),
    text: String(p.text ?? ""),
  }))
  return { hits, ok: true }
}

/**
 * Тот же ли это текст.
 *
 * 🔒 ДОСЛОВНЫЙ ПОВТОР НЕ СЧИТАЕТСЯ ВТОРЫМ НАБЛЮДЕНИЕМ (закон 213-1): это тот же текст, а не второй
 * взгляд. Иначе достаточно нажать «отправить» дважды, чтобы переписать навык.
 * 🛑 СРАВНИВАЕМ НОРМАЛИЗОВАННО: разный регистр, лишние пробелы и точка в конце — это по-прежнему
 * ТОТ ЖЕ текст, а не второе наблюдение, и делать вид, что мы этого не видим, значит отдать закон
 * тому, кто поставит пробел.
 */
export const sameText = (a, b) => normalize(a) === normalize(b)

function normalize(text) {
  return String(text ?? "")
    .toLowerCase()
    .replace(/ё/g, "е")
    .replace(/[^a-zа-я0-9]+/g, " ")
    .trim()
}
